# Production Go-Live Readiness — Audit Report
Verdict: **NO-GO**        Confidence: **Medium**
Auditor: Claude Opus 4.8 (1M)   Date: 2026-06-25   Git HEAD: `8184174` ("Advance production readiness hardening"; supersedes `afc1595` — the prior audit's 161 dirty-tree fixes are now committed)   Branch: `codex/worktree-production-readiness`
Baseline re-verified: FULL_SYSTEM_E2E (2026-06-25, afc1595) — **24 FIXED / 0 REGRESSED / 2 residual** (1 operator-config P2, 1 cosmetic P3)

> Method: read-only. ~21 parallel sub-auditors over D1–D13 + prior-finding re-verification + static rot check, each finding adversarially refuted before landing. No engine boot to live/paper, no broker/feed connection, no secret value printed. Static integrity GREEN: `py_compile` clean (871 files), `node --check` clean (103 files), zero reachable money-path stubs.

## Snapshot
| Area                          | State | One-line note |
|-------------------------------|:-----:|---------------|
| Live money path & brokers     | ok    | Live authority needs a conjunction of independent gates that each default to *blocked*; durable atomic order idempotency; no single env var flips to live |
| Risk & safety controls        | warn  | Gross/net hard caps bind with a post-check backstop, but sizing **overlays fail OPEN** under degraded inputs (P1) |
| Data durability & DR          | warn  | Restore proven (RTO 43s, RPO ~120s), WAL on ZFS; **offsite/3-2-1 leg dark in running config** (P1) |
| Reliability & ops             | warn  | Clean graceful shutdown; **no alert delivery channel by default — pages go nowhere off-box** (P1); no WatchdogSec/MemoryMax |
| Observability & truthfulness  | warn  | Money-path reads fail-closed; **disk-fill / WAL-archiver outage raises no health blocker or alert** in safe/sim/paper/shadow (P1) |
| Security & secrets            | warn  | Mature fail-closed secrets/auth; no secrets in git history; plaintext-provider gate too narrow (P2); operator-local `.env` blocks `--live` (P2) |
| Dependencies & reproducibility| warn  | CPU/dev and GPU profile lockfiles now carry hashes and hash-verified installs are enforced for lock-backed manifests; remaining dependency work is profile compatibility hardening |
| Test & CI gate sufficiency    | warn  | Money-path lane runs on real Postgres+Redis and fails-closed on skips, but no property/invariant tests, no soak/chaos, engine/strategy has no coverage floor |
| Multi-asset enablement gating | warn  | Options/futures/crypto fenced; **FX has no live-disable-by-default gate** (P1) |
| Data feeds & credentials      | fail  | Correctly fail-closed, but **no paid market-data feed configured — no live price rail** (P1) |
| Models & promotion governance | warn  | Promotion + live-model integrity strong/fail-closed; CPCV/PBO leakage gate opt-in (P2) |
| Operator UX under incident    | warn  | Designed degraded states + audited modal; GO-R8 adds degraded Emergency Stop fallback and a visible module-load failure banner |

## Prior-finding re-verification (2026-06-25 E2E)
- **[FIXED]** Champion competition view 500 — `engine/strategy/champion_manager.py:239-250,1526,1562-1585` create-before-read + `{ok:true,status:'empty',reason:'no_competition_data'}` → 200.
- **[FIXED]** `/api/jobs/history` 500 read-connection bug — `engine/runtime/locks.py:212-236` reader now uses the main runtime DB (`connect_ro_direct`) + `ensure_job_history()`; regression test `tests/test_runtime_reliability_regressions.py:330-397`.
- **[FIXED]** Copilot/ask 500 — `dashboard_server.py:2299-2387` sets explicit `meta.status` on every branch (200/400/413/422/503); `http_transport.py:326-334` honors it.
- **[FIXED]** Weather subsystem 500 — `engine/runtime/dashboard_weather_widgets.py:85-226` `table_exists`/`_missing_tables` guards → 200 with reason.
- **[FIXED]** Operator `institutionalCheck` 500 — `boot/operator_server.js:5920-6029` gates engine calls behind RUNNING and returns `jsonState(200, ok:false)`.
- **[FIXED]** `/api/operator/clear_last_error` 404 — both snake/camel aliases registered (`boot/operator_server.js:6489-6490`, `engine/dashboard/routing.py:40-41`).
- **[FIXED]** `bootstrap`/`start` ungated + slow + 500 — `boot/operator_server.js:5911-5913,6156-6181` now `requireOperatorConfirmation` (422 on empty), bounded by `OPERATOR_START_REQUEST_TIMEOUT_MS`, distinct reason/status mapping.
- **[FIXED]** Degradation cluster (advisories/validation/audit-records/data-source delete) — `engine/api/degradation.py:24-88` standard guard wired across `api_ops_handlers.py:340-369`, `api_read_advanced.py:1498-1511`, `api_dashboard_reads.py:218-248`, `routes/data_sources_routes.py:348-382` (builtin delete → 403).
- **[FIXED]** Alert ack/resolve phantom rows — `engine/api/api_write.py:240-261,401-403` `_alert_exists()` SELECT-guard → 404 before any INSERT; test `tests/test_alert_lifecycle_api.py:93-123`.
- **[FIXED]** Sim-fill Postgres concurrency — `engine/execution/broker_sim.py:799,822-838` null-cash defaulted (non-fatal); `engine/execution/broker_apply_orders.py:270-315` `_safe_commit/_safe_close`; test `tests/test_broker_apply_orders_postgres_concurrency.py:188`.
- **[FIXED]** P2/P3 dashboard remainder — GET token-gating on remote/LAN bind (`http_transport.py:1845-1910`), promotion-audit + relevance + market_stress + strategy/causal/size_policy "no-data" markers, `repair_schema` 422 confirmation, `runtime_watchdogs` clean `error:None`, `notifications/test` 422, `market/session` `data_ready` flag — all FIXED.
- **[FIXED]** `execution_mode_not_live` arm refusal status — `_map_error_to_status` maps `execution_mode_not_live`, `operator_execution_mode_not_live`, and other `*_not_live` conflict refusals to HTTP 409 while the arm handler still returns the same refusal body (`ok:false`, `error:"execution_mode_not_live"`, `execution_mode`).
- **[STILL-OPEN · residual P2 · operator]** `validate_repo --live` blocked — the **policy and shipped templates are correct/FIXED** (`deploy/compose/.env.example:398`, `deploy/env/trading.env.example:237-238` use approved `*_FILE`/`*_SECRET`); but the **operator-local untracked `.env`/`deploy/compose/.env`** carry inline `TRADING_MASTER_KEY`/`APP_MASTER_KEY`/`DASHBOARD_API_TOKEN` (`.env:138,140,238`) and the host key `/etc/trading/backup_evidence.hmac.key` is `0640` (`secret_sources.py:471` rejects group/other bits). Operator config hygiene, not a code defect.

## Blocking issues (P0)
**None.** D1 confirms there is no reachable single-fault path to live capital: `real_trading_allowed` is set at one chokepoint (`engine/runtime/gates.py:1490`) only after a conjunction of independent, default-blocked gates (DISABLE_LIVE_EXECUTION default-off, most-restrictive EXECUTION_MODE merge, DB-armed `armed==1` from an audited source, runtime state LIVE, `live_trading_preflight` with the non-overridable confirm phrase, clean pre-live reconciliation). Static integrity is clean; no secret leak; no unconfirmed destructive action.

## Critical issues (P1)  — all adversarially confirmed reachable; each must be fixed or carry an accepted mitigation
1. **[P1] Risk sizing-overlays fail OPEN under degraded inputs** — `engine/risk/portfolio_risk_engine.py:3084-3185` (drawdown/symbol-vol/corr-cluster/alpha-decay/vol-target overlays) swallow exceptions via `_warn_nonfatal` and leave sizes unchanged; their failure codes are **not** in `_PORTFOLIO_EXECUTION_BLOCKED_DEGRADED_CODES` (`engine/strategy/portfolio.py:1116-1123`) and `_post_constraint_checks` (`portfolio_risk_engine.py:1577-1682`) re-validates only the gross/net/symbol/asset/sector **hard** caps. → *Concentration/correlation/vol/drawdown shaping silently vanishes in exactly the degraded conditions it exists for.* **Fix:** add overlay-failure codes to the blocking set OR have `_post_constraint_checks` assert each enabled overlay actually ran (block if missing). **Mitigation:** gross/net hard cap + its post-check still bind (bounds blast radius; not P0).
2. **[P1] No off-host backup (3-2-1 leg dark in running config)** — `ops/backup/base_backup.sh:228-232` runs offsite only when `TS_BASE_BACKUP_OFFSITE_CMD` is set; `deploy/compose/.env:169-170` ships it empty; `backup_restore_evidence.sh` gate passes with zero off-host copies. → *A single-host disaster loses the live DB and every recovery point.* **Fix:** wire `TS_BASE_BACKUP_OFFSITE_CMD`/`TS_OFFSITE_BACKUP_DEST` + push WAL off-host; add an offsite-freshness check that fails the signed evidence closed. **Mitigation:** data is on redundant ZFS with verified daily base + continuous WAL (host/process failure recoverable).
3. **[P1] No alert delivery channel by default — every page goes nowhere off-box** — `engine/runtime/alerts_notify.py:61-66,863-899` skip all disabled channels (return `delivered:0`); `EQ_CRIT_SMTP_*`/`EQ_CRIT_WEBHOOK_URL` appear in **no** env template; `engine/runtime/alerts.py:608` only persists to DB; equity-reconciliation (`equity_drift.py:421,463`) calls only `emit_runtime_alert` (no delivery even when configured). → *Overnight kill-switch/equity-mismatch goes unseen.* **Fix:** `prod_preflight` gate fails go-live when 0 channels enabled; route equity-recon through `send_runtime_alert_notification`; document required keys. **Mitigation:** alerts still hit DB+dashboard (detection needs active watching).
4. **[P1] Disk-fill / WAL-archiver outage produces no health blocker or alert in the modes actually run** — `engine/strategy/jobs/observability_snapshot.py:326-330` returns early unless prod-locked; `engine/runtime/health.py:4523-4524,4776-4788` `storage_wal_guards` only blocks when `required` (live/flag-gated); `disk_pressure` is computed but never in any `ok` formula. → *The exact documented root-disk-fill incident class is un-alarmed in safe/sim/paper/shadow.* **Fix:** make disk-pressure CRITICAL a runtime `health.ok`/`critical_blockers` contributor and let `_emit_storage_wal_alerts` fire for free-space/WAL outage regardless of prod-lock. **Mitigation:** preflight backstop catches it at boot only; raw sub-objects remain inspectable.
5. **[P1] CPU/dev lock hash verification remediated (GO-R5)** — `requirements.lock.txt`/`requirements-dev.lock.txt` are regenerated with `--generate-hashes` across PyPI plus the PyTorch CPU index; lock-backed install paths use `pip install --require-hashes`; `tools/validate_dependency_lock.py --strict` fails when a checked-in lock package entry lacks a SHA-256 hash.
6. **[P1] GPU ROCm/CUDA profile transitive locks remediated (GO-R6)** — `requirements-nvidia-cuda.txt` now applies `requirements-nvidia-cuda.lock.txt`; `requirements-amd-rocm-full.txt` now applies `requirements-amd-rocm.lock.txt`; both locks were generated with `uv pip compile --generate-hashes` while honoring the cu121 index and ROCm direct wheel URLs. Runtime Docker, host install, upgrade, local bootstrap, and server bootstrap paths use `pip install --require-hashes` for the locked GPU profiles; `tools/validate_dependency_lock.py --strict` fails when a runtime profile lacks its expected `-c *.lock.txt` constraint or a profile lock package entry lacks a SHA-256 hash.
7. **[P1] FX has no live-disable-by-default gate** — `engine/execution/broker_router.py:897-913` `_fx_order_safety_block` allows the batch whenever an IBKR broker is present and checks no `FX_LIVE_TRADING_ENABLED`; `broker_ibkr_gateway.py:956-966` builds a live CASH/IDEALPRO contract; futures/crypto/options all default-off-gated, FX is not. → *Enabling FX research while live with IBKR routes real FX orders to IDEALPRO with no dedicated toggle.* **Fix:** add an `FX_LIVE_TRADING_ENABLED` disable-by-default block symmetric with futures at the router boundary. **Mitigation:** FX universe is default-off (`FX_PAIRS_ENABLED`), so it needs a deliberate operator action to bite.
8. **[P1] No paid market-data feed configured — no live price rail** — `/etc/trading/secrets` absent; `deploy/env/trading.env.example:285,299` `*_FILE` targets unpopulated; only yfinance (free/ToS-restricted) + simulated available. **Correctly fail-closed** (missing creds → 422; dashboard `ok` requires `live_market_data_ok`). → *No live/paper run can source real prices until creds are provisioned.* **Fix (operator):** provision Polygon/IBKR (and news/earnings/options) `*_FILE` secrets (0600) and prove each `status==pass`. **Dev companion (see P2 in D10):** stop counting yfinance as a "live" provider so a yfinance-only deployment cannot self-certify.
9. **[P1] Operator-UI crash resilience remediated (GO-R8)** — `boot/operator_ui.html` now installs a separate inline non-module `operatorUiCrashFallback` before the module script. It binds a degraded `window.emergencyStopHard` that still requires typed `KILL`, a reason, consequence acknowledgement, and the 3000 ms hold before POSTing `/api/operator/emergency_stop`; it also installs `window.onerror`, capture-phase `error`, and `unhandledrejection` boundaries that render "Operator UI failed to load — Emergency Stop is in degraded fallback mode" if module assets fail. The module overwrites the fallback with the full structured-modal handler after successful evaluation.
10. **[P1] Operator sidecar `/start` & `/restart` run unguarded async with no global rejection handler** — `boot/operator_server.js:6154,6459` are bare `async` handlers (not `wrapOperatorRoute`); `startEngine` (`:2916,2931-2936`) does fs ops that throw on EACCES/ENOSPC; `grep unhandledRejection|uncaughtException` = 0; Express 4 doesn't forward async rejections; Node 20 default-`throw` mode **terminates the process**. → *A disk-full/EACCES on the two most-pressed incident buttons crashes the only operator control plane.* **Fix:** wrap both handlers with `wrapOperatorRoute`/try-catch + add `process.on('unhandledRejection'|'uncaughtException', …)`. **Mitigation:** systemd `Restart=on-failure` self-heals until `StartLimitBurst=5/300s` is exhausted; `start_all.py` launcher has no respawn.

## Major / hardening (P2–P3)
- **[P2]** Gross/net hard caps fully disableable by two cooperating env flags with no notional backstop — `portfolio_risk_engine.py:3029-3039` + `portfolio_risk_gate.py:1359-1360` → in LIVE, treat a disabled engine/gate as `portfolio_risk_block=1` (fail-closed).
- **[P2]** Execution gate fails OPEN when the `portfolio_risk_state` read itself errors — `engine/runtime/gates.py:1224-1230` (the barrier is stricter at `execution_barrier.py:173-179`) → fail-closed on read error in live.
- **[P2]** Plaintext secret provider blocked only by `TS_ENV=production`, not by `ENV=prod`/`ENGINE_MODE=live`/`PROD_LOCK` — `services/secrets/providers/plaintext.py:23-24` → broaden to mirror `strict_secret_source_policy_required`.
- **[P2]** Cross-profile ML-lib pin divergence (numpy/sklearn/lightgbm) with no consistency check — `requirements-base.txt` vs `requirements-amd-rocm.txt` → validator asserts shared pins identical or allow-listed.
- **[P2]** `engine/strategy` money-path modules have no per-module coverage floor / zero-cov guard — `pyproject.toml` coverage_gate omits `engine/strategy`.
- **[P2]** No property/invariant tests (VaR≤CVaR, cap monotonicity) — add `hypothesis` tests wired into `-m safety_critical`.
- **[P2]** No nightly/scheduled soak or chaos lane — `.github/workflows/` has only `validate.yml` (no `schedule:`); soak harnesses exist but are never run.
- **[P2]** Crypto live-disable enforced only at the risk-governor, not the router boundary (defense-in-depth asymmetry vs futures/options) — `engine/execution/portfolio_risk_gate.py:405-414`.
- **[P2]** Live preflight surfaces only options readiness; FX/crypto/futures live-enable state absent — `engine/runtime/live_trading_preflight.py:1149-1155`.
- **[P2]** yfinance counts as a "live" provider; paid-equity preflight self-exempts when only yfinance/simulated enabled — `engine/runtime/feed_truth.py:8`, `prod_preflight.py:2271,2281-2284`.
- **[P2]** CPCV/PBO (embargo+purge) leakage gate is opt-in, never required even in live — `champion_manager.py:2251`, `statistical_gates.py:488`.
- **[P2]** Terminal directional BUY/SELL bypass the confirmation modal (sub-threshold one-click live order) — `ui/terminal/terminal.js:1252-1276,1416-1430`.
- **[P2]** Sim order→fill commits fills/account and the dedup idempotency marker in **separate** transactions — `engine/execution/broker_sim.py:2928,3878-3880` → fold the marker into the same txn (crash/commit-fail re-applies the batch).
- **[P2]** systemd engine/operator units have no `WatchdogSec` (a hung-but-alive process is never restarted) — `deploy/systemd/trading-engine.service`.
- **[P2]** No `MemoryMax`/`MemoryHigh`/`OOMScoreAdjust` on the engine unit (OOM killer can pick co-located Postgres/Redis) — `deploy/systemd/trading-engine.service`.
- **[P2 · operator]** Operator-local `.env`/`deploy/compose/.env` inline secrets + `0640` backup key block `validate_repo --live` (the gate is working as designed).
- **[P3 · remediated 2026-06-26]** Router broker-health degraded probe now binds the execution-health reader and returns WARNING-active snapshots when the reader is missing or raises; unprimed/empty health remains quiet and hard gates stay independent — `broker_router.py::_execution_degraded_from_cache()`, `tests/test_broker_router_degraded_probe_fail_closed.py`.
- **[P3]** `PORTFOLIO_VOL_HARD_BLOCK` default 0.0 (soft-only) + suppression `None` → permissive default — `portfolio_risk_engine.py:130`, `execution_policy_engine.py:745`.
- **[P3]** Restore-drill reuse window 90 days; live evidence reuses a ~9.5-day drill — `backup_restore_evidence.sh:37`; async spool `synchronous=NORMAL` default (`async_writer.py:77`).
- **[P3 · remediated 2026-06-26]** `effective_runtime_state` now delegates to the central API redactor and protected-route query-token auth is rejected in production/live and on non-loopback dashboard binds; loopback dev compatibility remains — `effective_runtime_state.py::redact_evidence()`, `http_transport.py::_require_protected_route_auth()`.
- **[P3]** No venv-vs-manifest drift detection in preflight; pgbouncer pooling path allow-skipped in CI.
- **[P3]** Live serving silently uses env-default `MODEL_NAME` when no champion assigned (not flagged as fallback) — `engine/strategy/predictor.py:482-493`.
- **[P3]** Emergency Stop lacks incident salience (same-size red button, no STOP shape); `service_ctl.sh logs` reads journald while units log to files; `broker_sim` raw commit/close.

## Host / OS fit
- **CUDA-less torch: PASS.** No CUDA leak on the capital path; torch is CPU/ROCm; only the (un-locked) ROCm GPU profile is a reproducibility concern (P1 #6), not a wrong-device fault.
- **Swap / OOM: GAP.** Live host `bart` currently has **only a 512 MiB `/swapfile` and no active zram** — the documented memory-pressure hardening (16 GiB swapfile + 32 GiB zram + 48 GiB ARC cap, `docs/MEMORY_PRESSURE_RUNBOOK.md`) is **not applied**, and the engine unit has no `MemoryMax`/`OOMScoreAdjust` (P2). Apply the runbook hardening *and* set per-cgroup caps before live.
- **ZFS/NVMe placement: PASS.** PGDATA on `dbpool/trading/timescaledb/data`, backups on `zpool/trading-backups`, runtime logs on `auxpool/trading/runtime/logs` — all off root `/`. Residual: backups are single-host (P1 #2).
- **Privileged ports: operator action.** Operator bridge `:4001` and dashboard `:8000` are unprivileged; LAN mode requires `DASHBOARD_API_TOKEN` (enforced). No root needed for the data path.

## Required for GO  (ordered; smallest complete set that flips NO-GO → GO. Each P1 must be fixed or carry an accepted, signed-off mitigation.)
- [ ] **FIX (dev)** — Risk overlays fail-closed: block execution (or fail `_post_constraint_checks`) when an *enabled* sizing overlay errors in live. Verify: fault-inject an overlay exception in live and assert the batch is blocked / `portfolio_risk_block=1`. *(P1 #1 — `GO-R1`)*
- [ ] **ADD (dev)** — Runtime disk-fill + WAL-archiver alarming in **all** run modes: make disk-pressure CRITICAL a `health.ok`/`critical_blockers` contributor and emit `STORAGE_FREE_SPACE_CRITICAL`/`WAL_ARCHIVER_OUTAGE` regardless of prod-lock. Verify: `ENGINE_MODE=paper` + simulated critical free-space → `health.ok=false` + alert row. *(P1 #4 — `GO-R2`)*
- [ ] **ADD (dev)** — Alert-delivery go-live gate: `prod_preflight` fails when 0 channels enabled; route equity-reconciliation through `send_runtime_alert_notification`; add `EQ_CRIT_*` keys to the prod env template marked required. Verify: preflight blocks with no channel; a simulated CRITICAL reaches a test channel. *(P1 #3 — `GO-R3`)*
- [ ] **ADD (dev) + CONFIG (operator)** — Off-host backup leg: add an offsite-freshness check that fails the signed evidence closed when no fresh off-host copy exists; operator sets `TS_BASE_BACKUP_OFFSITE_CMD`/`TS_OFFSITE_BACKUP_DEST` + WAL off-host. Verify: evidence JSON reports `offsite=pass` with a recent off-host tarball; gate fails when absent. *(P1 #2 — `GO-R4`)*
- [x] **FIX (dev)** — Hashed lockfiles + `--require-hashes`: regenerate `requirements*.lock.txt` with `--generate-hashes`, install `--require-hashes` in lock-backed install paths, add a validator rule failing on any hash-less line. Verify: `grep -c -- --hash` > 0; Docker CPU/default installs with hashes; `validate_dependency_lock.py --strict` enforces it. *(P1 #5 — `GO-R5`)*
- [x] **ADD (dev)** — Per-profile transitive locks for ROCm/CUDA referenced via `-c`; `validate_dependency_lock.py` asserts every runtime profile carries a constraint lock. Verify: each GPU manifest has a `-c *.lock.txt` line; validator fails without it. *(P1 #6 — `GO-R6`)*
- [ ] **ADD (dev)** — `FX_LIVE_TRADING_ENABLED` disable-by-default gate at the `broker_router` boundary (symmetric with futures), mirrored on the IBKR FX submit path. Verify: live IBKR chain + FX order is blocked when unset, allowed when set. *(P1 #7 — `GO-R7`)*
- [x] **FIX (dev)** — Operator-UI crash resilience: a separate inline non-module fallback Emergency-Stop handler + a `window.onerror`/`unhandledrejection` boundary rendering a visible "Operator UI failed to load — Emergency Stop is in degraded fallback mode" banner. Verify: serve a broken/404 `state_presenter.js` and confirm Emergency Stop still reaches the backend and the failure is visible. *(P1 #9 — `GO-R8`)*
- [ ] **FIX (dev)** — Wrap operator sidecar `/start` & `/restart` with `wrapOperatorRoute`/try-catch and add `process.on('unhandledRejection'|'uncaughtException', …)`. Verify: inject an fs fault on Start and confirm the sidecar returns a 500 JSON and stays up. *(P1 #10 — `GO-R9`)*
- [ ] **FIX (dev) + CONFIG (operator)** — Feed truth: paid-equity preflight requires ≥1 real paid provider live in paper/live (yfinance not counted as "live"); operator provisions Polygon/IBKR (+ news/earnings/options) `*_FILE` secrets (0600) and proves `status==pass`. Verify: yfinance-only paper preflight → blocker; `live_market_data_ok=true` only with a paid provider. *(P1 #8 — `GO-R10`)*
- [ ] **CONFIG (operator)** — Go-live config hygiene so `validate_repo --live` passes: remove inline secrets from local `.env`/`deploy/compose/.env` (use the `*_FILE`/`*_SECRET` forms already shipped), `chmod 0600 /etc/trading/backup_evidence.hmac.key`; apply the memory-pressure hardening (16 GiB swap + 32 GiB zram per runbook) and deploy the engine unit with `WatchdogSec`/`MemoryMax`/`OOMScoreAdjust`. Verify: `python tools/validate_repo.py --live` exits 0; `swapon --show` ≥ 16 GiB; `systemctl show trading-engine -p MemoryMax` set. *(FIX6 residual + P2 host fit — `GO-R11`)*

> Per-item, file-anchored **Codex deep-dive remediation prompts** (`GO-R1 … GO-R11`, each self-auditing) are in `docs/handoff/deep_dive_prompts/PRODUCTION_GO_LIVE_REMEDIATION_PROMPTS.md`.

## Recommended hardening roadmap (post-GO, prioritized)
- **Phase 1 (days):** P2 fail-closed asymmetries — risk-state read error (gates vs barrier), disable-flag fail-closed for gross/net caps, crypto router-boundary gate, plaintext-provider gate broadening, terminal BUY/SELL through the modal, fold the sim-fill idempotency marker into one txn.
- **Phase 2 (foundation):** Test/CI depth — property/invariant tests (VaR≤CVaR, cap monotonicity) in the safety-critical lane; `engine/strategy` coverage floor + zero-cov guard; cross-profile pin consistency check; pgbouncer in CI; nightly soak/chaos lane on the Postgres+Redis stack.
- **Phase 3 (scale/maturity):** Require CPCV/PBO in live promotion; per-asset live-enablement preflight snapshot; restore-drill cadence to 7–14 days with an enabled timer; venv-drift preflight; redaction-module unification; alarm rationalization + an actual paper soak of N days before sizing up.

## Working well (keep)
- **Fail-closed live authority** — a conjunction of independent, default-blocked gates with a single chokepoint (`gates.py:1490`); no single missing/misset env var reaches live; durable atomic SHA-256 order idempotency with `ON CONFLICT DO NOTHING` before submit and `stop_failover` on ambiguous submits.
- **The prior E2E NO-GO is substantially closed** — 24/26 prior findings FIXED with regression tests (degradation guards, jobs/history visibility, gating, alert-ack, broker concurrency); static integrity GREEN.
- **Risk core binds** — gross/net hard caps enforced at the runtime boundary with a post-check backstop and an independent re-check on the broker submission path; kill-switch/capital-guard/freshness all fail-closed.
- **Honest degradation** — money-path reads fail-closed (missing creds → 422; no fake-green "Test passed"); operator error/empty states are designed cards, not raw-JSON walls; DR machinery is real (restore proven, RTO 43s).

---
*Audit hygiene: read-only; no engine booted to live/paper; no broker/feed connected; no secret value written to this report. Severities reserved for capital/data/secret/uptime impact and adversarially refuted before landing. The verdict is NO-GO solely on the 10 open P1s above — the architecture and safety posture are sound.*
