# ML/AI Cutting-Edge Upgrade — Codex Deep-Dive Implementation Prompts

Authored 2026-06-26. These prompts convert the ML/AI capability review into focused, independently runnable implementation workstreams. Run one at a time in a fresh worktree. Re-confirm every local file path, line number, dependency version, and behavioral claim on the current dirty tree before editing.

Standing safety contract for all prompts: do not enable live-capital authority for a new model or policy. New predictive, graph, LLM, TSFM, tabular, and RL components must enter as shadow/challenger artifacts and must pass existing feature-schema, PIT, OOS, CPCV/statistical, replay, OPE where applicable, promotion, and operator-governance gates before live use. Preserve train/serve parity through `engine/strategy/feature_registry.py`, artifact provenance through the existing artifact store/registry surfaces, and fail-closed behavior in production-like modes.

---

## ML-AI-1 — Add shrinkage/denoised covariance as the canonical risk covariance source (**P1 / highest ROI**)

**Context.** Portfolio risk, correlation clustering, Monte Carlo inputs, vol targeting, and allocation overlays currently rely on lightweight pairwise sample correlations. That is pragmatic, but sample covariance is noisy and poorly conditioned when asset count rises relative to lookback length. The repo already has strong model promotion governance; this work strengthens the money-at-risk layer that consumes model output.

**Current evidence to reconfirm.** `engine/strategy/risk.py::corr_from_prices` computes pairwise Pearson correlation from recent log returns and `portfolio_realized_vol` builds variance from that pairwise proxy. `engine/risk/monte_carlo_risk_engine.py::_build_inputs` separately computes pairwise sample correlations and `_simulate` Cholesky-samples from the resulting covariance. `engine/risk/portfolio_risk_engine.py` consumes `corr_from_prices` for correlation-cluster caps and portfolio-vol proxies. Search for any newer shrinkage implementation before editing; as of the review, no Ledoit-Wolf/OAS/RMT denoising path was wired into the production risk flow.

**Your task.** Design and implement the optimal covariance-risk facade. Add a shared module, likely under `engine/risk/` or `engine/strategy/`, that (1) loads aligned PIT-safe return matrices for a requested symbol set and lookback; (2) computes raw sample covariance/correlation as a fallback; (3) supports Ledoit-Wolf and OAS shrinkage using existing `sklearn.covariance` when available; (4) optionally supports RMT eigenvalue clipping / detoning behind an explicit env/config flag for larger universes; (5) returns a deterministic, serializable diagnostics payload including method, n_obs, n_assets, shrinkage coefficient when available, condition number, fallback reason, and symbols actually covered; and (6) exposes small helper functions so `portfolio_realized_vol`, correlation cluster construction, and Monte Carlo input building can use the same covariance estimate instead of recomputing inconsistent pairwise correlations. Default to a conservative shrinkage method for production-like risk if data suffices; fail back to the existing sample-correlation behavior only with an explicit diagnostic reason. Do not add heavyweight dependencies unless the benefit is clear; prefer scikit-learn because it is already present.

**Integration requirements.** Wire the facade into `engine/strategy/risk.py`, `engine/risk/monte_carlo_risk_engine.py`, and the relevant `portfolio_risk_engine` correlation/vol paths without changing public API shapes unnecessarily. Preserve existing behavior for thin-history, single-asset, missing-price, and test backends. Add config keys to `.env.example`, `docs/REFERENCE_CONFIGURATION_GLOSSARY.md`, and risk docs for method selection, minimum observations, RMT enablement, and fallback policy. If using Postgres, avoid N+1 per-symbol hot queries where a batch query is straightforward.

**Falsify.** Add tests proving shrinkage is used when sufficient aligned history exists, raw fallback is used with clear diagnostics when history is insufficient, the produced covariance is symmetric positive semidefinite enough for Cholesky/Monte Carlo, condition numbers improve on a near-collinear synthetic matrix, correlation cluster decisions are stable/deterministic, and prior single-asset behavior is unchanged. Include a regression test that fails if Monte Carlo and portfolio risk silently use different covariance methods for the same symbol set.

After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.
---

## ML-AI-2 — Upgrade tail-risk simulation and add VaR/CVaR model backtesting (**P1 / High**)

**Context.** The Monte Carlo risk engine is a core sizing and safety input. Current simulation uses Gaussian shocks and Cholesky covariance, with stress knobs for volatility/correlation/drift. That is useful, but Gaussian shocks structurally understate fat tails and joint crash risk. The system needs tail-aware simulation and first-class validation evidence for VaR/CVaR forecasts.

**Current evidence to reconfirm.** `engine/risk/monte_carlo_risk_engine.py::_simulate` samples `rng.gauss(0.0, 1.0)` shocks and Cholesky-correlates them. Existing outputs include VaR/CVaR, fan/distribution detail, and stress-case summaries. Search for any existing Student-t copula, EVT/POT, filtered historical simulation, Kupiec, Christoffersen, or Basel traffic-light implementation before editing; as of the review, none was wired into the risk-model validation flow.

**Your task.** Design and implement the optimal tail-risk upgrade in staged, governed form. Add risk-engine support for configurable simulation methods: existing Gaussian as fallback, Student-t copula with configurable/estimated degrees of freedom, empirical/bootstrap or filtered historical simulation when sufficient data exists, and optional EVT/POT marginal-tail adjustment for CVaR. Add a risk-model backtesting module/job that records VaR exception evidence: forecast id/timestamp, horizon, confidence level, realized portfolio return/loss, exception flag, Kupiec POF result, Christoffersen independence result, rolling exception rate, and traffic-light status. Persist evidence in migration-backed tables and expose a read API/dashboard payload consistent with existing governance evidence patterns.

**Integration requirements.** Reuse the covariance facade from ML-AI-1 if it exists; if not, keep the integration boundary clean so ML-AI-1 can plug in later. Ensure Monte Carlo output metadata records simulation method, distribution assumptions, fitted DoF, EVT threshold/tail sample size, historical-simulation window, and fallback reasons. Do not make tail model failures block unrelated reads; fail closed only where live risk consumption requires a valid risk model. Add config keys to `.env.example`, `docs/REFERENCE_CONFIGURATION_GLOSSARY.md`, `engine/risk/README.md`, and dashboard docs if a UI endpoint is added. Keep deterministic seeds available for tests while avoiding deterministic production randomness unless already required by repo policy.

**Falsify.** Add tests showing Student-t simulation creates heavier tails than Gaussian on the same covariance, insufficient data falls back with explicit diagnostics, VaR/CVaR outputs remain finite and ordered, persisted backtest rows are PIT-aligned with realized outcomes, Kupiec/Christoffersen tests produce expected pass/fail statuses on controlled synthetic exception sequences, and missing backtest tables degrade gracefully in read endpoints. Confirm existing dashboard risk contracts still render.

After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

---

## ML-AI-3 — Add conditional volatility forecasts and blend them with HAR-RV (**P1 / High**)

**Context.** HAR-RV is already a credible realized-volatility input and should stay. The gap is conditional volatility response: GARCH-family models can capture volatility clustering and asymmetric spikes that trailing/HAR estimates can lag. This is a risk/sizing improvement, not a return-timing signal.

**Current evidence to reconfirm.** `engine/strategy/har_rv.py` computes realized-variance observations from intraday or daily OHLC bars, fits HAR coefficients with OLS, persists `har_rv_forecasts`, and `engine/risk/monte_carlo_risk_engine.py` can consume a resolved volatility forecast source. Search for existing `arch`, GARCH, EGARCH, GJR-GARCH, or realized-GARCH wiring before editing; as of the review, the repo did not declare `arch` or a GARCH-family forecaster.

**Your task.** Design and implement a conditional-volatility forecast path that complements HAR-RV. Prefer an optional `arch` dependency behind a guarded extra/config flag, with a deterministic fallback that does not break CPU installs when the dependency is absent. Implement GARCH(1,1) as the baseline and evaluate EGARCH/GJR-GARCH support where asymmetric volatility is useful. Persist forecasts in a migration-backed table or extend the existing vol forecast resolution path with a model/source discriminator. Add a blend resolver that can choose `trailing`, `har_rv`, `garch`, or `blend` with explicit weights and diagnostics. Keep the output units aligned with existing `forecast_vol_1d` / realized-return conventions.

**Integration requirements.** Wire the new source into `resolve_vol_forecast` or the equivalent canonical resolver so Monte Carlo, portfolio risk, and vol targeting can opt into it without duplicating code. Add a scheduled/refit job following existing job-lock/heartbeat conventions. Record fit diagnostics: model type, distribution, n_obs, convergence status, loglikelihood/AIC/BIC if available, forecast timestamp, forecast horizon, and fallback reason. Update env templates and docs. Do not use GARCH forecasts as alpha features by default unless separately registered and governed.

**Falsify.** Add tests for successful forecast persistence on synthetic clustered-volatility data, dependency-missing fallback, convergence-failure fallback, unit consistency with HAR/trailing vol, blend behavior, PIT timestamp use, and risk-engine consumption. Include a regression test that a missing GARCH table or unavailable dependency cannot crash startup or risk reads.

After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

---

## ML-AI-4 — Upgrade conformal sizing from symmetric residual intervals to action-level risk control (**P1 / High**)

**Context.** The repo already has split conformal prediction and adaptive alpha over matured decision residuals. That is a good foundation, but trading losses are heteroscedastic, asymmetric, serially dependent, and regime-dependent. The optimal next step is to control action-level risk metrics, not only interval width.

**Current evidence to reconfirm.** `engine/strategy/conformal.py` builds residual pools using `(model_family, horizon_s, symbol)`, asset-class, then global fallback groups; it uses symmetric residual quantiles and ACI-style alpha adaptation. `predictor.py`, portfolio sizing, execution policy, and uncertainty gates consume conformal diagnostics. Search for existing CQR, conformalized quantile regression, conformal risk control, DtACI, Conformal-PID, SPCI, or martingale drift detectors before editing.

**Your task.** Design and implement a production-safe conformal upgrade. Add support for asymmetric conformalized quantile regression (CQR) when a model family exposes lower/median/upper or quantile forecasts; otherwise fall back to existing residual intervals. Add conformal risk-control calibration for monotone trading losses such as accepted-trade loss, VaR breach, drawdown contribution, slippage breach, or size-rule shortfall. Add faster adaptive controllers such as DtACI or Conformal-PID behind config flags, with conservative defaults and diagnostics. Keep the current simple conformal path available as fallback.

**Integration requirements.** Extend the conformal payload schema without breaking existing consumers. The output should clearly separate interval diagnostics, risk-control diagnostics, calibration sample size, loss definition, target risk, realized empirical risk, controller state, and recommended action (`log_only`, `size_compress`, `suppress`, `hard_block`) while leaving final authority with portfolio/risk/execution gates. Persist calibration/backtest evidence where needed and surface it in governance/readiness views. Add feature/model-family contracts for quantile forecasts if PatchTST/iTransformer/GBM quantile variants are used. Update docs and env glossary for modes, targets, minimum calibration samples, and fallback policy.

**Falsify.** Add tests for CQR asymmetric intervals on synthetic asymmetric residuals, existing symmetric fallback compatibility, risk-control calibration on controlled monotone losses, adaptive-controller behavior across a simulated regime shift, insufficient-calibration fallback, payload backward compatibility, and enforcement that model output cannot bypass final risk/execution gates. Prove a live/prod-like mode fails closed or logs a clear blocker when configured risk-control thresholds are missing.

After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

---

## ML-AI-5A — Upgrade financial text embeddings and sentiment encoders with domain-specific benchmarks (**P2 / Medium-High**)

**Context.** The text stack is useful but dated by default. FinBERT and MiniLM are safe, lightweight baselines, but current financial NLP work shows domain-adapted embeddings and finance-tuned encoders are more appropriate for filings, earnings calls, financial news, and retrieval/novelty tasks. The goal is not to let text models trade directly; it is to improve text-derived features under existing PIT and promotion gates.

**Current evidence to reconfirm.** `engine/nlp/encoder.py::FinBertSentimentEncoder` defaults to `ProsusAI/finbert`; `SentenceTransformerEncoder` defaults to `all-MiniLM-L6-v2`. `engine/data/news_flow.py` supports FinBERT, sentence-transformer, OpenAI `text-embedding-3-small`, and hashing backends. `.env.example` exposes `FINBERT_MODEL_NAME`, `NEWS_EMBED_SENTENCE_MODEL`, and `NEWS_EMBED_OPENAI_MODEL`. There is an optional ONNX/NPU FinBERT path in `engine/data/finbert_onnx_backend.py`.

**Your task.** Design and implement a benchmarked, pluggable financial text embedding/sentiment upgrade. Add configurable backends for finance-domain embedding models such as Fin-E5 and finance-specialized API embeddings where available, while preserving current backends. Add a local evaluation harness over the repo's existing cached news/filing/transcript corpora that compares candidate embedding models on retrieval relevance, near-duplicate/staleness classification, entity/event clustering, and downstream feature IC/OOS contribution. Add model metadata and cache namespacing so embedding spaces never mix. For sentiment, support a modern finance-tuned encoder or LLM-distilled classifier behind a model-name config, and preserve persisted-enrichment behavior and ONNX/NPU compatibility where feasible.

**Integration requirements.** Keep all text features PIT-safe with `availability_ts_ms` and model-name/backend in cache keys. Do not overwrite existing embedding tables without a model namespace. Add a migration only if existing schema cannot represent the needed metadata. Update feature registry metadata, `.env.example`, docs, and operator configuration surfaces. Include a license/model-card review field in metadata for any suggested default. Default should remain conservative until local benchmark evidence supports switching.

**Falsify.** Add tests proving embeddings from different backends/model names are isolated, novelty comparisons do not mix embedding spaces, missing optional models degrade cleanly, cached text hashes remain stable, PIT filters exclude unavailable future text, and benchmark output records enough evidence to choose a backend. Include a regression test that the old FinBERT/MiniLM path still works when new dependencies are absent.

After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

---

## ML-AI-5B — Add structured LLM event extraction for filings, earnings calls, news, and macro events (**P2 / Medium-High**)

**Context.** The repo already uses LLMs safely as bounded hypothesis generators and diagnostics. The next high-value LLM use is not direct return prediction; it is structured, auditable event extraction that converts unstructured text into PIT-safe features: guidance changes, margin pressure, litigation/regulatory risk, supply-chain exposure, capital allocation, buyback/dividend changes, customer concentration, management tone, and macro/event surprises.

**Current evidence to reconfirm.** `engine/strategy/discovery/llm_factor_generator.py` calls Anthropic Messages directly and constrains output to a bounded DSL. `services/operator_ai/agent.js` and dashboard copilot paths are advisory/read-only. `engine/data/sec`, transcript, RSS/news, structured document events, and feature registry code already provide event/filing surfaces. Search for existing structured LLM extraction, schema-constrained tool calling, source-span evidence, and RAG event feature tables before editing.

**Your task.** Design and implement a structured LLM event-extraction pipeline. Add a strict JSON schema for extracted financial events with fields such as entity, ticker/symbol, event_type, polarity/direction, magnitude when numeric, confidence, source_doc_id, source_url, source_ts_ms, availability_ts_ms, evidence_span offsets/text snippets, extraction_model, prompt/schema version, and contamination/PIT guard metadata. Use provider SDKs or an adapter abstraction that supports structured outputs/tool-calling where available, with deterministic JSON validation and rejection logging. Add a RAG/source selection layer that retrieves only documents available by the decision timestamp. Persist extracted events in migration-backed tables and register derived features through `feature_registry.py` as shadow/opt-in first.

**Integration requirements.** The pipeline must be batch/offline by default, rate-limited, cost-bounded, credential-gated through the existing secret system, and safe under no-key/test modes. Never let LLM extracted events bypass existing discovery/statistical/promotion gates. Add prompt/model hash, schema version, and source-hash lineage to the experiment ledger or a sibling audit table. Update docs, env glossary, data contracts, and operator controls. Prefer reusable provider adapters over hardcoding a single model API in business logic; keep raw provider responses redacted or bounded.

**Falsify.** Add tests for schema validation, invalid JSON rejection, future-document exclusion, source-span persistence, prompt/model hash logging, missing-key no-op behavior, cost/rate limit enforcement, feature registry shadow status, and no direct order authority. Add a small deterministic fake-LLM test fixture that extracts events from sample text and proves features are materialized only when `availability_ts_ms <= decision_ts_ms`.

After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

---

## ML-AI-6 — Build a governed time-series foundation-model benchmark and challenger feature layer (**P2 / Medium-High**)

**Context.** The repo already has PatchTST, iTransformer, and a shadow-only Chronos-2 encoder. Current research suggests general TSFMs can be useful but are risky in finance unless benchmarked, domain-adapted, and used as features/challengers rather than blindly promoted. The correct move is a TSFM evaluation and feature/challenger layer behind existing governance.

**Current evidence to reconfirm.** `engine/strategy/ts_foundation_encoder.py` defaults to `amazon/chronos-2` and emits shadow `tsfm.chronos_v2.*` embeddings. `pyproject.toml` has a `ts-foundation` extra for `chronos-forecasting`. `engine/strategy/models/patchtst.py` and `engine/strategy/models/itransformer.py` are model families with shadow/default governance. Live serving rejects shadow feature contracts. Search for TimesFM, Moirai, Toto, Kronos, Tiny Time Mixers, or other TSFM adapters before editing.

**Your task.** Design and implement a TSFM benchmark and adapter framework. Add a provider/model adapter interface for Chronos-2 forecasts/embeddings, TimesFM, Toto/Moirai or equivalent open-weight models where dependency/license fit is acceptable, and a fake deterministic adapter for tests. Support both forecast outputs (quantiles, horizon path, volatility proxy) and embedding outputs. Build a benchmark job that runs PIT walk-forward evaluations across equities, FX, crypto, futures, and realized-volatility tasks, comparing TSFM outputs against trailing/HAR/GARCH/LightGBM/PatchTST/iTransformer baselines. Persist OOS predictions, feature snapshots, artifact/model metadata, latency/resource usage, and governance summaries.

**Integration requirements.** TSFM-derived features must remain shadow/opt-in until a reviewed gate exists. Use existing artifact store and feature registry contracts. Do not require CUDA; support CPU and ROCm profile detection where possible. Respect `local_files_only` and model artifact persistence requirements. Add config/docs for model id, backend, context length, horizon, feature dims, device, and fallback. If a TSFM forecast is used by risk, register it as a risk input with clear provenance, not as an unexplained alpha.

**Falsify.** Add tests for adapter interface consistency, Chronos-2 path compatibility or graceful dependency skip, feature-id stage enforcement, PIT timestamp enforcement, model-cache/artifact provenance, benchmark OOS row persistence, baseline comparison output, and live-serving rejection of shadow TSFM feature contracts. Include one test proving TSFM zero-shot results cannot be promoted without normal champion/challenger evidence.

After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

---

## ML-AI-7 — Convert graph/relational snapshots into a temporal heterogeneous graph challenger (**P2 / Medium-High**)

**Context.** The repo has a strong starting graph layer: PIT-aware shadow graph features from sector, industry, rolling correlation, ETF ownership, 13F shared managers, options co-movement, news co-mentions, and supply-chain edges. It is intentionally non-promotable. The next step is a true temporal heterogeneous graph challenger that learns from those relations while preserving the shadow/live boundary.

**Current evidence to reconfirm.** `engine/strategy/graph_relational.py` defines `GRAPH_RELATIONAL_FEATURE_IDS`, snapshot persistence, metadata, PIT controls, and `evaluate_graph_promotion_gate`, which returns `graph_relational_shadow_only` even with valid metadata. `engine/runtime/schema/migrations/0062_graph_relational_learning.py` creates graph snapshot tables. `api/feature_visibility.py` surfaces graph feature readiness. Search for existing PyTorch Geometric, DGL, GraphSAGE, GAT, TGN, temporal graph, or graph-Mamba implementation before editing.

**Your task.** Design and implement a governed graph challenger framework. Add a graph dataset builder that materializes PIT-safe temporal heterogeneous graph samples: node features from existing feature snapshots; edge types/weights from existing relationship sources; timestamps and availability metadata for every edge/node feature; target labels aligned to existing horizons. Implement an initial CPU-friendly baseline graph model, such as GraphSAGE/GAT over static snapshots plus temporal windows, or a simpler relational message-passing model with no heavy new dependency if PyG/DGL is too costly. Persist graph model artifacts, OOS predictions, graph metadata, feature/schema contracts, and shadow marketplace rows.

**Integration requirements.** Keep graph features/models shadow-only unless a future reviewed live gate is explicitly implemented. The existing `evaluate_graph_promotion_gate` must continue blocking live promotion. Add a graph benchmark job and docs. Ensure train/serve parity metadata includes graph_id, snapshot_version, edge types, max source timestamp, max availability timestamp, node feature schema, edge feature schema, and train/serve parity result. Do not let graph predictions directly choose orders; they may publish shadow predictions and OOS evidence only.

**Falsify.** Add tests for PIT-safe graph sample construction, future edge/feature exclusion, missing-edge fallback, deterministic graph metadata, graph model artifact persistence/loading, OOS prediction writing, feature visibility/readiness, and continued live-promotion blocking. Add an ablation test proving the benchmark reports graph-vs-non-graph incremental evidence rather than only raw graph score.

After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

---

## ML-AI-8 — Add TabM/TabPFN-style tabular foundation challengers without displacing GBM baselines (**P3 / Exploratory**)

**Context.** LightGBM/XGBoost should remain primary baselines for noisy financial tabular data. Modern tabular foundation and parameter-efficient deep-tabular methods are still worth testing as challengers, especially for thin-history symbols, meta-labeling, and cross-sectional classification/ranking tasks. The correct implementation is a governed challenger family, not a replacement.

**Current evidence to reconfirm.** Current active families include LightGBM, XGBoost, sklearn GBM, PatchTST, iTransformer, LGBM ranker, and Ridge ensembles. `requirements-base.txt` does not include TabPFN, TabM, AutoGluon, CatBoost, or TabNet. Training jobs persist feature schemas and OOS predictions. Search for any existing TabPFN/TabM implementation before editing.

**Your task.** Design and implement a pluggable tabular challenger family. Add an adapter abstraction that can support TabM and TabPFN/TabPFN-2.5 when installed, plus a deterministic fake/test estimator. Make dependencies optional behind extras and license review. Target use cases should be explicit: small/medium sample regression, classification/meta-labeling, and possibly cross-sectional rank labels. Persist artifacts, feature schema, training window, model card metadata, OOS predictions, calibration diagnostics, and latency/resource diagnostics. Provide a distillation/export path only if the library supports it and it fits repo deployment constraints.

**Integration requirements.** New tabular models must enter `model_registry` as shadow/challenger families with the existing promotion guard. They must use the same feature contract helpers as LightGBM/XGBoost. They must not bypass minimum observation, CPCV/PBO, DSR/FDR, deconfounded evidence, replay, net-cost, and champion cooldown gates. Add docs and env keys for enabling the optional family, max rows/features, timeouts, device, and fallback policy.

**Falsify.** Add tests for optional dependency absence, feature schema persistence/parity, deterministic fake estimator OOS writes, insufficient sample no-op, artifact load failure fail-closed, model-family registration, benchmark comparison against a GBM baseline, and promotion blocking until normal evidence exists. Add a license/config test or docs gate ensuring no non-approved dependency becomes a default production requirement by accident.

After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

---

## ML-AI-9 — Move portfolio RL research from online PPO/SAC shadow-only toward offline, risk-sensitive RL (**P3 / Exploratory**)

**Context.** The repo correctly keeps RL shadow-only with no live order authority. Current PPO/SAC shadow research is safe, but online RL is a weak fit for costly, nonstationary market interaction. Offline RL trained on historical trajectories, with doubly robust OPE and CVaR/drawdown-aware objectives, is a better research direction.

**Current evidence to reconfirm.** `engine/rl/README.md` states the subsystem is shadow-only and has no live order authority. `engine/rl/agents.py` wraps stable-baselines3 PPO/SAC. `engine/rl/portfolio_env.py` routes through real risk gates and broker simulation but tags outputs as shadow. `engine/strategy/ope_gate.py` and production checklist require fresh passing OPE for policy challengers. Search for IQL, CQL, Decision Transformer, behavior cloning, offline dataset builders, or CVaR-RL implementations before editing.

**Your task.** Design and implement an offline RL research track without weakening the existing RL safety boundary. Add a trajectory dataset builder from existing historical feature snapshots, portfolio states, decisions, fills, costs, and realized outcomes. Add initial offline policy baselines: behavior cloning or fitted Q evaluation as the safest first step, then IQL/CQL/Decision Transformer adapters only behind optional dependencies. Add risk-sensitive reward definitions including net PnL after costs, drawdown penalty, turnover/slippage penalty, exposure concentration, and CVaR/tail-loss terms. Persist policy artifacts, dataset hashes, behavior-policy metadata, OPE inputs, OPE estimates, effective sample size/support diagnostics, and shadow decision outputs.

**Integration requirements.** Preserve the hard rule that RL has no live broker path. Keep `shadow_runner.py` kill-switch-gated and advisory-only. New policies must publish only to `rl_shadow_decisions` or equivalent shadow/OPE tables. Integrate with `policy_ope_evidence` and block any non-shadow use unless doubly robust OPE lower confidence bound, support/effective sample size, and realized shadow evidence pass. Add docs and config keys for offline dataset windows, policy family, objective, OPE thresholds, dependency extras, and resource limits.

**Falsify.** Add tests for trajectory PIT alignment, cost/reward calculation, no live broker import/path, optional dependency absence, OPE evidence persistence, policy lower-confidence-bound blocking, shadow decision logging, kill-switch pause behavior, and production preflight blocking any configured live consumption of RL policy output. Include a static test that fails if offline RL modules import live broker routing or order application code.

After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.
