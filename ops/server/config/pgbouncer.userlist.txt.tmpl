;; Managed by ops/server/bootstrap.sh.
;; Values are SCRAM-SHA-256 verifier strings read from PostgreSQL roles.

"ts_ingest" "{{ ts_ingest_scram }}"
"ts_app" "{{ ts_app_scram }}"
"ts_reader" "{{ ts_reader_scram }}"
