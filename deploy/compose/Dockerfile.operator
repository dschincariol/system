ARG NODE_RUNTIME_IMAGE=node:20-bookworm-slim
FROM ${NODE_RUNTIME_IMAGE}

ENV NODE_ENV=production

WORKDIR /app

COPY package.json package-lock.json /app/

RUN npm ci --omit=dev

COPY . /app

RUN mkdir -p /app/logs /app/data

CMD ["node", "boot/operator_server.js"]

