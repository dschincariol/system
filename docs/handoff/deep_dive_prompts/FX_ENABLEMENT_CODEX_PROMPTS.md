# Currency / FX Trading Enablement — Codex Deep Dive Prompts

A set of 9 self-contained implementation prompts that, run in order, add currency/FX trading to this repo end to end: data source → ingestion → instrument model → features → prediction/regime/labels → portfolio & risk sizing → execution → backtest realism & governance → operator UI.

These were split from a single end-to-end master prompt and optimized for **Codex** (run one prompt per Codex task, headless, in isolation). Each prompt is fully self-contained: it restates the global constraints, pins the verified repo anchors it touches, and carries its own scope boundaries, Definition of Done, tests, and validation commands. Run them as separate tasks rather than one mega-task.

## How to use with Codex

- Run **one prompt per Codex task**, in the recommended order below. Codex has no memory across tasks, so each prompt is standalone by design.
- Merge (or stage) each workstream before starting the next one that depends on it. The `Prerequisites` line in each prompt names what must already be in place.
- Codex often runs without network or live credentials. Every prompt instructs Codex to mock/stub and mark gaps rather than fake success — keep that behavior; do not relax it.
- After each task, run that prompt's `Validation commands` and honor its `Self-audit & NO-GO` close before merging.

## Recommended execution order

`FX-00` (research) → `FX-01` (data source) → `FX-02` (instrument model, the keystone) → `FX-03` (features) → `FX-04` (prediction/regime/labels) → `FX-05` (sizing/risk) → `FX-06` (execution) → `FX-07` (backtest realism + governance) → `FX-08` (UI).

`FX-02` is the keystone — base/quote/pip/lot/leverage/session metadata that sizing, risk, execution, and backtest all consume; land it early.

## Global constraints (apply to every prompt)

- Target repo: `/home/david/gitsandbox/system/system`. All paths are relative to it.
- Never print, log, echo, commit, or test with real secret values. Use generated canary strings in tests and assert they never appear in logs or API responses.
- Never call live broker order, cancel, replace, or flatten paths. OANDA/IBKR/Alpaca work is read-only or simulated. All FX trading graduates through shadow/paper first and may not bypass governance.
- Build on the existing architecture; never replace it. Respect the model-vs-runtime contract (models propose intent, runtime owns safety gates), train/serve feature parity via `engine/strategy/feature_registry.py`, and the champion/challenger promotion path.
- `engine/runtime/storage.py` schema changes are high blast radius: any schema/migration change needs a migration plus tests.
- "Profitable" means net of realistic FX costs (spread in pips + swap/carry) proven through the existing statistical/backtest gates, never asserted.

## Prompt index

| ID | Workstream | Needs web | Depends on |
|----|------------|-----------|------------|
| FX-00 | Internet research dossier for FX enablement | yes | — |
| FX-01 | FX data source + ingestion | no | — |
| FX-02 | First-class FX instrument model | no | — |
| FX-03 | FX feature groups + train/serve parity | no | FX-01, FX-02 |
| FX-04 | Prediction routing, FX regime, FX-correct labels | no | FX-02, FX-03, FX-01 |
| FX-05 | Currency-aware portfolio sizing + risk | no | FX-02 |
| FX-06 | FX execution + broker routing + 24/5 session | no | FX-02, FX-04, FX-05 |
| FX-07 | FX backtest realism, governance, profitability evidence | no | FX-02 |
| FX-08 | FX surfacing in operator UI surfaces | no | FX-01, FX-04, FX-05, FX-06 |

---

## FX-00 — Internet research dossier for FX enablement

**Mission:** Produce one cited, decision-grade research dossier at `docs/handoff/research/FX_ENABLEMENT_RESEARCH.md` that grounds every later FX workstream (broker/API choice, price/macro data sources, alpha factors, FX mechanics math, and regulatory leverage caps) with primary-source citations and a one-page final recommendation — a docs-only deliverable that writes no runtime code.

**Prerequisites:** None. FX-00 is the first FX workstream and has no merge dependencies. It is the research substrate consumed by FX-01 through FX-08; do not assume any of their artifacts exist. Do not import, edit, or depend on anything those later prompts will add. FX-00 produces only a Markdown dossier — it persists **no** machine-readable artifact, so later prompts (notably FX-05's leverage-cap table) must treat FX-00 as a documentation reference they hand-copy from, not a file they read at runtime.

**Global constraints (read before doing anything):**
- Target repo: `/home/david/gitsandbox/system/system`. This is the only repo; every path below is relative to it. All file references in your output must be repo-relative.
- This is a **research / documentation task**. You will create exactly one new Markdown file and (optionally) one tiny index link. Do **not** write or modify any Python under `engine/`, `services/`, `routes/`, `boot/`, `ops/`, `tools/`, or `scripts/`. Do not add tests that import runtime code. Do not change `engine/runtime/storage.py` or any schema/migration.
- Never print, log, echo, commit, or include any real secret value, API key, or access token anywhere in the dossier. When you show an env-var name (e.g. `OANDA_ACCESS_TOKEN`), show the **name only**, never a value. If you include an example config, use an obviously fake placeholder like `OANDA_ACCESS_TOKEN=<redacted-do-not-commit>`.
- Never call live broker order/cancel/replace/flatten paths and never instruct anyone to. All broker guidance in the dossier must be framed as read-only (market data, account read, instrument metadata) or simulated/paper-first. State explicitly in the dossier that all FX trading graduates through shadow/paper first and may not bypass governance.
- Build on the existing architecture; the dossier must recommend wiring FX through the **existing** contracts, never replacing them: the model-vs-runtime contract (models propose intent, runtime owns safety gates), train/serve feature parity via `engine/strategy/feature_registry.py`, and the champion/challenger promotion path. Do not propose a parallel FX framework.
- "Profitable" means net of realistic FX costs (spread in pips + swap/carry) proven through the existing statistical/backtest gates — never asserted. The dossier's cost-model section must say this and point at the existing gate machinery rather than inventing a new one.
- **Network honesty:** This task is intended to use web research. If the sandbox has network access, you MUST fetch and cite current primary vendor/regulator docs (URL + the specific fact + retrieval date). If the sandbox has **no** network access, do not fabricate citations: produce a best-effort dossier from model knowledge, prefix every figure you could not verify online with `ASSUMPTION — confirm:` and add a top-of-file banner stating that network was unavailable and which sections are unverified. The dossier itself must make online-vs-offline mode machine-detectable (see the required banner / marker strings in step 1 and the Validation commands).

### In scope
- Create `docs/handoff/research/FX_ENABLEMENT_RESEARCH.md` covering all six research topics below, with a decision matrix and a one-page final recommendation.
- Create the directory `docs/handoff/research/` (it does not yet exist).
- Optionally add one row/link to `docs/handoff/README.md` so the new dossier is discoverable, and to `docs/handoff/deep_dive_prompts/README.md` only if a research-archive table already fits there. Keep any link target valid (see "Verified anchors" — `tools/validate_docs.py` validates relative Markdown links).
- Cite, in the dossier, the exact existing repo anchors that later FX work will extend (so downstream prompts inherit correct symbols), per the "Verified anchors" list.

### Out of scope (do not touch)
- Any `.py` file. No runtime code, no new feature ids, no new COT contract specs, no broker client. (The dossier may *recommend* that **FX-01** add the `6J/6B/6A/6C/6S/6N` COT specs to `engine/data/cftc_cot.py`, but FX-00 must not add them itself. Note in the dossier that **COT contract-spec authoring is owned solely by FX-01**, not FX-03 — FX-03 only surfaces the `fx.cot_*` feature ids — so the dossier must not tell two prompts to edit `DEFAULT_COT_CONTRACT_SPECS`.)
- `engine/runtime/storage.py`, any migration, any schema.
- `engine/strategy/feature_registry.py`, `engine/data/cftc_cot.py`, `engine/data/factor_ingestion.py`, `services/data_source_manager.py` — read-only for grounding; cite but do not edit.
- Any credential file, `.env`, `.env.example` values (you may reference env-var *names* that already exist).
- Live broker connectivity, order placement, or any network call to a brokerage trading endpoint.
- Work belonging to FX-01..FX-08 (ingestion, instrument model, feature groups, routing, sizing, execution, backtest realism, UI). FX-00 only researches and recommends.

### Verified anchors
Confirmed by reading the repo on 2026-06-23; use these exact symbols/paths in the dossier so downstream prompts inherit correct references. **Cite by symbol name; include line numbers only as "approximate (verify on read)" because line numbers drift.**
- `docs/handoff/` exists; `docs/handoff/research/` does **not** yet exist (you create it). Sibling examples of the doc style live in `docs/handoff/deep_dive_prompts/`.
- `docs/handoff/README.md` — the discoverability index table for handoff docs (add the dossier link here if you add a link).
- `docs/DOCUMENTATION_INDEX.md` exists and is a required file enforced by `tools/validate_docs.py`.
- `tools/validate_docs.py` — `_markdown_files()` rglobs `*.md` under `docs/`, `engine/`, `boot/`, `services/`, `ui/`, `ops/`, `deploy/` plus top-level `README.md`/`CONTRIBUTING.md`/`CHANGELOG.md` (it is **not** limited to `docs/**`); `_validate_links()` resolves every non-http relative Markdown link and a broken relative link fails the validator. Any link you add under `docs/` is covered — keep it valid.
- `tools/precommit_text_guards.py` — fails on trailing whitespace or a missing final newline in text files; your new `.md` must end with a single newline and have no trailing whitespace.
- `engine/data/cftc_cot.py` — `COT_FEATURE_IDS = ["cot_commercial_net_pctile_3y", "cot_noncomm_net_z", "cot_noncomm_extreme_flag", "cot_open_interest_z"]` (list opens at **line 34**; the preceding `from engine.runtime.storage import connect, run_write_txn` is at line 32).
- `engine/data/cftc_cot.py` — `class CotContractSpec` dataclass at **line 55** (the `@dataclass(frozen=True)` decorator is at line 54); fields: `contract_key`, `report_type`, `market_name_contains`, `symbols`, `topic`, `weight`.
- `engine/data/cftc_cot.py` — `DEFAULT_COT_CONTRACT_SPECS` (tuple opens ~line 68) currently includes FX only via `CotContractSpec("6E", "legacy", "EURO FX", ("FXE", "6E"), "fx")` at ~line 74. The full FX set (6J/6B/6A/6C/6S/6N) is **absent**; the dossier must list each with its CFTC `market_name_contains` string for **FX-01** to add later (single owner).
- `engine/data/factor_ingestion.py` — leakage-safe external-factor ingestion keyed on `(asof_ts, effective_ts, version)`; FRED/ALFRED point-in-time plumbing lives here (`_FRED_OBSERVATIONS_URL`, `_ALFRED_DOWNLOAD_URL`, `_ALFRED_VINTAGE_RE` vintage parsing). The dossier's macro section must route FRED FX-rate-differential series through this PIT path, not a new ad-hoc fetch.
- `engine/strategy/feature_registry.py` — `FEATURE_GROUPS` dict at **line 456** (train/serve parity catalog); `FEATURE_STAGE_SHADOW = "shadow"` at **line 504** and `FEATURE_STAGE_LIVE = "live"` at **line 505**; `PRICE_FEATURE_IDS` block opens at **line 328**. FX factors must enter as a new shadow-stage group here (computation/registration is FX-03 work, cited here only).
- `engine/strategy/conformal.py` — the FX asset bucket: `if asset in {"FX", "FOREX", "CURRENCY"}:` (line 94) `return "asset:FX"` (**line 95**). An FX regime/asset bucket already exists; the dossier must note FX is a recognized asset class, not net-new.
- `services/data_source_manager.py` — `_BASE_CREDENTIAL_RUNTIME_ENV_KEYS` (tuple opens ~line 79) already lists `OANDA_ACCESS_TOKEN`, `OANDA_API_KEY`, `IBKR_HOST`, `IBKR_PORT`, `IBKR_USERNAME`, `IBKR_PASSWORD`, `IBKR_CLIENT_ID`, `POLYGON_API_KEY` — so OANDA/IBKR/Polygon credential plumbing already exists; FX work configures, it does not invent. **There is no `IBKR_API_KEY` key — IBKR auth is host/port/username/password/client-id.** (Correction vs brief framing: the brief said to "recommend broker"; note in the dossier that OANDA + IBKR + Polygon credential env-var names are already registered here.) **Canonical OANDA token env-name is `OANDA_ACCESS_TOKEN`** (the registered name); the dossier must standardize on `OANDA_ACCESS_TOKEN` and warn FX-06 against introducing a divergent `OANDA_API_TOKEN`.
- Validators present: `tools/validate_docs.py`, `tools/validate_repo.py`, `tools/git_worktree_triage.py`, `tools/precommit_text_guards.py`.

### Downstream-ownership notes the dossier must record (so later prompts have unambiguous owners)
These are guidance lines for the dossier to carry forward; FX-00 changes nothing, but recording them prevents the dead-feature / dual-owner traps the FX decomposition is prone to. State each plainly in the relevant section (§3/§4/§5/§8 as noted):
- **Feature transforms have owners, not just feature ids.** Declaring an `fx.*` feature id is not the same as computing it. The dossier must state that the per-pair **rate-differential / carry** transform, the **DXY / cross-pair correlation / TSMOM-trend** transforms, are computed by **FX-03's loaders from FX-01's raw FRED + price rows** (FX-01 ships raw series and bars only). Do not describe these as "FX-01 provides a resolver" — assign the computation to FX-03 explicitly so the features cannot end up permanently `0.0`.
- **Economic-calendar / central-bank event data needs an ingestion owner.** The dossier's §3 recommends a calendar source for FOMC/ECB/BoJ/NFP/CPI; state that *ingesting and persisting* that event calendar is **FX-01 scope** (a calendar/event source or event table), because `fx.event_*` window features (an FX-03 group) are structurally dead `0.0` unless some prompt actually ingests the events. Flag this as a required FX-01 addition.
- **Canonical stored symbol form is owned by FX-02.** Note that FX-02 is the single source of truth for FX symbol semantics (parser + instrument metadata); the dossier must recommend one canonical stored symbol form flow (symbols table → features → risk → costs → UI) and that FX-05/FX-06/FX-07 normalize via FX-02's accessor rather than re-deriving base/quote — otherwise underscore-vs-concatenated keys (`EUR_USD` vs `EURUSD`) silently miss in cost-table lookups.
- **One canonical session clock.** Recommend a single session-boundary source of truth (the 17:00-ET FX day) that the label clock, execution session gate, and UI all derive from, and state the ET↔UTC equivalence explicitly so labels and weekend-gap handling cannot disagree.
- **Leverage caps are doc→code copy.** The NFA/CFTC/ESMA numbers in §6 are the **seed values** for FX-05's runtime cap table; since FX-00 persists nothing machine-readable, FX-05 hand-copies them and pins them with a comment/test referencing this dossier's §6.

### Implementation steps
1. **Determine network availability first.** Attempt one harmless web fetch (e.g. the OANDA developer docs root).
   - If it succeeds, you are in **online** mode — cite live URLs with the specific fact retrieved and a retrieval date, and put this exact marker line near the top of the dossier: `> NETWORK MODE: ONLINE — citations fetched 2026-06-23 (see §9).`
   - If it fails/blocked, you are in **offline** mode — put this exact banner as the first content line after the title: `> NETWORK UNAVAILABLE: figures marked "ASSUMPTION — confirm" were not verified against live sources.` and prefix every regulatory number, fee, and API detail with `ASSUMPTION — confirm:`.
   - These two marker strings make the mode machine-checkable. Do **not** fabricate URLs in either mode.
2. **Create the directory** `docs/handoff/research/`.
3. **Write `docs/handoff/research/FX_ENABLEMENT_RESEARCH.md`** with this exact section structure (use `##`/`###` headings; the file must end with a single trailing newline and contain no trailing whitespace):
   - **Title + scope banner**: one paragraph stating this dossier grounds FX-01..FX-08, is research-only, and that all FX trading is paper/shadow-first under existing governance. Include the network-mode marker/banner from step 1.
   - **§1 Execution venue / broker API comparison.** Compare **OANDA v20** (REST + streaming endpoints, practice vs live host distinction, instrument naming `EUR_USD`, integer `units` sizing with sign = direction, financing/swap accrual, leverage tiers) vs **IBKR Forex** (`secType="CASH"`, exchange `IDEALPRO`, base/quote contract e.g. `EUR.USD`, `ib_insync`/`ibapi`, lot/cash-quantity sizing) vs at least one alternative (e.g. FXCM/ForexConnect or a data-only venue). Explicitly state **Alpaca has no retail FX**. For the recommended **primary** and **fallback** broker, give the exact read-only/simulated contract + order-construction skeleton each needs (instrument symbol form, order type, units/quantity sign convention) — framed as paper/practice only, never live. Note that `OANDA_*` (canonical token: `OANDA_ACCESS_TOKEN`) and `IBKR_*` credential env names already exist in `services/data_source_manager.py` (`_BASE_CREDENTIAL_RUNTIME_ENV_KEYS`), and that there is **no** `IBKR_API_KEY`.
   - **§2 Price / history data (live + historical bid/ask).** Compare OANDA candles/pricing stream, Polygon forex (`C:EURUSD` ticker convention, aggregates vs quotes), CCXT caveats (CCXT is crypto-exchange-oriented — call out that it is **not** a clean spot-FX source), and historical tick/bar archives (HistData, TrueFX, Dukascopy) with their bid/ask coverage and licensing notes. Document the 24/5 FX clock: weekly open Sunday ~17:00 ET, weekly close Friday ~17:00 ET, the **17:00 ET daily rollover** boundary, and how to handle weekend gaps in bar construction. Recommend the live source and the historical bid/ask source, and recommend that one canonical session clock (the 17:00-ET day) be the source of truth for labels/execution/UI (ET↔UTC equivalence stated).
   - **§3 Macro / alpha data.** Name **specific FRED series IDs** for: policy/short rates (e.g. `DFEDTARU`/`DFF`, `ECBDFR`, plus a JP/UK/etc. short-rate ID — verify exact IDs online or mark ASSUMPTION), real yields (e.g. `DFII10`), and the inputs needed to compute **rate differentials** per major pair; the **DXY / trade-weighted dollar** (e.g. `DTWEXBGS`); and explicitly route these through the existing PIT ingestion in `engine/data/factor_ingestion.py`. State that **FX-01 ingests the raw series** and **FX-03's loaders compute the per-pair rate-differential/carry/DXY transforms** (no "resolver if present" hand-waving — assign the computation). List the **full CFTC COT FX contract set** the system should track — `6E` (already present), plus `6J` (JAPANESE YEN), `6B` (BRITISH POUND), `6A` (AUSTRALIAN DOLLAR), `6C` (CANADIAN DOLLAR), `6S` (SWISS FRANC), `6N` (NEW ZEALAND DOLLAR) — giving each the CFTC `market_name_contains` string and reusing the existing `CotContractSpec` shape from `engine/data/cftc_cot.py` line 55 (note: **FX-01 alone** adds these specs to `DEFAULT_COT_CONTRACT_SPECS`; FX-00 only specifies them and FX-03 must not also author them). Recommend one **economic-calendar source** that flags FOMC/ECB/BoJ/NFP/CPI with timestamps usable as decision-time event features, and state that **ingesting/persisting that calendar is FX-01 scope** (otherwise `fx.event_*` features are permanently `0.0`).
   - **§4 FX alpha factors with consensus citations.** For each of: time-series momentum/trend, carry / rate-differential, value / PPP, positioning (COT), and dollar / risk-on-off beta — state whether the academic+practitioner consensus says it is robust **net of cost** or fails out-of-sample, cite the source, and map it to **one concrete computable feature** (inputs, transform, and which existing group it would extend in `feature_registry.py`, noting the transform is computed in FX-03's loader). Explicitly flag factors that historically fail OOS net of cost.
   - **§5 FX mechanics math.** Define precisely: pip = `0.0001` (JPY pairs `0.01`), pip value formula, standard/mini/micro lot sizes (100k / 10k / 1k), notional, margin & leverage relationship, swap/rollover accrual including the **triple-swap (Wednesday) day** and **T+2 settlement** basis, quote conventions (base/quote, direct/indirect), spread expressed in pips, and a concrete **slippage + cost model for backtests** (spread-in-pips + swap/carry) that plugs into the existing cost/gate machinery rather than a new framework. Note that cost-table lookups must key off the **canonical FX-02 stored symbol form** (no `EUR_USD` vs `EURUSD` mismatch). State that profitability must be net of this and proven through existing statistical/backtest gates.
   - **§6 Regulatory / leverage caps as hard sizing limits.** US NFA/CFTC: ~50:1 on majors, ~20:1 on minors, **FIFO** ordering, **no-hedging** (no simultaneous opposite positions in the same pair). ESMA: 30:1 majors. Present these as **hard caps the runtime sizing layer (FX-05) must enforce**, not as model knobs, and state that these documented numbers are the **seed values FX-05 hand-copies** into its runtime cap table (FX-00 persists nothing machine-readable). Verify each number online or mark ASSUMPTION.
   - **§7 Decision matrix.** A Markdown table scoring each broker and each data source across: coverage, bid/ask fidelity, cost transparency, API ergonomics, paper/practice support, and licensing — with a clear chosen row.
   - **§8 One-page final recommendation.** Concise: recommended **primary + fallback broker**, **live price source + historical bid/ask source**, **macro/calendar sources**, **initial major-pair universe** (e.g. EUR_USD, USD_JPY, GBP_USD, AUD_USD, USD_CAD, USD_CHF, NZD_USD — justify), the **factor set** to start with, the **cost model** (spread pips + swap) to use in backtests, and a one-line ownership map (FX-01 ingest incl. event calendar; FX-02 canonical symbol/instrument; FX-03 feature transforms; FX-05 caps; one canonical session clock). End with an explicit statement that all of this enters via existing feature/governance/champion-challenger paths, paper-first.
   - **§9 Sources.** A numbered citations list. Online mode: real URLs + retrieval date + the fact each supports. Offline mode: list intended primary sources and mark the dossier as unverified per the banner.
4. **Make the dossier discoverable.** Add one row linking `research/FX_ENABLEMENT_RESEARCH.md` to the table in `docs/handoff/README.md` (relative link from that file's directory). Verify the relative link resolves so `tools/validate_docs.py` passes.
5. **Do not** register features, add COT specs, add env vars, or touch any `.py`. If you find yourself editing code, stop — that is FX-01..FX-08 scope.
6. **Self-check the file hygiene**: no trailing whitespace, exactly one final newline, no real secret values, every relative link valid.

### Tests to add
This is a docs-only deliverable, so add **no** Python tests that import runtime code. Verification is via the documentation validators and a small content check:
- No new file under `tests/` is required or permitted for FX-00. (If a downstream reviewer insists on a guard, the only acceptable addition is a self-contained doc-existence assertion, e.g. `tests/test_fx_research_dossier_exists.py` that asserts `docs/handoff/research/FX_ENABLEMENT_RESEARCH.md` exists and contains the six required section headings and the words "paper", "shadow", and "net of" — importing nothing from `engine`/`services`. Add this only if required; default is to add no test.)

### Definition of Done
- [ ] `docs/handoff/research/FX_ENABLEMENT_RESEARCH.md` exists and contains all sections §1–§9 with the headings described above.
- [ ] All six brief topics are covered: broker/API comparison (incl. "Alpaca has no retail FX"), price/history bid-ask sources + 24/5 clock + 17:00 ET rollover + weekend gaps, macro/COT/calendar data (full FX COT set 6E/6J/6B/6A/6C/6S/6N with `market_name_contains` strings), alpha factors with consensus citations + OOS-failure flags, FX mechanics math (pip/pip-value/lots/notional/margin/swap incl. triple-swap/T+2/spread-in-pips/cost model), and regulatory leverage caps (NFA/CFTC 50:1 majors / 20:1 minors, FIFO, no-hedging; ESMA 30:1) framed as hard sizing limits.
- [ ] A one-page final recommendation (§8) names primary+fallback broker, live + historical data sources, macro/calendar sources, initial major-pair universe, factor set, cost model, and the ownership map.
- [ ] §7 decision matrix table is present and renders as valid Markdown.
- [ ] Downstream-ownership notes are recorded: feature transforms owned by FX-03 (not "resolver if present"); event-calendar ingestion assigned to FX-01; COT specs owned solely by FX-01 (not FX-03); canonical symbol form owned by FX-02; one canonical session clock; leverage caps are the FX-05 seed; canonical OANDA token env is `OANDA_ACCESS_TOKEN`.
- [ ] Network mode is honest and machine-detectable: online → marker `> NETWORK MODE: ONLINE` + real cited URLs with retrieval dates; offline → banner `> NETWORK UNAVAILABLE:` + every unverified figure prefixed `ASSUMPTION — confirm:`.
- [ ] No real secret/API-key/token value appears anywhere in the dossier (env-var **names** only).
- [ ] No `.py` file changed; no schema/migration touched; `engine/strategy/feature_registry.py`, `engine/data/cftc_cot.py`, `engine/data/factor_ingestion.py`, `services/data_source_manager.py`, `engine/runtime/storage.py` are unmodified.
- [ ] The dossier cites the verified repo anchors by symbol (feature_registry `FEATURE_GROUPS` line 456 + `FEATURE_STAGE_SHADOW`/`FEATURE_STAGE_LIVE` 504-505, `CotContractSpec` line 55 + `COT_FEATURE_IDS` line 34 + the 6E-only gap, factor_ingestion PIT path, conformal `asset:FX` line 95, the existing OANDA/IBKR/Polygon credential env names with no `IBKR_API_KEY`) so downstream FX prompts inherit correct symbols. Line numbers are labeled approximate/verify-on-read.
- [ ] `docs/handoff/README.md` links to the new dossier with a valid relative link.
- [ ] New/edited Markdown has no trailing whitespace and ends with exactly one newline.

### Validation commands
Run from the repo root `/home/david/gitsandbox/system/system` and capture exact exit codes and key output lines. **"Expected exit N" is the PASS condition for that command — a headless runner must treat the listed code as success, even when it is nonzero (notably the secret-scan grep, where exit 1 = no match = PASS).**
- `python tools/precommit_text_guards.py docs/handoff/research/FX_ENABLEMENT_RESEARCH.md docs/handoff/README.md` -> expected exit `0` (no trailing whitespace / missing newline).
- `python tools/validate_docs.py` -> expected exit `0` (all relative Markdown links resolve, required docs present).
- `python tools/validate_repo.py` -> expected exit `0` (repo-level structural validator passes).
- `grep -rIEn "(sk-|access_token=[A-Za-z0-9]|OANDA_ACCESS_TOKEN=[A-Za-z0-9])" docs/handoff/research/FX_ENABLEMENT_RESEARCH.md` -> **expected exit `1` = PASS** (no match; i.e. no real-looking secret value). Expected: no output. (The fake placeholder `OANDA_ACCESS_TOKEN=<redacted-do-not-commit>` does not match because `<` is not `[A-Za-z0-9]`.)
- `grep -qE '^> (NETWORK MODE: ONLINE|NETWORK UNAVAILABLE:)' docs/handoff/research/FX_ENABLEMENT_RESEARCH.md` -> **expected exit `0` = PASS** (exactly one network-mode marker present, proving the mode was declared honestly). If offline, additionally `grep -q 'ASSUMPTION — confirm:' docs/handoff/research/FX_ENABLEMENT_RESEARCH.md` -> expected exit `0`.
- `test -f docs/handoff/research/FX_ENABLEMENT_RESEARCH.md && echo OK` -> prints `OK`.
- `git status --short --untracked-files=all` -> shows only the new `docs/handoff/research/FX_ENABLEMENT_RESEARCH.md` (and at most the `docs/handoff/README.md` link edit, and the optional doc-existence test if you added one). No `.py` runtime files, no schema/migration. (`git status` always exits 0; the real check is that the printed file set matches the intended set — inspect the lines, do not rely on the exit code.)
- `python tools/git_worktree_triage.py` -> expected exit `0` (or its documented clean status); confirm it reports no unexpected/unintended changes.
- If you added the optional doc-existence test: `python -m pytest tests/test_fx_research_dossier_exists.py -q` -> expected exit `0`.

### Self-audit & NO-GO
After writing the dossier, audit your own work and report:
1. **Exact files changed** (with `git status --short --untracked-files=all`) and one line each on why — expect only the new dossier, optionally the `docs/handoff/README.md` link, and optionally the one doc-existence test. If anything else changed (especially any `.py`), revert it and explain.
2. **Enforcement reality check.** State plainly that FX-00 produces *guidance only* and changes **no runtime behavior** — the hard caps (leverage 50:1/20:1, FIFO, no-hedging), the cost model, the canonical-symbol/clock/transform-ownership decisions, and the broker read-only/paper-first constraints described in the dossier are **recommendations** that FX-01..FX-06 must enforce in runtime code; FX-00 does not and must not enforce them. Confirm you did not smuggle enforcement into code.
3. **Network honesty.** State whether you were online or offline, and confirm the corresponding machine-detectable marker is present (`> NETWORK MODE: ONLINE` or `> NETWORK UNAVAILABLE:`). If offline, confirm every regulatory/fee/API figure is marked `ASSUMPTION — confirm:`. If online, confirm each cited number has a real URL + retrieval date and that you actually fetched it (no invented URLs).
4. **Anchor accuracy.** Confirm each repo anchor cited in the dossier still matches the repo by re-grepping the symbols and recording the line each lands on: `COT_FEATURE_IDS` (expect line 34), `class CotContractSpec` (expect line 55, decorator 54), the `6E`/`EURO FX` spec (~line 74), `FEATURE_GROUPS` (expect 456), `FEATURE_STAGE_SHADOW`/`FEATURE_STAGE_LIVE` (expect 504/505), `PRICE_FEATURE_IDS` (expect 328), `return "asset:FX"` (expect conformal line 95), and the OANDA/IBKR/Polygon env names in `_BASE_CREDENTIAL_RUNTIME_ENV_KEYS` (confirm no `IBKR_API_KEY`). Note any line-number drift and fix it in the dossier (keep line numbers labeled approximate).
5. **Docs update.** Confirm the dossier is linked from `docs/handoff/README.md` and that `tools/validate_docs.py` passes.
6. **Verdict.** If every Definition-of-Done item is met and all validation commands return their expected exit codes (per the PASS conditions above), say **GO**. If any required topic is missing, any figure is unverifiable without being marked, the network-mode marker is absent, any secret value leaked, any `.py`/schema was touched, or any validator fails, say **NO-GO** and list the exact remaining work.

---

## FX-01 — FX data source + ingestion

**Mission:** Stand up FX market-data ingestion as a first-class, operator-managed data source by building a real read-only OANDA price provider/adapter, seeding a major-pairs universe, extending macro factor ingestion with FX-relevant series (rate differentials, DXY, real yields), broadening CFTC COT FX coverage, and registering the FX feed through the existing data-source control plane — reusing (never forking) the ingestion runtime, storage facade, and job registry.

**Prerequisites:** None. FX-01 is the foundation workstream and merges first. Do not assume any FX-02..FX-08 artifact exists. In particular, there is **no** first-class FX instrument model yet (that is FX-02), so represent FX pairs using the existing `symbols`/universe + `meta_json` mechanism that other providers already use (e.g. `price_provider`, `ccxt_market`). Do not invent a new instrument schema.

> **Cross-workstream ownership notes (read once, then obey):**
> - **FX-01 is the sole owner of `engine/data/cftc_cot.py` `DEFAULT_COT_CONTRACT_SPECS` FX rows.** FX-03 consumes these specs and surfaces `fx.cot_*` feature ids only; it does **not** author COT contract specs. If a later prompt appears to also edit this dict, that is a known overlap resolved in FX-01's favor — author the full FX COT set here.
> - **FX-01 ships raw rows only, not per-pair transforms.** FX-01 lands raw FRED series, raw COT rows, and raw OANDA price bars. It does **not** compute per-pair rate differentials, carry, DXY z-scores, cross-pair correlations, or tsmom/trend features — those transforms are owned by FX-03 (feature loaders) and consume FX-01's raw rows. Do not add feature computation here; do not claim FX alpha.
> - **Canonical stored FX symbol form is the 6-letter concatenated form** (`EURUSD`, `USDJPY`). FX-01 seeds exactly that form into `symbols`. The OANDA underscore form (`EUR_USD`) exists only as a per-provider instrument mapping via the `fx_pair_to_oanda_instrument` helper and `meta_json.oanda_instrument`; it is never the stored symbol. FX-02 later formalizes symbol semantics, but FX-01's seeded form is the canonical one it must adopt.
> - **Canonical OANDA credential env name is `OANDA_ACCESS_TOKEN`** (fallback `OANDA_API_KEY`), already present in `data_source_manager._BASE_CREDENTIAL_RUNTIME_ENV_KEYS` (lines 106-107) and `runtime_bootstrap._BOOTSTRAP_CREDENTIAL_RUNTIME_ENV_KEYS` (lines 72-73). Do **not** introduce `OANDA_API_TOKEN` or any other token name.
> - **Economic-calendar / central-bank event ingestion is explicitly NOT in FX-01.** FX-01 ingests only FRED macro series and COT. The `fx.event_*` event-window feature family (FOMC/ECB/BoJ/NFP/CPI windows) has no ingestion owner in this workstream; do not silently absorb it here. Note it as out-of-scope/unowned in your self-audit so it is not mistaken for a delivered capability.

**Global constraints (read and obey all):**
- Target repo: `/home/david/gitsandbox/system/system` (the only repo; all paths below are relative to it).
- **Never** print, log, echo, commit, or test with real secret values. In tests use generated canary strings (e.g. `oanda-canary-<uuid>`) and assert they never appear in logs, API responses, connection-test evidence, or status payloads.
- **Never** call live broker order, cancel, replace, or flatten paths. The OANDA work here is **read-only market data only** (pricing/candles). Do not add, import, or wire any OANDA order/trade/account-mutation endpoint. All FX trading graduates through shadow/paper and governance later (other workstreams); FX-01 adds data only.
- Build on existing architecture; never replace it. Respect the model-vs-runtime contract (models propose intent; runtime owns safety gates) and train/serve parity through `engine/strategy/feature_registry.py`. FX-01 does **not** add model features or feature ids — it only lands raw data + factor series; feature wiring is FX-03.
- `engine/runtime/storage.py` schema changes are high blast radius. The COT and macro tables FX-01 touches are created via `CREATE TABLE IF NOT EXISTS` inside `engine/data/cftc_cot.py` and `engine/data/factor_ingestion.py` (not in `storage.py`); reuse those existing idempotent `ensure_*` helpers and add **no** new columns to `storage.py`. If you find you need a genuinely new table, add it through the same `CREATE TABLE IF NOT EXISTS` + idempotent-index pattern already used in those modules, and cover it with a test.
- "Profitable" is never asserted in this workstream. FX-01 ships no alpha and no labels; it must not claim profitability anywhere.
- Fail closed: missing OANDA credentials must disable the OANDA feed and fall back to existing providers, exactly like the existing `_SAFE_NO_CREDENTIAL_ENV` / `safe_no_credential_market_data_mode()` behavior — never fake a working feed.

### In scope
1. A real **OANDA read-only price provider** under `engine/data/live_prices/oanda_live.py` implementing the same duck-typed contract the other live providers expose: a class `OANDAPriceProvider` with `fetch_last_prices(ticker_map: Dict[str, str]) -> Dict[str, dict]` returning rows shaped like the existing providers (`{"ts_ms", "price", "bid", "ask", "spread", "volume", "source"}`).
2. Registering OANDA in the price-provider registry (`engine/data/provider_registry.py`) as a polling `live_price_provider` advertising `supports={"asset_classes": ["fx"], "transport": "rest"}`, default-disabled via an `OANDA_ENABLED` env flag.
3. Extending the **"supports"** metadata of providers that genuinely can serve FX so the catalog stops advertising equities/crypto-only. Concretely: add `"fx"` to IBKR's `asset_classes` (IBKR supports FX via cash/`IDEALPRO`). Note the IBKR definition is the streaming entry (`mode="streaming"`, `implementation_kind="daemon"`, lines 93-103) — that is the object whose `supports` you update; there is no separate polling IBKR definition. Add `"fx"` to CCXT only if you also make the adapter FX-capable; otherwise leave CCXT untouched. Do **not** claim FX support you did not implement.
4. Seeding a **major-pairs FX universe** in `engine/data/default_symbols.py` via a new `FX_MAJOR_SEED_SYMBOLS` list (the 7 majors: `EURUSD, USDJPY, GBPUSD, USDCHF, USDCAD, AUDUSD, NZDUSD`) wired into `load_default_symbols()` behind the existing `DEFAULT_SYMBOLS_INCLUDE_SEEDS` opt-in, plus an FX-specific opt-in env, plus a helper that maps each pair symbol to its OANDA instrument form (e.g. `EURUSD` -> `EUR_USD`).
5. Extending `engine/data/factor_ingestion.py` `MACRO_SERIES_SPECS` (list ends at line 219) with FX-relevant FRED series: a US **real yield** (FRED `DFII10`), the **trade-weighted dollar / DXY proxy** (FRED `DTWEXBGS`), and at least two **policy-rate / rate-differential inputs** for non-US majors (e.g. ECB `ECBDFR`, plus a Japan or UK policy/short-rate series) so a downstream consumer (FX-03) can compute USD-vs-foreign rate differentials. FX-01 ships only the raw series rows; it does **not** compute the differentials. Use the existing `MacroSeriesSpec` dataclass (line 92; note the field is `source_series_id`, not `series_id`), `download_mode="fred_graph"` for daily series, and the existing materialize path. Do not change the factor table schema.
6. Broadening **CFTC COT FX coverage** in `engine/data/cftc_cot.py`: keep the existing `6E` EURO FX contract and add the other major FX futures (British Pound `6B`, Japanese Yen `6J`, Swiss Franc `6S`, Canadian Dollar `6C`, Australian Dollar `6A`, New Zealand Dollar `6N`) as `CotContractSpec` rows in `DEFAULT_COT_CONTRACT_SPECS`, each mapped to its FX pair symbol(s) with `topic="fx"`. FX-01 is the sole owner of these specs (see ownership note above).
7. **Enabling the COT ingestion job to be operator-controllable for FX**: the `ingest_cftc_cot` daemon stays default-off, but ensure the broadened FX contracts are picked up when an operator enables the `cftc_cot` source through the control plane. Do not flip the global default to on; do not bypass governance.
8. Registering the FX feed in the **data-source control plane** (`services/data_source_manager.py`): a new `oanda_fx` catalog entry in `_default_catalog()`, an `oanda` provider-account entry in `_provider_account_catalog()` sharing `OANDA_ACCESS_TOKEN`/`OANDA_API_KEY`, a truthful connection-test handler `_test_oanda_connection` registered in `_PROVIDER_TEST_REGISTRY`, and `_SOURCE_CATALOG_OPERATIONAL_METADATA` entry as needed. The feed must flow through enable/disable, runnable state, `inject_into_provider_registry()`, and `get_desired_ingestion_jobs()`.
9. Route exposure: confirm and (only if needed) extend `routes/data_sources_routes.py` so the new `oanda_fx` source appears in `/api/data_sources` (catalog/templates), supports enable/disable/test, and never leaks the OANDA token. The existing routes already dispatch generically; prefer to add **nothing** here unless a test proves a gap.

### Out of scope (do not touch)
- FX-02: any first-class FX instrument metadata model, new instrument tables, pip/lot/contract-size metadata schemas. Use existing `symbols.meta_json`.
- FX-03: feature ids, `engine/strategy/feature_registry.py`, train/serve feature wiring, any model input. Do not register FX features. Do **not** compute per-pair rate-differential / carry / DXY / cross-pair / tsmom transforms (those are FX-03's loaders consuming FX-01's raw rows).
- **Economic-calendar / central-bank event ingestion** (FOMC/ECB/BoJ/NFP/CPI event timestamps). No prompt in this workstream owns it; FX-01 does not absorb it. Flag it as unowned in the self-audit.
- FX-04: prediction routing, FX regime detection, FX-correct labels.
- FX-05/FX-06: portfolio sizing, currency-aware risk, execution, broker routing, 24/5 session logic. **No order/cancel/replace/flatten code.** Do not create an OANDA execution adapter (`engine/execution/broker_oanda_rest.py` is FX-06, if ever).
- FX-07: backtest realism, swap/carry cost modeling, governance/promotion gates, profitability evidence.
- FX-08: operator-UI rendering beyond the data-source status that the control plane already surfaces. Do not edit `ui/` beyond what an existing test requires (prefer zero UI changes).
- `engine/runtime/storage.py` schema/migrations. Do not modify it. (Note: there is no `engine/data/storage.py` file in this repo; `engine/runtime/storage.py` is the only storage facade.)

### Verified anchors
(Confirmed by reading the repo at `/home/david/gitsandbox/system/system` on the FX-01 branch. Line numbers verified; treat as exact but re-grep if your checkout differs.)
- `engine/data/provider_registry.py:55` — `_builtin_provider_definitions()`. Definitions: `polygon_ws` (streaming/daemon, lines 82-92), **`ibkr` (streaming/daemon, lines 93-103, `supports={"asset_classes": ["equities"], "transport": "gateway"}` at line 101)**, `polygon` (polling/live_price_provider, 104-114), `tradier` (polling/options_chain_provider, 115-124), `yfinance` (polling/live_price_provider, 126-135), `ccxt` (polling/live_price_provider, `["crypto"]` at line 144). No FX provider exists. `_env_enabled` local at line 56.
- `engine/data/provider_registry.py:277` — `get_provider_definition(name)`. `:287` — `get_polling_provider_names()` returns enabled polling `live_price_provider` names; `poll_prices.py` consumes this. `:310` — module-level `_env_enabled(name, default=False)`. `:325` — `_operational_market_data_job_names(candidates)` (gates `poll_prices` on `yfinance_enabled`/`ccxt_enabled`/polygon, lines 332-362). `:365` — `get_enabled_market_data_job_names()`. `:419` — `build_price_provider(provider_name)`.
- `engine/data/live_prices/ccxt_live.py:1114` — `class CCXTPriceProvider`; `:1118` — `fetch_last_prices(ticker_map)`. Row shape at lines 598-604: `{"ts_ms", "price", "bid", "ask", "spread", "volume", "source": "ccxt"}` — mirror this exactly. `engine/data/live_prices/ibkr_live.py:57` — `class IBKRPriceProvider`; `:112` — `fetch_last_prices`. `polygon_live.py` and `yfinance_live.py` follow the same duck-typed shape; use them as templates for `oanda_live.py`. `_warn_nonfatal` helper exists in `ccxt_live.py:84` — mirror the nonfatal-warn pattern.
- `engine/data/_credentials.py:93` — `get_data_credential(name, ttl_s=300) -> str`. Use this for `OANDA_ACCESS_TOKEN` (fallback `OANDA_API_KEY`).
- `engine/data/default_symbols.py:13` — `_VALID_TICKER_RX = re.compile(r"^[A-Z][A-Z0-9]{0,5}$")` (max 6 chars). All 7 majors are exactly 6 chars and **verified to match** this regex. `:35` `ETF_SEED_SYMBOLS`; `:42` `CROSS_ASSET_SEED_SYMBOLS`; `:184` per-symbol validation via `_VALID_TICKER_RX.match`; `:197` `load_default_symbols()`; `:204` reads `DEFAULT_SYMBOLS_INCLUDE_SEEDS` (default "1"); `:207-208` ETF/cross-asset seed gating. **No FX symbols seeded.**
- `engine/data/factor_ingestion.py:61` — `_DEFAULT_MACRO_SYMBOL_MAP`. `:92` — `class MacroSeriesSpec` (fields: `factor_id`, `source_series_id`, `family`, `name`, `cadence`, `applies_to`, `units`, `transform`, `release_hour_et`, `release_minute_et`, `is_revisioned`, `history_start`, `z_window`, `delta_lag`, `download_mode="alfred_initial"`, `emit_release_event=None`, `symbol_topic=None`). `:112` — `MACRO_SERIES_SPECS` (US-only FRED series CPI/Fed target/unemployment/GDP/WTI/NatGas; **list ends at line 219**; existing `fred_graph` examples at lines 198, 216). `:396` `_download_fred_graph_rows`; `:914` `_materialize_release_features`; `:976` symbol-map build; `:1007` `macro_target_symbols`; `:1418` `ensure_macro_factor_registry`; `:1598` `sync_macro_factors`. **No DXY, no real yields, no rate differentials.**
- `engine/data/cftc_cot.py:34` — `COT_FEATURE_IDS`. `:54` — `@dataclass(frozen=True)` decorator; `:55` — `class CotContractSpec` (fields: `contract_key`, `report_type`, `market_name_contains`, `symbols: Tuple[str, ...]`, `topic=""`, `weight=1.0`; `dataset_id` property). `:68` — `DEFAULT_COT_CONTRACT_SPECS`; **only one FX row: `CotContractSpec("6E", "legacy", "EURO FX", ("FXE", "6E"), "fx")` at line 74.** `:156` reads `CFTC_COT_CONTRACTS_JSON` override; `:186` `load_cot_contract_specs()`; `:289` `seed_default_cot_mappings(con)`; `:355` `_where_for_spec(spec, since_day)` (uses `market_name_contains`); `:363` `fetch_cot_records`; `:790` `ingest_cot_batch`.
- `engine/data/jobs/ingest_cftc_cot.py:36` — `JOB_NAME`; `:40` `INGEST_ENABLED` from `INGEST_CFTC_COT_ENABLED` default "0"; `:103` `main()` requires `ENGINE_SUPERVISED=1`, then checks `INGEST_ENABLED` and `manager.is_job_enabled(...)`. Job is default-off and control-plane-gated.
- `engine/runtime/runtime_bootstrap.py:45` — `_BOOTSTRAP_CREDENTIAL_RUNTIME_ENV_KEYS`; `OANDA_ACCESS_TOKEN`/`OANDA_API_KEY` appear as names at lines 72-73.
- `services/data_source_manager.py` — `OANDA_ACCESS_TOKEN`/`OANDA_API_KEY` are in `_BASE_CREDENTIAL_RUNTIME_ENV_KEYS` at **lines 106-107**. No OANDA `SourceDefinition`, no `ProviderAccountDefinition`, no `_test_oanda_*` handler exists. Dataclasses: `SourceDefinition` **line 401** (fields include `source_type`, `display_name`, `job_name`, `provider_name=""`, `singleton=True`, `default_enabled=True`, `credential_env`, `setting_env`, `guide`, `storage_tables=()`, `consumers=()`, `safe_to_auto_enable=False`, `runtime_runnable=True`), `ProviderAccountDefinition` **line 457** (fields `account_key`, `display_name`, `provider_name`, `credential_env`, `used_by_sources=()`, `used_by_jobs=()`, `guide`), `ConnectionTestResult` **line 471** (`status`, `classification`, `message`, `evidence`, `next_steps`). `_PROVIDER_TEST_REGISTRY` **line 502** (keyed by source/provider key -> `{"handler", "label"}`). `_default_catalog()` **line 540** (price-provider entries use `source_type="price_provider"`, see `polygon` at 569). `_provider_account_catalog()` **line 1351** (see `polygon` account at 1353). `_SOURCE_CATALOG_OPERATIONAL_METADATA` **line 1607** (keyed by source key; price-provider entries use `storage_tables=("prices","price_quotes","price_quotes_raw","price_provider_health")`, `safe_to_auto_enable=False`; `runtime_runnable` defaults True and is omitted unless False, as in `alpaca_broker_data` at 1623-1628). `inject_into_provider_registry()` **line 4995** (projects enabled `price_provider`/`options_provider` source rows to provider overrides keyed by lower-case provider name with `enabled`/`source_key`/`job_name`/`config_hash`). `get_desired_ingestion_jobs()` **line 5006**. Connection helpers: `_connection_pass` **5420**, `_connection_fail` **5423**, `_connection_degraded` **5433**, `_missing_credentials_result(provider, *fields, source=None)` **5452**, `_dependency_missing_result` **5482**, `_connection_effective_env_value(source, env_name)` **5527**, `_connection_setting` **5571**, `_http_json_probe(source, *, provider, url, method="GET", params=None, headers=None, body=None, timeout_s=10.0, expected_paths=(), success_message, empty_message, apply_rate_limit=True)` **5671** (uses module-level `requests`; safe-endpoint redaction built in).
- `routes/data_sources_routes.py:20-29` — generic dispatch table (`/api/data_sources` GET at 20, create/update/delete/enable/disable/test/test_save at 22-28, accounts/update at 29). `api_get_data_sources` at line 64; `api_post_data_source_test` at line 27 (dispatch) / handler in same module. New catalog entries mount without per-source edits.
- `engine/runtime/job_registry.py:341` — `poll_prices` daemon. `:355` `poll_macro`. `:362` `backfill_macro_vintages`. `:1077` — `ingest_cftc_cot` registered as `daemon`, `{"execution": False, "schedule": "every 86400s", "cadence_seconds": 86400}`. Reuse `poll_prices` for OANDA polling; reuse `poll_macro`/`backfill_macro_vintages` for the new macro series. **Do not add a new daemon for OANDA.**
- `engine/data/poll_prices.py:584` — `class PollingProviderSession`; `:610` `set_symbol_map`; `:656`/`:699` `fetch_last_prices` calls. `:717` — `@dataclass(frozen=True) class ActiveSymbolUniverse` with fields `active_symbol_rows`, `provider_symbol_rows`, `yf_map`, `ccxt_map`, `polygon_map`, and `assigned_symbol_count` property (lines 717-727). **Adding an `oanda_map` requires adding it to this dataclass and to `assigned_symbol_count`.** `:801-855` — builds per-provider symbol maps from `symbols.meta_json` (`price_provider` + `ccxt_market`/`polygon_ticker`/`yf_ticker`); the `provider == "ccxt"` branch is at 817-821, `polygon`/`polygon_ws` at 823-825 — add an `oanda` branch here mapping `meta.get("oanda_instrument")` (or derived `EUR_USD`).
- `engine/data/README.md:15` — `live_prices/` listing. `:165` — "Extending Data Flows": add provider under `live_prices/`, register the job, decide startup orchestration, document in README. Follow this.
- `requests==2.32.3` and `zoneinfo` are both importable in this environment (verified) — `oanda_live.py` and `_test_oanda_connection` may rely on `requests` without an SDK.

### Implementation steps
1. **OANDA provider/adapter.** Create `engine/data/live_prices/oanda_live.py`:
   - Guard the HTTP client import the same defensive way the other adapters guard their SDKs (wrap in try/except, store an import-error sentinel, raise a clear `oanda_unavailable:` `RuntimeError` if used without deps). Prefer plain `requests` (already a dependency) hitting the OANDA v20 **pricing** REST endpoint (`/v3/accounts/{account_id}/pricing` or the instrument candles endpoint) — read-only only.
   - Read credentials via the existing pattern: `from engine.data._credentials import get_data_credential` for `OANDA_ACCESS_TOKEN` (fallback `OANDA_API_KEY`), and `OANDA_ACCOUNT_ID`, `OANDA_ENVIRONMENT` (`practice`|`live`, default `practice`) from env. Never log token values; use `_warn_nonfatal` like the sibling adapters.
   - Implement `class OANDAPriceProvider` with `__init__` (resolve base URL from environment: practice -> `https://api-fxpractice.oanda.com`, live -> `https://api-fxtrade.oanda.com`) and `fetch_last_prices(ticker_map)` returning the standard row dict. Compute `spread = ask - bid` when both present; set `"source": "oanda"`. On missing creds / HTTP failure, return `{}` and warn — never raise into the polling loop.
   - Honor the canary-secret constraint: token must never appear in returned rows, logs, or exceptions.
2. **Register OANDA in `provider_registry.py`.** In `_builtin_provider_definitions()` add a `PriceProviderDefinition(provider_name="oanda", mode="polling", implementation_kind="live_price_provider", enabled=_env_enabled("OANDA_ENABLED", False), daemon_job_name="poll_prices", daemon_script="engine/data/poll_prices.py", priority=<between yfinance(40) and ccxt(50)>, supports={"asset_classes": ["fx"], "transport": "rest"}, build_price_provider=_build_oanda)` and add `_build_oanda()` importing `OANDAPriceProvider`. Add an `oanda_enabled = _env_enabled("OANDA_ENABLED", False)` check inside `_operational_market_data_job_names` so that enabling OANDA keeps `poll_prices` in the job set (mirror the `ccxt_enabled`/`yfinance_enabled` logic in the `poll_prices` branch, lines 355-361). Update the IBKR `supports` `asset_classes` (streaming definition, line 101) to include `"fx"`.
3. **FX universe seed.** In `engine/data/default_symbols.py` add `FX_MAJOR_SEED_SYMBOLS = ["EURUSD","USDJPY","GBPUSD","USDCHF","USDCAD","AUDUSD","NZDUSD"]`. (All 7 are exactly 6 chars and verified to pass `_VALID_TICKER_RX`; re-confirm with the validation command below before relying on it.) Wire `FX_MAJOR_SEED_SYMBOLS` into `load_default_symbols()` behind `DEFAULT_SYMBOLS_INCLUDE_SEEDS`, gated additionally by an FX opt-in env (`FX_PAIRS_ENABLED`, default off, also honoring an `OANDA_FX_PAIRS` explicit override list) so FX symbols are not force-seeded for non-FX deployments. Add a pure helper `fx_pair_to_oanda_instrument(symbol) -> str` (e.g. `"EURUSD" -> "EUR_USD"`) and its inverse `oanda_instrument_to_fx_pair`, kept dependency-free for reuse by the provider and `poll_prices`. This helper is the **only** OANDA-form converter in the codebase.
4. **poll_prices routing.** In `engine/data/poll_prices.py`: add an `oanda_map: Dict[str, str]` field to the `ActiveSymbolUniverse` dataclass (line 717-723) and include it in `assigned_symbol_count` (line 727). In the map-building loop (801-828) add an `if provider == "oanda":` branch reading `meta.get("oanda_instrument")` or deriving it from the symbol via `fx_pair_to_oanda_instrument`, populating `oanda_map`. Apply `filter_symbol_mapping_for_shard` to `oanda_map` alongside the others (line 849-851) and add it to the returned `ActiveSymbolUniverse`. Wire an OANDA `PollingProviderSession` fetch path consistent with the existing ccxt/polygon/yfinance sessions. Keep ordering deterministic and do not disturb existing providers.
5. **Macro FX factor specs.** In `engine/data/factor_ingestion.py` append `MacroSeriesSpec` rows to `MACRO_SERIES_SPECS` (before the closing `]` at line 219) for: `macro.real_yield_10y` (`source_series_id="DFII10"`, daily, `fred_graph`), `macro.usd_broad_index` (`source_series_id="DTWEXBGS"`, daily, `fred_graph`), `macro.ecb_policy_rate` (`source_series_id="ECBDFR"`, daily, `fred_graph`), and one more non-US short rate (e.g. UK `IUDSOIA` or Japan `IRSTCI01JPM156N`). Use `family="macro"`, sensible `applies_to`/`symbol_topic` (e.g. `"fx"`/`"rates"`), `is_revisioned=False`, conservative `history_start`. Do not change the table schema or the materialize logic; reuse `_materialize_release_features`. Add the new series to `_DEFAULT_MACRO_SYMBOL_MAP` (line 61) under an `"fx"` topic pointing at the FX pair symbols (or leave symbol-less if `macro_target_symbols` would otherwise force equities). **FX-01 ships only the raw series; it does not compute per-pair rate differentials or carry — that is FX-03.**
   - **FRED resolvability caveat:** the `fred_graph` CSV endpoint cannot be reached offline in this environment, so the tests cannot prove a series id resolves live. If you cannot confirm a chosen series id (especially the non-US short rate) resolves on the FRED graph CSV endpoint, you MUST treat that series as unverified: keep `DFII10`/`DTWEXBGS`/`ECBDFR` (well-known, high-confidence ids), mark the 4th series with a `# TODO(FX-01): verify <id> resolves on fred_graph CSV` comment and an xfail/skip note, and declare **NO-GO** on that single item rather than asserting GO on an unverifiable id.
6. **COT FX contracts.** In `engine/data/cftc_cot.py` extend `DEFAULT_COT_CONTRACT_SPECS` (line 68) with `6B`/`6J`/`6S`/`6C`/`6A`/`6N` (`report_type="legacy"`, correct `market_name_contains` upper-case substrings: `"BRITISH POUND"`, `"JAPANESE YEN"`, `"SWISS FRANC"`, `"CANADIAN DOLLAR"`, `"AUSTRALIAN DOLLAR"`, `"NEW ZEALAND DOLLAR"`), each `topic="fx"`, mapped to the corresponding FX pair symbol (e.g. `CotContractSpec("6B", "legacy", "BRITISH POUND", ("GBPUSD", "6B"), "fx")`). Keep the existing `6E` row. These flow automatically through `seed_default_cot_mappings`, `fetch_cot_records` (`_where_for_spec` uses `market_name_contains`), and `ingest_cot_batch`. Do not change the COT table schema. **FX-01 is the sole author of these specs.**
7. **Control-plane catalog entry.** In `services/data_source_manager.py`:
   - Add `"oanda_fx": SourceDefinition(source_type="price_provider", display_name="OANDA FX", provider_name="oanda", job_name="poll_prices", default_enabled=False, credential_env={"access_token": "OANDA_ACCESS_TOKEN"}, setting_env={"account_id": "OANDA_ACCOUNT_ID", "environment": "OANDA_ENVIRONMENT", "fx_pairs": "OANDA_FX_PAIRS"}, safe_to_auto_enable=False, storage_tables=("prices","price_quotes","price_quotes_raw","price_provider_health"), guide=_source_guide(category="Market Data", summary="Read-only OANDA v20 FX pricing.", ... explicit read-only / no-order-authority safety note ...))` to `_default_catalog()` (line 540). Because `source_type="price_provider"`, it is picked up by `inject_into_provider_registry()` and projected as the `oanda` provider override. (`runtime_runnable` defaults True — leave it runnable; OANDA pricing is data-only.)
   - Add an `"oanda"` `ProviderAccountDefinition(account_key="oanda", display_name="OANDA", provider_name="oanda", credential_env={"access_token": "OANDA_ACCESS_TOKEN", "api_key": "OANDA_API_KEY"}, used_by_sources=("oanda_fx",), guide=...)` to `_provider_account_catalog()` (line 1351), with a read-only safety guide.
   - Register `"oanda_fx": {"handler": "_test_oanda_connection", "label": "OANDA FX pricing"}` in `_PROVIDER_TEST_REGISTRY` (line 502).
   - Implement `_test_oanda_connection(self, source)`: resolve token via `self._connection_effective_env_value(source, "OANDA_ACCESS_TOKEN")`; if absent, return `self._missing_credentials_result("oanda_fx", "OANDA_ACCESS_TOKEN", source=source)`. Otherwise probe a read-only OANDA pricing/instruments endpoint with `self._http_json_probe(source, provider="oanda_fx", url=..., headers={"Authorization": f"Bearer {token}"}, ...)` — never put the token in `evidence`/`params`. Return `_connection_pass`/`_connection_fail`/`_connection_degraded` truthfully. Add an `"oanda_fx"` entry to `_SOURCE_CATALOG_OPERATIONAL_METADATA` (line 1607) with `"storage_tables": ("prices","price_quotes","price_quotes_raw","price_provider_health")`, `"consumers": ("price_router","model_feature_snapshots","dashboard_data_health")`, `"safe_to_auto_enable": False` (omit `runtime_runnable` so it defaults True).
8. **Routes.** Verify `oanda_fx` surfaces through the existing generic handlers in `routes/data_sources_routes.py` (it should, with zero edits). Only add code if a test proves a gap; if you do, keep the token redacted.
9. **Docs.** Update `engine/data/README.md` (`live_prices/` listing at line 15 + "Extending Data Flows" at line 165) to document the OANDA FX adapter and the FX universe/macro/COT additions. Add a short note to the data-source/operator docs if one already enumerates sources (search for where existing sources like `cftc_cot`/`ccxt` are documented and mirror it). Do not create brand-new top-level docs.
10. **Self-check the secret surface.** Grep your new code for any path that could serialize the token into logs, evidence, status payloads, or `meta_json`, and prove (with a test) it is redacted/absent.

### Tests to add
Place tests under `tests/` following existing conventions (`importlib.reload` for isolation, `monkeypatch`, sqlite isolation via `DB_PATH`/`TS_STORAGE_BACKEND=sqlite`/`DATA_SOURCE_MASTER_KEY` like `tests/test_data_source_catalog_metadata.py`, which uses `base64.b64encode(bytes(range(32)))` for the master key and sets `ENGINE_MODE=safe`/`EXECUTION_MODE=safe`/`DISABLE_LIVE_EXECUTION=1`). One behavior per bullet:
- `tests/test_oanda_live.py`:
  - `OANDAPriceProvider.fetch_last_prices` parses a mocked OANDA pricing JSON payload into the standard row dict (`price`/`bid`/`ask`/`spread`/`source == "oanda"`), monkeypatching `requests`.
  - Missing `OANDA_ACCESS_TOKEN` returns `{}` and does not raise.
  - A generated canary token is never present in returned rows or in captured log records.
- `tests/test_fx_provider_registry.py`:
  - With `OANDA_ENABLED=1`, `provider_registry.get_polling_provider_names()` includes `"oanda"` and its definition `supports["asset_classes"] == ["fx"]`.
  - With `OANDA_ENABLED` unset/false, `"oanda"` is absent and `poll_prices` remains the fallback job; enabling OANDA keeps `poll_prices` in `get_enabled_market_data_job_names()`.
  - IBKR definition `supports["asset_classes"]` now contains `"fx"`.
- `tests/test_fx_universe_seed.py`:
  - With FX seeding opted in, `load_default_symbols()` contains all 7 majors; with it opted out it contains none of them; non-FX deployments (default env) are unaffected.
  - `fx_pair_to_oanda_instrument("EURUSD") == "EUR_USD"` and round-trips through `oanda_instrument_to_fx_pair`.
- `tests/test_fx_macro_specs.py`:
  - `MACRO_SERIES_SPECS` includes `source_series_id` values `DFII10`, `DTWEXBGS`, `ECBDFR`, and the 4th rate series; each has a unique `factor_id`; no duplicate `factor_id`s across the whole list; each new spec's `download_mode` is `"fred_graph"`. (This cannot prove live FRED resolvability — see the NO-GO caveat for the unverified series.)
- `tests/test_fx_cot_contracts.py`:
  - `DEFAULT_COT_CONTRACT_SPECS` contains contract keys `6E,6B,6J,6S,6C,6A,6N` all with `topic == "fx"`; `seed_default_cot_mappings(con)` on an in-memory/sqlite test DB inserts the FX pair mappings; `_where_for_spec` for the yen spec contains `JAPANESE YEN`.
- `tests/test_fx_data_source_catalog.py`:
  - `_default_catalog()` has `oanda_fx` with `source_type=="price_provider"`, `provider_name=="oanda"`, `default_enabled is False`, `safe_to_auto_enable is False`.
  - `_PROVIDER_TEST_REGISTRY["oanda_fx"]["handler"] == "_test_oanda_connection"`.
  - `_test_oanda_connection` with no token returns a missing-credentials/non-pass result; with a mocked successful `_http_json_probe` returns `status=="pass"` and the canary token is absent from the result payload/evidence. (The live-probe path against a real OANDA endpoint is **not** exercised offline — mark it a known GAP in the self-audit; only the mocked branch is verified.)
  - After enabling `oanda_fx` through the manager, `inject_into_provider_registry()` reports `oanda` enabled and `get_desired_ingestion_jobs()` includes `poll_prices`.
  - `/api/data_sources` (via `routes.data_sources_routes.api_get_data_sources`) lists `oanda_fx` in templates/sources and never contains the canary token.

### Definition of Done
- [ ] `engine/data/live_prices/oanda_live.py` exists with a read-only `OANDAPriceProvider.fetch_last_prices` returning the standard row dict; no order/trade/account-mutation endpoints anywhere; deps are import-guarded; failures degrade (return `{}`) rather than raise.
- [ ] `provider_registry.py` advertises an `oanda` polling provider with `asset_classes:["fx"]`, default-off via `OANDA_ENABLED`, buildable via `build_price_provider("oanda")`; IBKR `supports` (streaming definition, line 101) includes `"fx"`.
- [ ] `default_symbols.py` seeds the 7 FX majors behind an FX opt-in and exposes `fx_pair_to_oanda_instrument`/`oanda_instrument_to_fx_pair`; non-FX deployments are unaffected.
- [ ] `poll_prices.py` routes OANDA-tagged symbols to the OANDA provider via a new `oanda_map` on `ActiveSymbolUniverse` without disturbing existing provider maps.
- [ ] `factor_ingestion.py` adds the FX-relevant FRED specs with no schema change and no duplicate `factor_id`s (or NO-GO on any series id that cannot be confirmed to resolve on the FRED graph CSV endpoint).
- [ ] `cftc_cot.py` covers all 7 major FX futures with `topic="fx"`, keeping `6E`, with no COT schema change; FX-01 is the sole author of these specs.
- [ ] `data_source_manager.py` has the `oanda_fx` catalog entry, `oanda` provider account, `_test_oanda_connection` handler, and operational metadata; the feed flows through enable/disable, runnable state, provider-registry injection, and desired-jobs.
- [ ] No per-pair rate-differential / carry / DXY / cross / tsmom computation and no `fx.*` feature ids were added (those belong to FX-03); FX-01 ships raw rows only.
- [ ] Enforcement lives in runtime code (registry/manager/poll_prices/adapter), not only in tests or docs. The `ingest_cftc_cot` daemon remains default-off and control-plane-gated (`INGEST_CFTC_COT_ENABLED` default "0" unchanged); no global defaults flipped to on.
- [ ] No real secret is logged/echoed/committed; canary-token tests prove redaction across rows, logs, evidence, and API responses.
- [ ] `engine/runtime/storage.py` unchanged; no broker order/cancel/replace/flatten code added; no OANDA execution adapter created.
- [ ] All new tests pass; full suite shows no regressions vs the captured baseline; lint clean; worktree triage clean.

### Validation commands
Run from the repo root `/home/david/gitsandbox/system/system`. Expected exit code is `0` unless noted.
- **Baseline capture (run FIRST, before any edits):** `python -m pytest -q 2>&1 | tail -40 > /tmp/fx01_pytest_baseline.txt; echo "baseline captured"` — capture the pre-change pass/fail set so any later red can be classified as pre-existing vs caused by FX-01. Re-run after your changes and diff: a failure present in the baseline is pre-existing (note it, do not mask it); a new failure is a regression you must fix.
- `python -c "import re; rx=re.compile(r'^[A-Z][A-Z0-9]{0,5}\$'); m=['EURUSD','USDJPY','GBPUSD','USDCHF','USDCAD','AUDUSD','NZDUSD']; assert all(rx.match(x) for x in m), [x for x in m if not rx.match(x)]; print('all majors valid')"` -> `0` (confirms the seed symbols pass `_VALID_TICKER_RX`; run before relying on the seed).
- `python -m pytest tests/test_oanda_live.py tests/test_fx_provider_registry.py tests/test_fx_universe_seed.py tests/test_fx_macro_specs.py tests/test_fx_cot_contracts.py tests/test_fx_data_source_catalog.py -q` -> `0`
- `python -m pytest tests/test_provider_registry_safe_jobs.py tests/test_data_source_catalog_metadata.py tests/test_data_source_provider_accounts.py tests/test_ccxt_live.py tests/test_cftc_cot_features.py -q` -> `0` (no regressions in adjacent suites)
- `python -m pytest -q` -> compare to baseline; new failures must be `0`. If a pre-existing failure is unrelated to FX-01, quote it from the baseline and do not mask it.
- `python -c "import engine.data.live_prices.oanda_live, engine.data.provider_registry, engine.data.default_symbols, engine.data.factor_ingestion, engine.data.cftc_cot, services.data_source_manager, routes.data_sources_routes"` -> `0` (imports clean)
- `python -c "from engine.data.provider_registry import build_price_provider, get_provider_definition; import os; os.environ['OANDA_ENABLED']='1'; d=get_provider_definition('oanda'); assert d and d.supports['asset_classes']==['fx'], d"` -> `0`
- `python tools/syntax_check_workspace.py` -> `0`
- `ruff check .` (or `python -m ruff check .`) -> `0`
- `git status --short --untracked-files=all` -> review the listed paths by hand (this command exits `0` regardless of contents, so it is a review aid, not a pass/fail gate). Only FX-01 files should appear: new `engine/data/live_prices/oanda_live.py`, new `tests/test_fx_*.py`/`tests/test_oanda_live.py`, and edits to `provider_registry.py`, `default_symbols.py`, `poll_prices.py`, `factor_ingestion.py`, `cftc_cot.py`, `services/data_source_manager.py`, `engine/data/README.md` (+ any source-enumerating operator doc).
- `python tools/git_worktree_triage.py` -> `0` (no duplicate-worktree problems introduced)

### Self-audit & NO-GO
After implementation, audit your own work and report:
1. **Exact files changed and why** — list every created/modified path with a one-line justification tied to a scope item above.
2. **Enforcement location proof** — show that FX enablement, default-off gating, and credential fail-closed behavior live in runtime modules (`provider_registry.py`, `data_source_manager.py`, `poll_prices.py`, `oanda_live.py`), not merely in tests/docs. Quote the specific lines that gate `OANDA_ENABLED`, the default-disabled `oanda_fx` source, and the missing-credentials path.
3. **Secret-surface audit** — grep your diff for `OANDA_ACCESS_TOKEN`/`OANDA_API_KEY`/`Authorization`/token variables and confirm none are logged, returned in evidence, or persisted to `meta_json`; cite the redaction test(s) that prove it. Confirm you used `OANDA_ACCESS_TOKEN` (canonical) and did not introduce `OANDA_API_TOKEN`.
4. **No-broker-authority proof** — confirm no OANDA order/trade/account-mutation endpoint, no order/cancel/replace/flatten path, and no OANDA execution adapter was added; the work is read-only pricing only.
5. **Schema proof** — confirm `engine/runtime/storage.py` is untouched (and that no `engine/data/storage.py` was created) and that any table interaction reuses existing `ensure_*` / `CREATE TABLE IF NOT EXISTS` idempotent helpers in `cftc_cot.py`/`factor_ingestion.py`.
6. **Ownership-boundary proof** — confirm FX-01 added no `fx.*` feature ids and no per-pair rate-diff/carry/DXY/cross/tsmom transforms (FX-03's job), and that you did not absorb economic-calendar/event ingestion (explicitly unowned in this workstream). Confirm the COT FX specs are authored solely here.
7. **Docs updated** — confirm `engine/data/README.md` (and any source-enumerating operator doc) reflects the FX additions.
8. **Unverifiable-path disclosure** — explicitly state which paths could only be exercised against mocks/offline: (a) the live OANDA pricing/instruments probe in `_test_oanda_connection` (only the mocked branch ran), and (b) live FRED `fred_graph` resolvability for the chosen series ids. For any FRED series id you could not confirm resolves, leave a `# TODO(FX-01)` + xfail/skip and declare **NO-GO** on that item.
9. If **any** Definition-of-Done item is not fully implemented, explicitly say **NO-GO**, mark the gap clearly (use a stub + `TODO(FX-01)` and a skipped/xfail test rather than faking success), and describe the remaining work. Otherwise state **GO** with the validation evidence (including the baseline-vs-final pytest diff).

---

## FX-02 — First-class FX instrument model

**Mission:** Replace the 4-symbol hardcoded FX heuristic with a real base/quote instrument-metadata model that is persisted in the `symbols` table and exposed through one documented accessor, becoming the single source of truth for FX symbol *semantics* (pair parsing, base/quote, pip/lot/contract, P&L currency, leverage-cap reference value, session-calendar id, and the canonical stored symbol form) consumed by later FX workstreams.

**Prerequisites:** None. This is the keystone FX workstream and must merge before every other FX-0x prompt. Specifically, FX-03 (features), FX-04 (labels/regime), FX-05 (sizing/risk), FX-06 (execution), and FX-07 (backtest/governance) all consume the accessor and the canonical-symbol rule you build here. Do NOT assume any FX-0x code exists in the tree; if you see references to FX feature groups, FX sizing, FX execution, an OANDA adapter, or a `fx_costs`/`fx_leverage_caps` module, treat them as out of scope and do not create or modify them. This prompt is self-contained: it depends only on the existing repo state described in **Verified anchors**.

**Global constraints (restate — these bind this task):**
- Target repo: `/home/david/gitsandbox/system/system`. All paths below are relative to that directory. This is the only repo.
- Build on existing architecture; never replace it. Respect the model-vs-runtime contract (models propose intent; the runtime owns safety gates). Preserve train/serve feature parity (`engine/strategy/feature_registry.py`) and the champion/challenger promotion path. This task adds a metadata layer; it must not give any model order authority or alter any gate.
- Never call any live broker order/cancel/replace/flatten path. This task is pure data/metadata; it touches no broker code.
- Never print, log, echo, commit, or test with real secret values. There are no secrets in this task; if a test needs a sentinel, use a generated canary string and assert it never appears in logs or outputs.
- `engine/runtime/storage.py` and its backends are HIGH blast radius: nearly every subsystem reads/writes through them. Any schema change requires a numbered Postgres migration AND the matching SQLite-test-backend change AND tests. Proceed carefully and additively (new nullable columns only; never drop or rename existing columns). Do NOT add new entries to `storage.py`'s `_REQUIRED_BACKEND_SYMBOLS` contract (verified at `engine/runtime/storage.py:56`), and do NOT add the accessor to `storage.py` (see step 3 — `storage.py` does not re-export universe helpers today).
- "Profitable net of realistic FX costs" is proven later through existing gates — out of scope here. Do not assert profitability. Your pip/lot/contract/leverage values are reference metadata only; they are *consumed*, not graded, by this task.

### In scope
- A pure-Python FX pair parser: given a symbol string, decide whether it is an FX spot pair and, if so, return its base currency, quote currency, pip size, contract/lot size, the currency P&L accrues in, a conservative leverage-cap *reference* value, and a tradable-session calendar identifier.
- A persisted instrument-metadata record on the existing `symbols` table (new nullable columns), populated on upsert, plus a documented read accessor `get_instrument_metadata(con, symbol) -> dict | None`.
- Rewiring `asset_class_for_symbol` in `engine/data/asset_map.py` so FX is derived from the parser instead of the hardcoded tuple `("DXY", "EURUSD", "USDJPY", "GBPUSD")`, while keeping `DXY` classified as FX (it is an index, not a parseable pair — the parser handles it as an explicit `fx_index` special case) and preserving the function's exact existing signature and return contract for all non-FX symbols.
- Threading metadata through universe management (`engine/data/universe.py` `upsert_symbol`).
- A new numbered migration `0070_fx_instrument_metadata.py` for Postgres plus the matching SQLite test-backend column additions, plus updates to `docs/Database_Schema.md`'s `symbols` Table Register row.
- A normative statement (FX-02 is the source of truth) fixing the **canonical stored symbol form** for FX pairs (6-letter concatenated, uppercase, no separator — e.g. `EURUSD`, `USDJPY`), so later prompts that key cost/risk tables normalize through the FX-02 accessor rather than re-deriving.
- Tests covering the parser, the accessor, the migration/round-trip (including float-vs-text column typing), and the asset-class derivation.

### Out of scope (do not touch)
- Anything that *consumes* the metadata for behavior: feature computation (`engine/strategy/feature_registry.py` beyond leaving its `asset_class_for_symbol` call working), sizing/risk math (`engine/risk/*`), execution/broker routing (`engine/execution/*`), labels, prediction routing, backtest realism. Those are FX-03/04/05/06/07. You may NOT add pip-to-P&L conversion, swap/carry, session-gating of orders, or leverage *enforcement* here — only persist the reference values. Leverage-cap **enforcement** (reconciling the per-instrument reference value against regulatory caps via `min(...)`) is owned by FX-05; your `leverage_cap` is a documented reference constant only.
- Authoring or modifying any OTHER FX symbol/pair parser. FX-02 is the single source of truth for FX symbol semantics; FX-06 and FX-07 must consume `is_fx_symbol`/`parse_fx_symbol`/`get_instrument_metadata` rather than re-deriving base/quote/pip. Do not pre-build those consumers here.
- Operator UI surfacing (FX-08), FX data ingestion / data sources / price feeds (FX-01), economic-calendar or COT contract specs (FX-00/FX-01/FX-03). Do not add a price feed or any data source.
- Removing or renaming any existing column on `symbols` or any existing migration. Migrations are append-only; do not edit `0001_baseline.py` or any existing numbered migration.
- Do not change the `asset_class_for_symbol` signature, its `_OVERRIDE`/`ASSET_CLASS_MAP_JSON` env behavior, or the classification of any non-FX symbol.

### Verified anchors (re-confirmed against the tree)
- `engine/data/asset_map.py:63` — `def asset_class_for_symbol(symbol: str) -> str:`. Precedence inside it: `if not s: return "UNKNOWN"` → `_OVERRIDE` (env `ASSET_CLASS_MAP_JSON`) → `_DEFAULT` (line ~24: `{"SPY":"EQUITY","BTC":"CRYPTO","OIL":"COMMODITY"}`) → then the lightweight heuristics block. The FX heuristic is at `engine/data/asset_map.py:81-82` exactly: `if s in ("DXY", "EURUSD", "USDJPY", "GBPUSD"):` then `return "FX"`. This is the hardcoded heuristic to replace. The final fallback is `return "UNKNOWN"`. `_OVERRIDE` and `_DEFAULT` MUST keep precedence over your new FX branch.
- `engine/data/asset_map.py:21-22` — module imports `log_failure` (from `engine.runtime.failure_diagnostics`) and `get_logger` (from `engine.runtime.logging`); `_warn_nonfatal(code, error, **extra)` is defined at line ~32. Reuse it for any non-fatal parse/import warning.
- `engine/data/universe.py:54` — imports `asset_class_for_symbol` inside a `try/except` that calls `_warn_nonfatal(...)` and defines a fallback `def asset_class_for_symbol(symbol)` on failure. `engine/data/universe.py:121` — `def upsert_symbol(con, symbol, *, asset_class=None, status=None, score_delta=0.0, last_seen_event_ts_ms=None, meta=None) -> None`. It computes `ac` at line 136. The new-row path uses `con.execute("INSERT OR IGNORE INTO symbols( symbol, asset_class, status, score, last_seen_event_ts_ms, meta_json, created_ts_ms, updated_ts_ms ) VALUES (?,?,?,?,?,?,?,?)", (...))` at lines 161-179. The existing-row path uses `con.execute("UPDATE symbols SET asset_class=?, status=?, score=?, last_seen_event_ts_ms=COALESCE(?, last_seen_event_ts_ms), meta_json=?, updated_ts_ms=? WHERE symbol=?", (...))` starting at line 213. Both write `meta_json`. These two `con.execute` sites are where instrument metadata must be attached on write.
- `engine/data/universe.py:290` — `def get_universe_snapshot(con, limit=5000)` selects `symbol, asset_class, status, score, last_seen_event_ts_ms, updated_ts_ms, meta_json FROM symbols`. Confirm it still round-trips after your change (it does not select the new columns, so it must remain unaffected).
- `engine/runtime/schema/migrations/0001_baseline.py:169` — `symbols` table DDL (exact): `symbol TEXT PRIMARY KEY, asset_class TEXT NOT NULL DEFAULT 'UNKNOWN', status TEXT NOT NULL DEFAULT 'WATCH', score DOUBLE PRECISION NOT NULL DEFAULT 0.0, last_seen_event_ts_ms BIGINT, last_traded_ts_ms BIGINT, meta_json JSONB, created_ts_ms BIGINT NOT NULL, updated_ts_ms BIGINT NOT NULL`. Existing indexes: `idx_symbols_status_score ON symbols(status, score)` (line 372) and `idx_symbols_updated ON symbols(updated_ts_ms)` (line 373).
- `engine/runtime/schema/migrations/` — highest existing migration is `0069_data_source_provider_accounts.py`; your new migration MUST be `0070_fx_instrument_metadata.py`. Migration module contract (verified in `0069` and `0024_size_policy_columns.py`): module-level `id = <int>`, `description = "..."`, and `def up(conn) -> None:` whose body is one or more `conn.execute("...")` calls. `0024`'s exact additive pattern is `conn.execute("ALTER TABLE IF EXISTS size_policy ADD COLUMN IF NOT EXISTS lookback_days BIGINT NOT NULL DEFAULT 0")` — copy this idempotent shape. The migrator (`engine/runtime/schema/migrator.py`) auto-discovers modules whose leaf filename starts with 4 digits and sorts by `int(getattr(module,"id"))`; `expected_migration_ids(package="engine.runtime.schema.migrations")` (default arg) returns the ordered ids. No manual registration needed.
- `engine/runtime/storage_sqlite.py:45` — `SCHEMA_VERSION = 1`. `engine/runtime/storage_sqlite.py` `_ensure_universe_audit_schema` (around line 2708) creates `symbols` via `_create_table(con, "symbols", ("symbol","score","status","asset_class","meta_json","updated_ts_ms"), (("symbol",),))`. `_create_table` (line 1752) delegates to `_ensure_table`, which CREATEs missing tables and `ALTER TABLE ... ADD COLUMN`s any requested column that is absent (lines ~481-525) — so adding names to the `_create_table` tuple auto-migrates pre-existing test DBs. Column SQL types are name-driven by `_column_type` (line 264). **TRAP (must fix):** `_column_type`'s float branch (lines ~304-323) returns `REAL` only when the lowercased name contains one of `price/value/score/rate/ratio/sharpe/drawdown/return/confidence/equity/cash/pnl/margin/ret/cost/fee/slippage` (or a small set). Consequences for your columns: `pip_size`, `contract_size`, `leverage_cap` match NONE of these → default `TEXT` (WRONG, they must be float); and `pnl_ccy` contains the substring `pnl` → matches → `REAL` (WRONG, it is a currency string). You MUST add explicit name handling in `_column_type` so the three float columns map to `REAL` and the currency/string columns map to `TEXT`.
- `engine/runtime/schema/table_classification.py` — `"symbols"` appears at line 854 INSIDE the `_add(...)` registry call that opens at line 826 and closes with `_r(_BOUNDED_RATIONALE, cleanup="...")` at line 865 (a Regular / bounded-rationale classification block). It is NOT a member of `SOURCE_DECLARED_TABLES`, which is the `frozenset({...})` at lines ~932-943 containing exactly 8 entries (`competition_post_commit_actions`, `feature_store`, `price_ticks`, `timescale_schema_version`, `price_data`, `feature_data`, `model_predictions`, `trade_outcomes`). The conclusion stands: `symbols` is classified Regular and must STAY Regular. The `_add` block is a flat list of table names sharing one rationale — there is no per-table human comment to edit there, so you do NOT need to touch `table_classification.py` at all (adding columns to an existing Regular table does not change its classification).
- `docs/Database_Schema.md:7` — header states the register is "kept in lockstep with `engine/runtime/schema/table_classification.py`". `docs/Database_Schema.md:352` — the `| `symbols` | Regular | ... |` row in the Table Register. Update its rationale text (right-most column) to mention the FX instrument-metadata columns; keep the class `Regular`.
- Consumers of `asset_class_for_symbol` you MUST keep working unchanged (do not edit them; just don't break them): `engine/risk/portfolio_risk_engine.py:37`, `engine/strategy/feature_registry.py:29`, `engine/strategy/predictor.py:20`, `engine/strategy/conformal.py:28`, `engine/strategy/temporal_predictor.py:88`, `engine/strategy/embed_regressor.py:37`, `engine/strategy/model_feature_snapshots.py:43`, `engine/strategy/portfolio_risk_gate.py:14`, `engine/strategy/models/lgbm_ranker.py:25`, `engine/strategy/train_temporal_predictor.py:123` (all `from engine.data.asset_map import asset_class_for_symbol`).
- **Import path for the accessor (resolved):** `engine/runtime/storage.py` does NOT re-export universe helpers — every consumer in the tree imports symbol helpers directly from `engine.data.universe` (e.g. `from engine.data.universe import get_active_symbols`, verified across `engine/data/jobs/*`, `engine/runtime/event_replay.py:816`, etc.). Therefore the accessor lives in `engine.data.universe` and dependents import `from engine.data.universe import get_instrument_metadata`. Do NOT touch `storage.py`.
- Tests live flat under `tests/*.py` (376 files). `tests/conftest.py:13-14` inserts the repo root onto `sys.path`. SQLite-backed storage tests reload the module after setting env; the verified pattern in `tests/test_storage_sqlite_decomposition_contract.py:14-22` is: `monkeypatch.setenv("DB_PATH", str(tmp_path/"x.db"))`, `TS_STORAGE_BACKEND=sqlite`, `TS_TESTING=1`, `TIMESCALE_ENABLED=0`, `SQLITE_LIVENESS_DB_ENABLED=0`, `SQLITE_LIVENESS_QUEUE_ENABLED=0`, then `importlib.reload(importlib.import_module("engine.runtime.storage_sqlite"))`. Schema-classification consistency is enforced by `tests/test_schema_classification.py`; migrator ordering/contract by `tests/test_storage_migrator.py`. `tools/syntax_check_workspace.py` and `tools/git_worktree_triage.py` both exist (verified).

### Canonical FX symbol form (normative — FX-02 owns this)
- The canonical **stored** symbol for an FX spot pair is the **6-letter, uppercase, separator-free concatenation `BASE+QUOTE`** (e.g. `EURUSD`, `USDJPY`, `EURGBP`). `DXY` is stored as-is. This is the form written to the `symbols` table and the key all downstream FX tables (features, risk, costs, UI) must use.
- The parser accepts the friendly variants `EUR/USD`, `EUR_USD`, `EURUSD` (case-insensitive) but **normalizes to the canonical 6-letter form**; `get_instrument_metadata(...)["symbol"]` returns the canonical form. Document in the accessor docstring that later prompts (notably FX-06 broker mapping and FX-07 cost-table lookups, which may use underscore forms like `EUR_USD`) MUST normalize through `parse_fx_symbol(sym).symbol` / `get_instrument_metadata(con, sym)["symbol"]` before keying their tables, so an `EUR_USD`-keyed lookup never silently misses an `EURUSD`-stored position. (You only state and document the rule here; you do not edit those downstream tables.)

### Implementation steps
1. **Create the parser module `engine/data/fx_instrument.py`.** Pure Python, no I/O, no DB, no network. Define:
   - A frozen dataclass `InstrumentMetadata` with fields: `symbol: str` (canonical form), `asset_class: str` (always `"FX"` for parsed pairs/index), `instrument_kind: str` (`"fx_spot"` for pairs, `"fx_index"` for DXY), `base_ccy: str | None`, `quote_ccy: str | None`, `pip_size: float`, `contract_size: float`, `pnl_ccy: str` (currency P&L accrues in — for a standard spot pair this is the quote currency), `leverage_cap: float`, `session_calendar: str` (e.g. `"FX_24x5"`), and `source: str` (`"parser"`). Include a `to_dict()` returning a JSON-serializable dict with sorted keys.
   - `parse_fx_symbol(symbol: str) -> InstrumentMetadata | None`. Normalize input: `str(symbol or "").upper().strip()`, then strip a single optional separator so `EUR/USD`, `EUR_USD`, `EURUSD` all collapse to `EURUSD`. Recognize a 6-letter pair as `base=first 3`, `quote=last 3` **only** when both halves are in a curated `KNOWN_CCY` set. Define a conservative, explicit, documented set of majors + common crosses: at minimum `USD, EUR, JPY, GBP, CHF, AUD, NZD, CAD` plus a small, confident set of common ones (e.g. `SEK, NOK, MXN`); keep it explicit so arbitrary 6-letter tickers like `GOOGLE` are rejected. Return `None` for anything not a recognized pair. Handle `DXY` as a special non-pair FX index (`instrument_kind="fx_index"`, `base_ccy=None`, `quote_ccy="USD"`, `pnl_ccy="USD"`, `symbol="DXY"`).
   - Pip-size rule: `0.01` when the quote currency is `JPY` (covers all JPY-quoted crosses), else `0.0001`. `contract_size`: standard FX lot `100000.0` units of base. `pnl_ccy`: the quote currency for spot pairs; `"USD"` for DXY. `leverage_cap`: a single conservative documented constant (e.g. `20.0`) — reference metadata only, NOT enforced here; add a comment that FX-05 owns enforcement via `min(reference, regulatory)`. `session_calendar`: `"FX_24x5"` for spot pairs (Sun open / Fri close mechanics are FX-06's, not here).
   - `is_fx_symbol(symbol: str) -> bool` returning `True` iff `parse_fx_symbol(...)` is not `None`. Verify `is_fx_symbol("DXY") is True`.
   - Use the module-level logger pattern from `engine/data/asset_map.py` (`get_logger` + a local `_warn_nonfatal` helper) for any unexpected parse failure; **never raise to callers** — return `None` on unparseable input.
2. **Rewire `engine/data/asset_map.py`.** Add an import of `is_fx_symbol` from `engine.data.fx_instrument`, guarded by the same `try/except` + `_warn_nonfatal` pattern the module already uses (so a parser-import failure degrades to a no-op `is_fx_symbol` that returns `False`, never crashing classification). In `asset_class_for_symbol`, after the `_OVERRIDE` and `_DEFAULT` checks (which keep precedence) and within the heuristics block, replace the hardcoded `if s in ("DXY", "EURUSD", "USDJPY", "GBPUSD"):` / `return "FX"` (lines 81-82) with `if is_fx_symbol(s): return "FX"`. `DXY` stays FX (covered by the parser's `fx_index` branch). Do not change any other branch, the signature, the env behavior, or the `UNKNOWN` fallback.
3. **Add the read accessor in `engine/data/universe.py`.** Implement `get_instrument_metadata(con, symbol) -> dict | None` (universe.py already owns symbol-table reads). It must: SELECT the persisted instrument columns for the (canonicalized) symbol from `symbols`; if the row exists and has persisted FX metadata, return it as a dict; if the symbol exists but has no persisted FX metadata, fall back to `parse_fx_symbol(symbol).to_dict()` (parser is always the source of truth; the table is a cache); if the symbol is not present at all, still derive from the parser (so callers can ask about an un-upserted pair); return `None` for non-FX symbols. Be defensive: if the instrument columns do not yet exist on an older DB, catch the missing-column error and fall back to the parser via `_warn_nonfatal`. Give it a docstring stating: (a) it is the single source of truth FX-03/04/05/06/07 MUST consume; (b) the returned `symbol` is the canonical 6-letter form and callers should re-key their tables through it; (c) the import path is `from engine.data.universe import get_instrument_metadata`. Do NOT add it to `storage.py` or to `_REQUIRED_BACKEND_SYMBOLS`.
4. **Persist metadata on upsert.** In `upsert_symbol`, after computing `ac` (line 136), call `parse_fx_symbol(sym)` once; if it returns metadata, populate the new instrument columns on BOTH `con.execute` sites — the `INSERT OR IGNORE` at lines 161-179 and the `UPDATE` starting at line 213 (extend the column list and the params tuple). Keep writing `meta_json` exactly as today (do NOT move metadata into `meta_json`; use dedicated columns so the data is queryable). Be defensive: wrap the extended-column writes so a missing-column `OperationalError` (older DB without the new columns) degrades to the existing legacy INSERT/UPDATE via the `_warn_nonfatal` path rather than failing the upsert. Re-derive the column list once, not per call. Non-FX symbols leave the new columns NULL.
5. **Postgres migration `engine/runtime/schema/migrations/0070_fx_instrument_metadata.py`.** Module-level `id = 70`, `description = "fx instrument metadata columns"`, `def up(conn) -> None:`. Add nine nullable columns to `symbols`, each via its own idempotent `conn.execute("ALTER TABLE IF EXISTS symbols ADD COLUMN IF NOT EXISTS <col> <type>")` (no `NOT NULL`, no `DEFAULT`): `instrument_kind TEXT`, `base_ccy TEXT`, `quote_ccy TEXT`, `pip_size DOUBLE PRECISION`, `contract_size DOUBLE PRECISION`, `pnl_ccy TEXT`, `leverage_cap DOUBLE PRECISION`, `session_calendar TEXT`, `instrument_meta_source TEXT`. Optionally add one minimal partial index `CREATE INDEX IF NOT EXISTS idx_symbols_quote_ccy ON symbols(quote_ccy)` only if it aids FX lookups — keep it minimal or omit it. Do NOT backfill rows in the migration; add a comment stating backfill is lazy/on-write (handled by `upsert_symbol` and the parser-fallback accessor). Do not edit any prior migration.
6. **SQLite test backend.** In `engine/runtime/storage_sqlite.py`: (a) extend the `_create_table(con, "symbols", (...))` column tuple in `_ensure_universe_audit_schema` (~line 2708) to include the same nine columns (`_ensure_table` will `ALTER TABLE ... ADD COLUMN` them onto pre-existing test DBs automatically). (b) Fix `_column_type` (line 264) so the new names get correct SQLite affinity: `pip_size`, `contract_size`, `leverage_cap` must return `REAL`; `instrument_kind`, `base_ccy`, `quote_ccy`, `pnl_ccy`, `session_calendar`, `instrument_meta_source` must return `TEXT`. **Critical:** `pnl_ccy` currently matches the `"pnl"` float-substring rule and would wrongly become `REAL` — add an explicit `TEXT` mapping (e.g. a name set checked BEFORE the float-substring block, or an `endswith("_ccy")` rule) that takes precedence; and add the three float names to an explicit `REAL` set since they match no existing float keyword. Do NOT bump `SCHEMA_VERSION` (additive `_create_table` columns do not require it; verify and leave at `1`).
7. **Docs.** Update the `symbols` row rationale in `docs/Database_Schema.md:352` to mention the FX instrument-metadata columns, and add a short note (near the symbols entry) listing the nine columns and stating the parser is the source of truth and the canonical stored symbol form is 6-letter concatenated. Do NOT modify `engine/runtime/schema/table_classification.py` (the symbols entry is a flat name in an `_add` block with no per-table comment, and its class stays `Regular`). Put the FX-accessor contract (single source of truth; import path; canonical-symbol rule) in the `get_instrument_metadata` docstring so dependents have a code-level reference.
8. **Sandbox-honesty note.** This task needs no network and no live credentials. The sandbox has no Postgres; run the SQLite-backed equivalents and explicitly mark the Postgres-only migration *apply* as "not executed in sandbox — covered by the `0070` module import + the migrator-contract test" rather than claiming it passed. Do not fabricate a Postgres apply result.

### Tests to add
- `tests/test_fx_instrument_parser.py` — pure-parser behavior: `parse_fx_symbol("EURUSD")` → base `EUR`/quote `USD`, `pip_size 0.0001`, `contract_size 100000.0`, `pnl_ccy "USD"`, canonical `symbol "EURUSD"`; `parse_fx_symbol("USDJPY")` → `pip_size 0.01`, `pnl_ccy "JPY"`; the variants `EUR/USD`, `eur_usd`, `EURUSD` parse to the SAME metadata (canonical `symbol == "EURUSD"`); `parse_fx_symbol("DXY")` → `instrument_kind "fx_index"`, `base_ccy None`, `quote_ccy "USD"`; non-pairs (`SPY`, `BTC`, `GOOGLE`, `""`, `None`) → `None`; `is_fx_symbol` agrees with `parse_fx_symbol` on every case including `is_fx_symbol("DXY") is True`.
- `tests/test_fx_asset_class_derivation.py` — `asset_class_for_symbol` returns `"FX"` for `EURUSD`/`USDJPY`/`GBPUSD`/`DXY` AND for at least one pair NOT in the old hardcoded tuple (e.g. `EURGBP`, `AUDUSD`); returns the unchanged class for `SPY`(EQUITY)/`BTC`(CRYPTO)/`OIL`(COMMODITY)/`TLT`(RATES)/`ZZZ`(UNKNOWN); an `ASSET_CLASS_MAP_JSON` override for a symbol still wins over the FX branch; the function signature/return type is unchanged (returns `str`).
- `tests/test_fx_instrument_metadata_storage.py` — SQLite-backed using the reload pattern from `tests/test_storage_sqlite_decomposition_contract.py` (set `DB_PATH`/`TS_STORAGE_BACKEND=sqlite`/`TS_TESTING=1`/`TIMESCALE_ENABLED=0` and the two liveness flags, reload). After `upsert_symbol(con, "EURUSD")`: the `symbols` row has the instrument columns populated; **assert the float columns round-trip as numbers, not strings** — e.g. `isinstance(row_value_for_pip_size, float)` and `pip_size == 0.0001`, and assert `pnl_ccy == "EUR".../"USD"` is a `str` (this is the regression guard for the `_column_type` trap — a TEXT pip_size or a REAL pnl_ccy must fail this test). `get_instrument_metadata(con, "EURUSD")` returns the full dict with canonical `symbol`; `get_instrument_metadata(con, "SPY")` returns `None`; `get_universe_snapshot(con)` still round-trips the row; an `upsert_symbol(con, "SPY")` leaves the instrument columns NULL and the accessor returns `None`.
- `tests/test_fx_instrument_migration.py` — import `engine.runtime.schema.migrations.0070_fx_instrument_metadata` (use `importlib.import_module`, since the module name starts with a digit) and assert `id == 70`, non-empty `description`, callable `up`; assert `expected_migration_ids()` (from `engine.runtime.schema.migrator`, called with no args — the `package` default applies) includes `70` and is strictly increasing and contiguous through `70`; assert the module source text (read the file) references all nine new column names. Do NOT require a live Postgres connection.
- Confirm `tests/test_schema_classification.py` still passes (symbols still `Regular`, no undeclared table introduced) and `tests/test_storage_migrator.py` still passes (70 discovered, contiguous).

### Definition of Done
- [ ] `engine/data/fx_instrument.py` exists with `parse_fx_symbol`, `is_fx_symbol`, and a frozen `InstrumentMetadata`; it is pure (no DB/network/I/O) and never raises on bad input (returns `None`).
- [ ] `engine/data/asset_map.py` derives FX from `is_fx_symbol` (the hardcoded 4-tuple at lines 81-82 is removed); `DXY` still classifies as FX; all non-FX classifications, the signature, and the env behavior are unchanged; a parser-import failure degrades gracefully via the existing `_warn_nonfatal` guard.
- [ ] `get_instrument_metadata(con, symbol)` exists in `engine.data.universe`, is documented as the single FX source of truth and as fixing the canonical 6-letter stored symbol form, returns full metadata for FX symbols (table cache OR parser fallback) and `None` for non-FX. It is imported as `from engine.data.universe import get_instrument_metadata`; `storage.py` is untouched.
- [ ] `upsert_symbol` populates the nine new instrument columns on both the INSERT (161-179) and UPDATE (213) paths, degrades safely if the columns are absent, and still writes `meta_json`; non-FX symbols leave the columns NULL.
- [ ] Migration `0070_fx_instrument_metadata.py` adds the nine nullable columns idempotently, does not edit any prior migration, and is discovered by the migrator (`expected_migration_ids()` includes 70 contiguously).
- [ ] SQLite test backend `symbols` gains the same nine columns with correct affinity — `pip_size`/`contract_size`/`leverage_cap` are `REAL`, `pnl_ccy` (and the other string columns) are `TEXT` (the `_column_type` `pnl`-substring trap is explicitly fixed); pre-existing test DBs auto-add the columns; `SCHEMA_VERSION` stays `1`.
- [ ] `docs/Database_Schema.md` symbols row + note are updated; `table_classification.py` is unchanged and symbols stays `Regular`.
- [ ] The FX-derivation enforcement lives in runtime code (`asset_map.py`, `universe.py`, the migration, the sqlite backend), not only in tests or docs.
- [ ] No broker, sizing, risk, execution, feature, label, or UI behavior changed; no new model order authority; no secrets logged; no second FX pair parser introduced.
- [ ] All four new test files pass (including the float-vs-text round-trip assertion); pre-existing storage/schema/classification/migrator tests still pass.

### Validation commands
Run from `/home/david/gitsandbox/system/system`. For storage tests use the SQLite backend: `export TS_STORAGE_BACKEND=sqlite TS_TESTING=1 TIMESCALE_ENABLED=0`.
- `python -c "import engine.data.fx_instrument, engine.data.asset_map, engine.data.universe"` → exit 0 (imports clean).
- `python -c "from engine.data.fx_instrument import parse_fx_symbol as p, is_fx_symbol as f; m=p('eur_usd'); assert m and m.symbol=='EURUSD' and m.base_ccy=='EUR' and m.quote_ccy=='USD' and m.pip_size==0.0001 and m.pnl_ccy=='USD'; assert p('USDJPY').pip_size==0.01 and p('USDJPY').pnl_ccy=='JPY'; assert p('GOOGLE') is None and p('') is None; assert f('DXY') and not f('SPY')"` → exit 0.
- `python -c "import importlib; m=importlib.import_module('engine.runtime.schema.migrations.0070_fx_instrument_metadata'); assert m.id==70 and callable(m.up) and m.description"` → exit 0.
- `python -c "from engine.runtime.schema.migrator import expected_migration_ids as e; ids=e(); assert 70 in ids and list(ids)==sorted(ids), ids"` → exit 0.
- `python -m pytest tests/test_fx_instrument_parser.py tests/test_fx_asset_class_derivation.py tests/test_fx_instrument_metadata_storage.py tests/test_fx_instrument_migration.py -q` → exit 0.
- `python -m pytest tests/test_schema_classification.py tests/test_storage_migrator.py tests/test_storage_sqlite_decomposition_contract.py -q` → exit 0 (regression guard).
- `python tools/syntax_check_workspace.py` → exit 0.
- `python tools/git_worktree_triage.py` → run and confirm it reports no unexpected duplication of your new files (informational; record the output).
- `git status --short --untracked-files=all` → human review (this command returns 0 regardless; the real check is that ONLY the intended files appear): `engine/data/fx_instrument.py`, `engine/data/asset_map.py`, `engine/data/universe.py`, `engine/runtime/storage_sqlite.py`, `engine/runtime/schema/migrations/0070_fx_instrument_metadata.py`, `docs/Database_Schema.md`, and the four new test files. `engine/runtime/schema/table_classification.py` and `engine/runtime/storage.py` must NOT appear.
- Postgres migrate (`python -m engine.runtime.schema.migrator` or the project migrate entrypoint) → only if a real Postgres is reachable; otherwise explicitly mark it "not executed in sandbox — covered by the 0070 module-import check and the migrator-contract test", do not claim success.

### Self-audit & NO-GO
After implementation, audit your own work and report:
- The exact list of files changed with a one-line reason each; confirm no out-of-scope files (risk/execution/feature/UI/broker, prior migrations, `storage.py`, `table_classification.py`) were modified.
- Confirm the FX-derivation enforcement is in runtime code (`asset_map.py`, `universe.py`, the migration, the sqlite backend), not only tests/docs; paste the before/after of ONLY the changed `asset_map.py` branch (lines 81-82) and the specific `universe.py` lines that populate/read the new columns.
- Confirm `asset_class_for_symbol` is unchanged for every non-FX symbol and that its signature/return contract is identical.
- Confirm the `_column_type` fix: paste the exact added/changed lines and confirm `pip_size`/`contract_size`/`leverage_cap` → `REAL` and `pnl_ccy` → `TEXT`, and cite the test assertion that pins float-vs-text round-trip.
- Confirm `get_instrument_metadata` is the documented single source of truth, imported from `engine.data.universe` (not `storage.py`), and that no consumer behavior (sizing/risk/execution/features) was altered.
- Confirm the canonical stored symbol form is stated and that the accessor docstring instructs downstream prompts to normalize through it.
- Confirm migrations are append-only (no edits to `0001`–`0069`) and `0070` is idempotent (`ADD COLUMN IF NOT EXISTS`).
- State which validation commands actually ran and their exit codes; for any that could not run in-sandbox (live Postgres migrate), say so explicitly and how the gap is otherwise covered — do not fake success.
- If any Definition-of-Done item is not fully implemented, say **NO-GO** and list the precise remaining work; otherwise say **GO** with the evidence above.

---

## FX-03 — FX feature groups + train/serve parity

**Mission:** Add FX-driver feature groups (rate differential, carry, real-yield spread, dollar/DXY beta, cross-pair correlation, COT positioning, trend/time-series momentum, central-bank event-window flags) to `engine/strategy/feature_registry.py` with explicit `feature_ids` that round-trip through the persisted feature schema, and gate the equity-only groups OFF for `asset_class == "FX"` so FX models are never fed structural-zero noise — all proven by train/serve parity tests. The FX feature *transforms* (computing per-pair rate differentials, carry, DXY beta, cross-pair correlation, and time-series momentum from the raw series/price history FX-01 ingests) are owned by THIS prompt: implement them inside the loaders, not as a deferred "if an FX-01 resolver exists" stub.

**Prerequisites (merge order):**
- **FX-01 (FX data source + ingestion) — HARD prerequisite, must be merged BEFORE this prompt.** FX-01 ships the raw FRED series (real-yield / DXY / foreign-rate rows), OANDA price bars, and the authoritative CFTC COT contract specs. FX-03 EDITS `engine/data/cftc_cot.py` and CONSUMES FX-01's raw rows, so it must merge after FX-01 to avoid a dual-owner edit and duplicate COT specs. In THIS sandbox FX-01's ingested data will NOT be present and live network/credentials are unavailable; therefore every FX feature *loader* you write MUST be resilient to missing data: open data via a guarded `connect()` exactly like the existing `_load_macro` pattern, return a deterministic structural value (`0.0`) on any failure, and NEVER raise. The registry schema (the `feature_ids` and their order) must be fully defined and testable regardless of whether FX-01 data exists. Mark "live FX feature values verified end-to-end" as a GAP, not done.
  - **Ownership boundary with FX-01 (read carefully):** FX-01 OWNS the CFTC COT `CotContractSpec` rows for FX majors (it adds `6E`/`6J`/`6B`/etc. to `DEFAULT_COT_CONTRACT_SPECS`). FX-03 is **consume-only** for contract specs: do NOT add new `CotContractSpec` rows. FX-03 owns ONLY (a) surfacing the `fx.cot_*` feature ids, and (b) ensuring an `EURUSD` → `6E` *symbol mapping* exists — and even that is gated "only if FX-01 did not already add it" (see step 8). This eliminates the merge collision both prompts would otherwise cause on `engine/data/cftc_cot.py`.
- **FX-02 (First-class FX instrument model) — HARD prerequisite, must be merged BEFORE this prompt.** It provides `asset_class == "FX"` classification for FX pairs and per-instrument metadata (base/quote currency, pip convention) and is the single source of truth for FX symbol semantics and the canonical stored symbol form (concatenated 6-letter, e.g. `EURUSD`). Prefer FX-02's accessor for any symbol parsing you need. If FX-02 is NOT present in the tree you are working in, you MUST detect this and degrade gracefully: rely solely on the existing `engine.data.asset_map.asset_class_for_symbol`, which already returns `"FX"` for `DXY`, `EURUSD`, `USDJPY`, `GBPUSD` (verified — `engine/data/asset_map.py:81-82`). Do not invent an FX-02 metadata API that does not exist; if you need richer metadata than `asset_class_for_symbol` exposes, add a thin local helper, mark the dependency gap explicitly in code comments and your final report, and NO-GO that sub-item rather than fabricate it.

**Global constraints:**
- Target repo: `/home/david/gitsandbox/system/system`. All paths below are relative to it. This is the only repo.
- Never print, log, echo, commit, or test with real secret values. Use generated canary strings in tests and assert they never appear in logs or outputs.
- Never call live broker order/cancel/replace/flatten paths. OANDA/IBKR/Alpaca access stays read-only or simulated. FX trading graduates through shadow/paper and may not bypass governance. (This workstream touches features only — do not add any execution code.)
- Build on existing architecture; never replace it. Preserve the model-vs-runtime contract (models propose intent, runtime owns safety gates), train/serve feature parity via `engine/strategy/feature_registry.py`, and the champion/challenger promotion path.
- `engine/runtime/storage.py` schema changes are high blast radius: any schema/migration change needs a migration plus tests. This workstream MUST NOT need storage schema changes (features resolve to in-memory dicts and persist through the existing feature-snapshot schema). If you believe a storage change is required, STOP and NO-GO with justification instead of editing storage.
- Persisted-schema safety: the FX ids are NEW ids appended via NEW group constants; do NOT insert anything into `BASE_FEATURE_IDS` (`:95`) or otherwise shift the insertion-order positions of existing ids — already-trained models depend on that order. The determinism test (`tests/test_feature_registry_determinism.py`) guards this; confirm it would actually fail on an order shift (run it before and after your change; it must stay green because you only appended new ids to new groups, never reordered existing ones).
- "Profitable" is never asserted; it is proven downstream through existing statistical/backtest gates (out of scope here).

### In scope
- Add new FX feature-group constants (explicit `feature_id` lists) and register them in `FEATURE_GROUPS` / `FEATURE_GROUP_METADATA` in `engine/strategy/feature_registry.py`.
- Add the FX `feature_id` prefix (`"fx."`) to `_SNAPSHOT_PREFIXES` so they round-trip as symbol-snapshot features, and extend `feature_set_tag_from_ids` so FX groups produce a stable, distinct tag fragment.
- Add per-`feature_id` resolution branches inside `compute_feature_snapshot` via guarded loaders that return `0.0` on missing data, mirroring the existing `_load_macro` pattern. The loaders for rate/carry/DXY/cross/momentum MUST COMPUTE the per-pair transform from the raw series/price history FX-01 ingests (see step 5) — they are the owner of that math; they are not thin pass-throughs to a non-existent FX-01 resolver.
- Add an asset-class-aware filtering layer so that for `asset_class == "FX"` the equity-only groups (options/IV, social, social-regime, and — when their opt-in env flag is enabled — Form-4 insider, 13F, congressional, short interest, government, fundamentals, NLP filings/transcripts/finbert) are excluded from the resolved/expected columns, while FX groups are included; and for equities the FX groups are excluded and the equity groups remain present. This MUST be implemented in runtime resolution code, not only asserted in tests.
- Ensure the `fx.cot_*` ids resolve via the existing CFTC COT machinery, and ensure an `EURUSD` → `6E` symbol mapping exists IF FX-01 did not already add it (consume-only; no new contract specs). Keep this additive and idempotent.
- Update developer-facing docs to describe FX feature groups and the asset-class gating contract.

### Out of scope (do not touch)
- **CFTC COT contract-spec authoring** (`CotContractSpec` rows / market-name strings for FX majors) — that is FX-01. FX-03 only surfaces `fx.cot_*` ids and may add the `EURUSD`→`6E` symbol mapping gated on absence (step 8).
- **Economic-calendar / central-bank event ingestion** — no prompt currently ingests FOMC/ECB/BoJ/NFP/CPI event timestamps. The `fx.event_*` feature group is therefore a KNOWN PERMANENT STUB in this workstream: define the ids and a guarded loader that returns `0.0`, document them as a stub awaiting an upstream calendar feed, and call this out in your report. Do NOT add a calendar ingestion source here (that would belong to FX-01 scope).
- Model code, prediction routing, FX regime, or label changes (FX-04). Do not modify `engine/strategy/predictor.py` model adapters, routing, or label logic. The ONLY predictor-adjacent change permitted is passing a symbol/asset-class through to the feature-id resolution call if and only if it is required to thread asset-class gating; prefer doing the gating inside `feature_registry.py` so `predictor.py` needs no edit.
- Portfolio sizing / currency-aware risk (FX-05). Do not touch `engine/risk/*` or `engine/strategy/portfolio*`.
- Execution / broker routing / sessions (FX-06). Do not touch `engine/execution/*`.
- Backtest realism / governance / profitability evidence (FX-07).
- Operator UI (FX-08). Do not touch `dashboard_server.py` or UI assets.
- `engine/runtime/storage.py` and any DB migration. No storage schema changes.

### Verified anchors (re-confirmed against the tree at `/home/david/gitsandbox/system/system`)
- `engine/strategy/feature_registry.py:72` — `def _env_bool(name, default=False)` (use this for the new flag).
- `engine/strategy/feature_registry.py:95` — `BASE_FEATURE_IDS` (insertion-order schema starts here; do NOT modify).
- `engine/strategy/feature_registry.py:328` — `PRICE_FEATURE_IDS = [` (the block begins here; place the new FX constants AFTER this block — NOT at ~356, which was a stale anchor).
- Equity-only opt-in toggles (corrected — they are NOT in a `294–502` range):
  - `:27` imports `USE_CONGRESSIONAL_TRADE_DATA`; `:61` `USE_SOCIAL_FEATURES`; `:62` `USE_SOCIAL_REGIME`; `:64` `USE_OPTIONS_FEATURES`; `:68` `USE_NLP_FEATURES`; `:84` `USE_INSIDER_FEATURES`; `:85` `USE_SHORT_FEATURES`; `:90` `USE_13F_FEATURES`; `:91` `USE_GOV_FEATURES`; `:92` `USE_FUNDAMENTALS_PIT_FEATURES`.
  - The `*_FEATURE_IDS` constants gated by those flags are defined at `:284–:293` (e.g. `INSIDER_FEATURE_IDS = list(...) if USE_INSIDER_FEATURES else []`). When the flag is OFF the list is EMPTY (see "Default-registration reality" below).
- `engine/strategy/feature_registry.py:456` — `FEATURE_GROUPS = { ... }` (unconditional members); `:481–:502` — conditional group registrations (`FEATURE_GROUPS["insider"] = ...` etc., only when the gated `*_FEATURE_IDS` are non-empty). Note the options group appears under BOTH `"options_symbol"` and `"options"` keys (`:464`/`:465`).
- `engine/strategy/feature_registry.py:506` — `FEATURE_STAGES` dict comprehension assigns `live` to every group member (shadow overrides applied at `:513/:515/:517` for chronos/graph/structured-doc only); new FX groups will therefore be `live` by virtue of registration — do not add them to the shadow override loops.
- `engine/strategy/feature_registry.py:519` — `FEATURE_GROUP_METADATA` is auto-derived from `FEATURE_GROUPS` (each entry: `{"feature_ids": [...], "schema_version": <legacy unless name contains "_v">}`). New FX groups will default `schema_version="legacy"` — acceptable; do NOT add a `_vN` suffix.
- `engine/strategy/feature_registry.py:606` — `_SNAPSHOT_PREFIXES` tuple; add `"fx."` here so `_feature_uses_symbol_snapshot` (`:876`, returns `text.startswith(_SNAPSHOT_PREFIXES)` at `:880`) routes FX ids through the symbol-snapshot path.
- `engine/strategy/feature_registry.py:1205` — `def resolve_feature_ids(feature_ids=None, *, model_name=None, model_spec=None, fallback_to_default=True)`. It iterates `requested` in order and keeps an id only if it is in `_registered_feature_allowlist(include_shadow=True)` or is a registered symbolic feature — so unregistered ids are already dropped. `:1252` — `def expected_columns(...)` simply delegates to `resolve_feature_ids`. NEITHER currently accepts `symbol`/`asset_class`; add `asset_class` as a NEW optional keyword (default `None` = no gating) to BOTH so existing call sites keep working.
- `engine/strategy/feature_registry.py:1268` — `def feature_set_tag_from_ids(feature_ids)` builds a `parts` list and returns `"+".join(parts)` (`:1331`); insert an `fx` fragment before the join.
- `engine/strategy/feature_registry.py:1402` — `_load_macro(ts_ms)`: guarded `from engine.runtime.storage import connect; con = connect()` in try/finally, `return {}` on any failure. This is the exact pattern your FX loaders (including the COT loader) must follow. `:1464` — `_load_options(symbol, ts_ms)` (second guarded-loader template).
- `engine/strategy/feature_registry.py:1906` — `def compute_feature_snapshot(*, event, symbol, feature_ids=None)`; `:1907` is `ids = resolve_feature_ids(feature_ids)` — the call to thread `asset_class` through. `:1922` — the per-`fid` resolution loop where the new FX branches go.
- `engine/strategy/feature_registry.py:2061` — `def build_feature_snapshot(*, event, symbol, feature_ids=None)`; `:2062` is `ids = resolve_feature_ids(feature_ids)` — the second call to thread `asset_class` through.
- `engine/strategy/feature_registry.py:1358` — `asset_class = asset_class_for_symbol(symbol)` is already called inside `_build_context` (NOT inside `compute_feature_snapshot`); `asset_class_for_symbol` is imported at `:29`. You will add a fresh `asset_class_for_symbol(symbol)` call at the two resolve sites above.
- `engine/data/asset_map.py:63` — `def asset_class_for_symbol(symbol)`; `:81-82` returns `"FX"` for `DXY`, `EURUSD`, `USDJPY`, `GBPUSD` (verified).
- `engine/data/cftc_cot.py:34` — `COT_FEATURE_IDS = ["cot_commercial_net_pctile_3y", "cot_noncomm_net_z", "cot_noncomm_extreme_flag", "cot_open_interest_z"]`.
- `engine/data/cftc_cot.py:54` — `@dataclass(frozen=True)`; `:55` — `class CotContractSpec:` with fields `(contract_key, report_type, market_name_contains, symbols, topic="", weight=1.0)`.
- `engine/data/cftc_cot.py:68` — `DEFAULT_COT_CONTRACT_SPECS`; `:74` — `CotContractSpec("6E", "legacy", "EURO FX", ("FXE", "6E"), "fx")` (owned by FX-01; do NOT add rows).
- `engine/data/cftc_cot.py:289` — `def seed_default_cot_mappings(con)` seeds `cot_contract_symbol_map` from `load_cot_contract_specs()`; `:314` — `def cot_target_contracts_for_symbol(con, symbol)` reads that map; `:716` — `def resolve_cot_features(con, *, symbol, ts_ms)` returns structural zeros (the 4 `cot_*` keys) when no mapping/data (safe for sandbox). `fetch_cot_records` (`:363`) is the LIVE HTTP path — do NOT call it.
- Tests live flat under `tests/`. Parity/determinism test: `tests/test_feature_registry_determinism.py` (uses `expected_columns()`, `PYTHONHASHSEED` subprocess pattern, `_write_test_secrets`). COT test: `tests/test_cftc_cot_features.py` (in-memory sqlite via `sqlite3.connect(":memory:")` + `ensure_cot_tables` + `seed_default_cot_mappings`). Pytest config: `pyproject.toml:42` (`[tool.pytest.ini_options]`), `testpaths=["tests"]` (`:43`), `pythonpath=["."]` (`:44`), `addopts="-q --strict-markers --strict-config --durations=20"` (`:46`).
- Validators: `tools/validate_repo.py`, `tools/system_audit.py` (`python tools/system_audit.py`), `tools/git_worktree_triage.py` (argparse `main`, JSON report) all exist.

### Default-registration reality (READ THIS — it changes which ids your tests must use)
With all opt-in env flags at their defaults (OFF, the sandbox state), `expected_columns()` returns ~111 ids and ONLY these equity-only prefixes are actually present in the default serving schema (verified by running `expected_columns()`):
- `options_symbol.` — 8 ids (e.g. `options_symbol.iv_rank`) — **present by default** (`OPTIONS_FEATURE_IDS` is non-empty regardless of the env flag).
- `social.` — 8 ids (e.g. `social.mention_rate_z`) — **present by default**.
- `social_regime.` — 8 ids (e.g. `social_regime.mania_score`) — **present by default**.

The `insider`, `inst_13f`, `congressional`, `short`, `gov`, `fundamentals`, and `nlp.*` groups are EMPTY/unregistered by default, so `resolve_feature_ids` already drops their ids whether or not asset-class gating runs. Consequences you MUST honor:
- Your FX-gating tests and validation one-liners MUST use **default-registered** equity-only representatives (`options_symbol.iv_rank`, `social.mention_rate_z`, `social_regime.mania_score`) to prove the gate actually fires. Using `insider_*`/`13f_*`/etc. would make the assertion pass trivially (the id is never registered) and prove nothing.
- Still include the opt-in groups in `_is_equity_only_feature` so the gate is correct when an operator enables them. Add ONE test that flips `USE_INSIDER_FEATURES` (or similar) on via env in a subprocess/monkeypatch and asserts the now-registered `insider_*` id is gated OFF for FX — and mark this as the "opt-in groups also gated" coverage.

### Implementation steps
1. **Define FX feature-id constants** near the existing group constants, placed AFTER the `PRICE_FEATURE_IDS` block (`engine/strategy/feature_registry.py:328`). Use a single shared namespace prefix `fx.` so one entry in `_SNAPSHOT_PREFIXES` covers all of them. Create explicit, ordered lists (names are concrete and final — do not rename casually, the schema is persisted):
   - `FX_RATE_FEATURE_IDS = ["fx.rate_diff_2y", "fx.rate_diff_10y", "fx.rate_diff_2y_mom_20d", "fx.rate_diff_10y_mom_20d", "fx.real_yield_spread_2y", "fx.real_yield_spread_10y"]`
   - `FX_CARRY_FEATURE_IDS = ["fx.carry_annualized", "fx.carry_z_60d", "fx.carry_to_vol"]`
   - `FX_DXY_FEATURE_IDS = ["fx.dxy_level_z", "fx.dxy_ret_5d", "fx.dollar_beta_60d", "fx.dollar_corr_20d"]`
   - `FX_CROSS_FEATURE_IDS = ["fx.cross_corr_eurusd_20d", "fx.cross_corr_usdjpy_20d", "fx.cross_corr_basket_20d", "fx.cross_beta_basket_60d"]`
   - `FX_COT_FEATURE_IDS = ["fx.cot_commercial_net_pctile_3y", "fx.cot_noncomm_net_z", "fx.cot_noncomm_extreme_flag", "fx.cot_open_interest_z"]` (FX-specific COT view; resolves via `engine.data.cftc_cot.resolve_cot_features`).
   - `FX_MOMENTUM_FEATURE_IDS = ["fx.tsmom_20d", "fx.tsmom_60d", "fx.tsmom_120d", "fx.trend_strength_60d", "fx.breakout_flag_55d"]`
   - `FX_EVENT_FEATURE_IDS = ["fx.event_fomc_window", "fx.event_ecb_window", "fx.event_boj_window", "fx.event_nfp_window", "fx.event_cpi_window", "fx.hours_to_next_cb_event"]` — **PERMANENT STUB group** (no event-calendar feed owner exists); always resolves to `0.0` until an upstream calendar feed is added. Add a code comment saying so.
   - `FX_FEATURE_IDS = list(FX_RATE_FEATURE_IDS) + list(FX_CARRY_FEATURE_IDS) + list(FX_DXY_FEATURE_IDS) + list(FX_CROSS_FEATURE_IDS) + list(FX_COT_FEATURE_IDS) + list(FX_MOMENTUM_FEATURE_IDS) + list(FX_EVENT_FEATURE_IDS)`
   Gate availability behind an env flag mirroring existing style: `USE_FX_FEATURES = _env_bool("USE_FX_FEATURES", False)` (use `_env_bool` at `:72`). When the flag is off the constants still exist (so the schema is stable and testable) but the FX groups are NOT auto-added to the default serving schema for non-FX symbols.
2. **Register the groups.** Add to `FEATURE_GROUPS` (`:456`): `"fx_rate"`, `"fx_carry"`, `"fx_dxy"`, `"fx_cross"`, `"fx_cot"`, `"fx_momentum"`, `"fx_event"`, and an aggregate `"fx"` → `list(FX_FEATURE_IDS)`. `FEATURE_GROUP_METADATA` is auto-derived at `:519`; the new groups will get `schema_version="legacy"` — acceptable. They will be `live` stage via the `FEATURE_STAGES` comprehension at `:506`; do NOT add them to the shadow override loops (`:513–:517`).
3. **Snapshot routing.** Add `"fx."` to the `_SNAPSHOT_PREFIXES` tuple (`:606`) so `_feature_uses_symbol_snapshot` returns `True` for FX feature ids and they persist through `model_feature_snapshots`.
4. **Feature-set tag.** In `feature_set_tag_from_ids` (`:1268`), before the final `return "+".join(parts)` (`:1331`), add: `if any(fid.startswith("fx.") for fid in ids): parts.append("fx")`. This keeps the FX feature set tag distinct from `symbol_snapshot`, so FX models do not collide with equity snapshot caches.
5. **Resolvers (guarded, never raise — and they OWN the transform math).** In the `compute_feature_snapshot` loop (`:1922`) add branches that resolve `fx.*` ids. Implement small module-private loaders following the exact `_load_macro` guarded-import + guarded `connect()` + try/finally + `_warn_nonfatal` + `return {}`-on-failure pattern (`:1402`). Each returns `Dict[str, float]`. Lazy-init each loader dict like the existing `if macro is None: macro = _load_macro(...)` pattern so a single load serves all ids in its group. Each branch sets `snap[fid] = float((loaded or {}).get(fid, 0.0) or 0.0)`.
   - `_load_fx_rate_features(symbol, ts_ms)` and `_load_fx_carry_features(symbol, ts_ms)` — COMPUTE the per-pair rate differential / carry from FX-01's raw FRED rows. Read the raw series via the existing macro/factor reader (`from engine.data.factor_ingestion import macro_feature_row_asof` over `connect()`, the same call `_load_macro` uses) for the USD and foreign real-yield/DXY series FX-01 ingests, then compute the differential/carry. You own this math; do not stub it out as "if an FX-01 resolver exists". When the underlying rows are absent (sandbox), the reader returns 0.0 and your computed differential is `0.0` — that is the correct structural-zero behavior. If FX-01 published a NAMED per-pair resolver, prefer calling it; otherwise compute here. Document in a comment exactly which FX-01 series ids you read.
   - `_load_fx_dxy_features(symbol, ts_ms)`, `_load_fx_cross_features(symbol, ts_ms)`, `_load_fx_momentum_features(symbol, ts_ms)` — COMPUTE DXY beta/z, cross-pair correlation/beta, and time-series momentum/trend/breakout from FX-01's DXY rows and price-bar history (read price history through the existing price-read path the repo already uses for tech features; degrade to `{}`/`0.0` when no bars). Same ownership note: this is your math, not a deferred FX-01 call.
   - `_load_fx_event_features(symbol, ts_ms)` — PERMANENT STUB; return `{}` (callers default to `0.0`). Comment that it awaits an economic-calendar feed.
   - `_load_fx_cot_features(symbol, ts_ms)` — reuse `engine.data.cftc_cot.resolve_cot_features(con, symbol=symbol, ts_ms=ts_ms)` (open/close `connect()` exactly like `_load_macro`), then map its `cot_*` keys onto the `fx.cot_*` ids (`cot_commercial_net_pctile_3y` → `fx.cot_commercial_net_pctile_3y`, etc.); on any failure return `{}`. Do NOT call `fetch_cot_records` (the live HTTP path); only read already-ingested rows.
6. **Asset-class gating (the core parity requirement).** Add a pure helper `_is_equity_only_feature(fid)` returning `True` for ids in the equity-only set: `options_symbol.`/`options`, `social.`, `social_regime.`, plus (for when their opt-in flags are enabled) `insider`/`insider_`, `short_`/`si_`/`days_to_cover_`, `13f_`, `congress_`/`congressional.`, `gov_`/`lobbying_`, `fund_`, and `nlp.`/`sentiment.finbert.`. Add a pure helper `_is_fx_feature(fid)` (`fid.startswith("fx.")`). Optionally derive an `equity_only_groups()` set from `FEATURE_GROUPS` keys for documentation, but the prefix membership test is what the gate uses. Then add a NEW backward-compatible keyword to `resolve_feature_ids` and `expected_columns`: `asset_class: Optional[str] = None`. Behavior:
   - `asset_class` normalized upper == `"FX"`: drop every requested id where `_is_equity_only_feature(fid)`; and (when `USE_FX_FEATURES` OR the model explicitly requested any `fx.*` id) ensure the FX group ids are present and appended in canonical `FX_FEATURE_IDS` order after the surviving non-FX ids.
   - `asset_class` is a non-FX equity class (e.g. `"EQUITY"`): drop all `fx.*` ids; leave equity-only ids untouched.
   - `asset_class is None`: behavior IDENTICAL to today (no gating) so all existing call sites are unaffected.
   Keep ordering deterministic (preserve registry insertion order / requested order; never sort by hash). Apply the gating AFTER the existing allowlist filtering so you never re-introduce an unregistered id.
7. **Thread asset-class where a symbol is known.** At `compute_feature_snapshot:1907` and `build_feature_snapshot:2062`, compute `asset_class_for_symbol(symbol)` (imported at `:29`) and pass it as the new `asset_class=` kwarg into the `resolve_feature_ids(...)` call, so the resolved id list is asset-class-correct at snapshot build time. This makes the gating live runtime behavior, not just a test convenience. Do NOT change `predictor.py`; the snapshot builder is the enforcement point.
8. **FX COT symbol mapping (consume-only, idempotent, no new specs).** FX-01 owns the `CotContractSpec` rows. FX-03's only COT edit is to guarantee an `EURUSD` → `6E` *symbol* mapping exists IF FX-01 did not already add it. Inspect the `6E` spec's `symbols` tuple at `engine/data/cftc_cot.py:74`: if `"EURUSD"` is NOT already a member, append it to that existing tuple (additive, no new `CotContractSpec` row, `topic` stays `"fx"`); if it is already present (FX-01 added it), make NO edit to `cftc_cot.py` at all and note that in your report. Do NOT alter any other spec. After `seed_default_cot_mappings(con)`, `cot_target_contracts_for_symbol(con, "EURUSD")` must return `6E`, so `resolve_cot_features(con, symbol="EURUSD", ...)` yields real FX-COT positioning once FX-01 data is ingested and structural zeros otherwise.
9. **Docs.** Update `docs/README_DEVELOPER_MAP.md` and/or `docs/README_ARCHITECTURE.md` (the "Feature schema and train/serve parity" section at `README_ARCHITECTURE.md:236`) to document: the FX feature groups and their ids, the `USE_FX_FEATURES` flag, the asset-class gating contract (equity-only groups OFF for FX, FX groups OFF for equities, `asset_class=None` = legacy no-gating), and the `fx.event_*` permanent-stub caveat (no calendar feed owner). Update the `CLAUDE.md` feature bullet at line 18 ONLY if the group enumeration there needs the FX groups added and the statement stays accurate.

### Tests to add
Add a single focused module `tests/test_fx_feature_groups_parity.py` (flat under `tests/`; set up `sys.path` / secrets the same way `tests/test_feature_registry_determinism.py` does, including `_write_test_secrets`). Cover, one behavior per test function:
- **FX groups register:** every id in `FX_FEATURE_IDS` appears in `FEATURE_GROUPS["fx"]` and in `FEATURE_GROUP_METADATA["fx"]["feature_ids"]`, with no duplicates and stable order.
- **Snapshot round-trip:** `_feature_uses_symbol_snapshot("fx.rate_diff_2y")` is `True`; `feature_set_tag_from_ids([...fx ids...])` contains the `fx` fragment and differs from the pure-equity `symbol_snapshot` tag.
- **Serve == train schema for an FX symbol:** `expected_columns(<fx ids + equity ids>, asset_class="FX")` equals the resolved train-time column list for the same inputs, contains the FX ids in canonical order, and is identical across repeated calls (determinism, like the existing parity test).
- **Equity-only ids absent/masked for FX:** for `asset_class="FX"`, the resolved columns contain NO id from the **default-registered** equity-only set — assert absence of `options_symbol.iv_rank`, `social.mention_rate_z`, and `social_regime.mania_score` (these are the only equity-only ids present by default; do NOT assert on `insider_*`/`13f_*`/etc. here — they are unregistered by default and prove nothing).
- **Opt-in equity groups also gated (separate test):** in a subprocess or with monkeypatched env, enable an opt-in group (e.g. `USE_INSIDER_FEATURES=1`) so its ids register, then assert a now-registered representative id (e.g. an `insider_*` id from `INSIDER_FEATURE_IDS`) is gated OFF for `asset_class="FX"`. Skip cleanly if the group still does not register.
- **Equity-only ids present for equities:** for `asset_class="EQUITY"` (and for `None`), `options_symbol.iv_rank`/`social.mention_rate_z`/`social_regime.mania_score` resolve through unchanged AND any `fx.*` ids are dropped (for `"EQUITY"`).
- **Backward compatibility vs a captured golden baseline:** capture the pre-gating default ONCE at test setup by calling `expected_columns()` (no kwarg) and snapshotting it to a module-level list; assert `expected_columns(asset_class=None) == that_snapshot` AND `resolve_feature_ids(<fixed list>) == resolve_feature_ids(<fixed list>, asset_class=None)`. (Self-comparing two same-process calls only proves they agree; to prove no regression for existing call sites, ALSO assert the `asset_class=None` path equals the no-kwarg path for several representative inputs, and that the default length is unchanged at ~111. Document that a true pre-change golden cannot be captured post-edit and that the determinism test guards order.)
- **Resolvers never raise on missing FX-01 data:** `compute_feature_snapshot(event={"ts_ms": <t>}, symbol="EURUSD", feature_ids=list(FX_FEATURE_IDS))` returns a dict with every `fx.*` id present and finite (all `0.0` when no data), and emits no exception. Use an isolated DB / in-memory connection so no live network is touched.
- **FX COT mapping:** mirror `tests/test_cftc_cot_features.py` style — on an in-memory sqlite, `ensure_cot_tables` + `seed_default_cot_mappings`, then `cot_target_contracts_for_symbol(con, "EURUSD")` returns the `6E` FX contract, and `resolve_cot_features(con, symbol="EURUSD", ts_ms=...)` returns the 4 `cot_*` keys (structural zeros with no rows). Place this in the new test module or in `tests/test_cftc_cot_features.py`.
- **Canary/secret hygiene:** in any test that captures logs, assert a generated canary string (e.g. `"CANARY-" + uuid.uuid4().hex`) you place in an env/secret-shaped value never appears in resolved columns, snapshot values, or captured log output.

### Definition of Done
- [ ] `engine/strategy/feature_registry.py` defines all FX feature-id constants and the `USE_FX_FEATURES` flag, with the exact ids listed in step 1, placed after `PRICE_FEATURE_IDS` (`:328`); `BASE_FEATURE_IDS` order is unchanged.
- [ ] New groups (`fx`, `fx_rate`, `fx_carry`, `fx_dxy`, `fx_cross`, `fx_cot`, `fx_momentum`, `fx_event`) appear in `FEATURE_GROUPS` and `FEATURE_GROUP_METADATA`.
- [ ] `"fx."` added to `_SNAPSHOT_PREFIXES`; `feature_set_tag_from_ids` emits a distinct `fx` tag fragment.
- [ ] `compute_feature_snapshot` resolves every `fx.*` id via guarded loaders that return `0.0` (never raise) when FX-01 data is absent; the rate/carry/DXY/cross/momentum loaders COMPUTE the transform (own the math), and `fx.event_*` is a documented permanent stub.
- [ ] `resolve_feature_ids`/`expected_columns` accept a backward-compatible `asset_class` kwarg; with `asset_class="FX"` the default-registered equity-only ids (`options_symbol.*`, `social.*`, `social_regime.*`) and any enabled opt-in equity groups are excluded and FX groups included; with a non-FX class `fx.*` ids are excluded; with `asset_class=None` behavior is identical to pre-change (golden-baseline + determinism proven).
- [ ] Gating is enforced in runtime resolution code (`feature_registry`), not only in tests; `compute_feature_snapshot:1907` and `build_feature_snapshot:2062` pass `asset_class_for_symbol(symbol)` into resolution so FX symbols never receive equity-only columns at snapshot build time.
- [ ] `engine/data/cftc_cot.py` change is consume-only: `EURUSD` maps to `6E` (added to the existing `6E` `symbols` tuple ONLY if FX-01 hadn't); NO new `CotContractSpec` rows; no behavior change for existing specs. If FX-01 already added `EURUSD`, `cftc_cot.py` is untouched.
- [ ] No change to `engine/runtime/storage.py` and no DB migration.
- [ ] No change to `engine/strategy/predictor.py` model adapters / routing / labels, nor to risk/execution/UI files.
- [ ] `tests/test_fx_feature_groups_parity.py` exists and all listed behaviors pass; FX COT mapping and the opt-in-group gating path covered.
- [ ] Developer docs updated to describe FX groups + asset-class gating contract + `fx.event_*` stub caveat.
- [ ] No real secrets in code or tests; canary assertion present and passing.

### Validation commands
Run from `/home/david/gitsandbox/system/system`. Expected exit codes in parentheses.
- `python -m pytest tests/test_fx_feature_groups_parity.py -q` → 0
- `python -m pytest tests/test_feature_registry_determinism.py -q` → 0  (proves no parity/order regression)
- `python -m pytest tests/test_cftc_cot_features.py -q` → 0  (proves FX COT mapping is additive)
- `python -c "from engine.strategy.feature_registry import expected_columns; a=expected_columns(); b=expected_columns(asset_class=None); assert a==b, 'asset_class=None must equal default'; assert len(a)>=100, len(a); print('PARITY_OK', len(a))"` → 0
- `python -c "from engine.strategy.feature_registry import resolve_feature_ids, FX_FEATURE_IDS; req=list(FX_FEATURE_IDS)+['options_symbol.iv_rank','social.mention_rate_z','social_regime.mania_score']; fx=resolve_feature_ids(req, asset_class='FX'); assert all(not f.startswith(('options_symbol.','social.','social_regime.')) for f in fx), fx; assert any(f.startswith('fx.') for f in fx); print('FX_GATE_OK', len(fx))"` → 0  (uses default-registered equity-only ids so the gate is actually exercised)
- `python -c "from engine.strategy.feature_registry import resolve_feature_ids, FX_FEATURE_IDS; eq=resolve_feature_ids(list(FX_FEATURE_IDS)+['social.mention_rate_z'], asset_class='EQUITY'); assert not any(f.startswith('fx.') for f in eq), eq; assert 'social.mention_rate_z' in eq; print('EQUITY_GATE_OK', len(eq))"` → 0
- `python tools/system_audit.py` → 0  (or unchanged baseline; if it reports pre-existing failures unrelated to your diff, capture them and confirm your files add none — capture this baseline BEFORE editing for comparison)
- `python tools/validate_repo.py` → 0  (deterministic checks; do not pass `--live`)
- `git status --short --untracked-files=all`  (review every file touched; there must be no stray/edited files outside the in-scope set — this is a human-review check, not an exit-code gate)
- `python tools/git_worktree_triage.py`  → expect `"ok": true` in the JSON report (no blockers introduced)

### Self-audit & NO-GO
After implementation, audit your own work and report:
1. **Exact files changed and why** — list each path (`engine/strategy/feature_registry.py`, `engine/data/cftc_cot.py` *if* `EURUSD` mapping was needed, `tests/test_fx_feature_groups_parity.py`, the doc file) with a one-line rationale. Confirm you touched NONE of the out-of-scope files (storage, predictor adapters, risk, execution, UI) and added NO new `CotContractSpec` rows.
2. **Enforcement is in runtime code** — show the lines where `compute_feature_snapshot:1907` / `build_feature_snapshot:2062` pass asset class into resolution and where equity-only ids are dropped for FX. Demonstrate (via the `FX_GATE_OK` / `EQUITY_GATE_OK` one-liner outputs) that the gate fires at runtime against DEFAULT-REGISTERED equity-only ids, not only in tests.
3. **Parity proof** — paste the lengths from `PARITY_OK`, `FX_GATE_OK`, `EQUITY_GATE_OK`; confirm `asset_class=None` equals the default and the default length is unchanged (~111); state explicitly that the determinism test stayed green before and after (no `BASE_FEATURE_IDS` reorder).
4. **Transform ownership** — confirm the rate/carry/DXY/cross/momentum loaders COMPUTE their values from FX-01's raw series/price history (name the FX-01 series ids you read) rather than deferring to a non-existent FX-01 resolver, and that `fx.event_*` is documented as a permanent stub with no calendar feed owner.
5. **Sandbox honesty** — explicitly state that FX-01 ingested data and live network were unavailable, that all FX loaders were exercised only in their structural-zero path, and that the live-data path is guarded (mark "live FX feature values verified end-to-end" as a GAP). State whether FX-01 had already added the `EURUSD`→`6E` mapping (so `cftc_cot.py` was untouched) or whether you added it. If FX-02's richer instrument metadata API was absent and you fell back to `asset_class_for_symbol`, say so.
6. **Docs updated** — confirm the developer doc (and, if changed, `CLAUDE.md` line 18) reflect the new groups, the gating contract, and the `fx.event_*` stub caveat.
7. **Verdict** — if every Definition-of-Done item is objectively met, say GO. If any item (live FX data wiring, the event-calendar stub, FX-02 metadata dependency, or a failing validator) is not fully implemented, say **NO-GO** and enumerate the precise remaining work; do not claim success you cannot demonstrate with the validation commands above.

---

## FX-04 — Prediction routing, FX regime, FX-correct labels

**Mission:** Adapt the existing prediction stack to the 24/5 FX clock — fix forward-return label windows across the 5pm-ET rollover and weekend gaps, add a USD-strength / carry regime layer to the regime stack, and make the predictor route FX symbols to FX-appropriate regime context — without altering asset-agnostic model internals.

**Prerequisites (merge order + dependency strength):**
- **FX-02 (HARD, must be merged first)** — first-class FX instrument model. Provides reliable FX symbol semantics. You consume it; you do not re-implement it.
- **FX-03 (SOFT, feature-id namespace only)** — registered FX feature groups in `engine/strategy/feature_registry.py` reachable via `resolve_feature_ids(...)`. FX-04 does **not import any FX-03 symbol** and does **not** require FX-03's loaders to return non-zero. Your only relationship to FX-03 is that, if FX-03 already registered an FX carry / rate-differential proxy feature id, your FX regime helper *may* read it; if absent, fall back to a price-series proxy and zeros. Do not add or edit FX-03 feature registrations here.
- **FX-01 (SOFT, sandbox-degradable)** — FX-01 ingests the raw factor data (USD-strength proxy such as `DXY`/`DTWEXBGS`, rate series) that your FX regime helper reads through the existing factor-universe reads. This is FX-04's *real* data dependency (the carry/DXY raw data is FX-01's, not FX-03's). In the sandbox this data is NOT present; that is expected — your FX regime signals must degrade to bounded zeros, never raise, and you must mark the missing-data fallback as an explicit gap in your self-audit. FX-01 need not be merged for FX-04 to be correct and default-safe; it is only needed for the FX regime signals to carry real information.

These prerequisites provide FX asset-class classification: this repo already has `engine/data/asset_map.py::asset_class_for_symbol` (def at `:63`), which returns `"FX"` for `DXY`, `EURUSD`, `USDJPY`, `GBPUSD` (heuristic at `:81`) and respects the `ASSET_CLASS_MAP_JSON` override (parsed at `:45`–`:61`). FX-02 is expected to have broadened classification to the full FX pair set. **You must not re-implement FX classification or FX feature registration.** If, when you start, `asset_class_for_symbol` does NOT recognize the FX pairs you test with, do NOT add a competing classifier — instead drive classification through the existing `ASSET_CLASS_MAP_JSON` env override inside your tests, and note the dependency gap in your self-audit.

**Global constraints:**
- Target repo: `/home/david/gitsandbox/system/system`. All paths below are relative to it. This is the only repo.
- Never print, log, echo, commit, or test with real secret values. Where a test needs a secret-shaped value, generate a canary string and assert it never appears in logs or returned payloads.
- Never call live broker order/cancel/replace/flatten paths. No OANDA/IBKR/Alpaca live calls. All FX trading graduates through shadow/paper and may not bypass governance.
- Build on existing architecture; never replace it. Respect the model-vs-runtime contract: models propose intent, the runtime owns safety gates. Preserve train/serve feature parity through `engine/strategy/feature_registry.py`. Preserve the champion/challenger promotion path.
- `engine/runtime/storage.py` (and the backends `engine/runtime/storage_sqlite.py` / `engine/runtime/storage_pg.py`) are high blast radius. Any schema/migration change requires a migration plus tests in BOTH backends. The design here needs **NO schema change** — see step 4, which gives the verified, definitive reason.
- "Profitable" is proven net of realistic FX costs (spread in pips + swap/carry) through the existing statistical/backtest gates, never asserted. FX-04 does NOT touch sizing, risk, or cost modeling — it only makes labels and regime context correct so downstream gates can do their job.
- If a step requires network or a credential the sandbox lacks, stub/mock it and mark the gap explicitly; do not fake success.
- **Canonical session-clock ownership:** FX-04's `engine/data/prices/fx_clock.py` is the **single source of truth for FX market-open / market-closed boundaries** for the whole FX work-stream. It is built on `America/New_York` 17:00-ET week boundaries. FX-06 (execution session, UTC ~22:00) and FX-08 (UI session, UTC ~22:00) must derive their boundaries from this module or, at minimum, configure identical boundaries via the same env knobs. State the ET↔UTC equivalence explicitly in the module docstring (Fri 17:00 ET ≈ Fri 21:00/22:00 UTC depending on US DST; Sun 17:00 ET reopen likewise) so the three clocks cannot silently disagree about when the market is closed.

### In scope
1. A small, self-contained FX clock helper module that computes correct forward-evaluation timestamps for the 24/5 FX market: it advances an evaluation timestamp across the Friday 17:00-ET → Sunday 17:00-ET weekend gap (no price formation) and is robust to the daily 17:00-ET rollover boundary, so that a 1h forward-return window opened Friday 16:30 ET does not silently span the weekend.
2. Making FX label construction use that helper: `engine/strategy/labeling.py` (the `label_event` path) and `engine/data/jobs/backfill_labels_price_from_prices.py` (the price-derived `labels_price` path) must, for FX symbols only, evaluate forward returns at an FX-clock-correct target timestamp and skip/flag windows that fall inside a market-closed gap instead of pairing an entry against a stale post-gap price.
3. A USD-strength / carry regime layer added to the regime stack (`engine/strategy/regime_stack.py::compute_regime_vector`) for FX symbols, exposed in the returned regime vector and wired so it flows into `regime_compatibility`.
4. Routing in `engine/strategy/predictor.py` so FX symbols receive FX-appropriate regime context instead of the hardcoded equity (`SPY`) regime currently used at the resolved-model path.

### Out of scope (do not touch)
- Core model classes in `engine/strategy/models/` (lgbm_regressor, lgbm_ranker, xgb_regressor, patchtst, itransformer, gbm_model, base_model) and `engine/strategy/ensemble/ridge_meta.py`. Do NOT change their feature contracts, training, or inference math. The only permitted touch is if an FX regime feature must be threaded through an existing, already-generic call — and only if genuinely required; prefer not to.
- Position sizing, currency-aware risk, exposure netting (that is **FX-05**).
- FX execution, broker routing, 24/5 trading-session enablement for live order flow (that is **FX-06**). You author a *label/regime* clock helper and designate it canonical, NOT a live trading session controller. Do not enable any live FX order path.
- Backtest cost realism (spread/swap), governance gates, profitability evidence (that is **FX-07**).
- FX surfacing in the operator UI / dashboard (that is **FX-08**). You may extend an existing regime-context payload, but do not build new UI.
- FX data ingestion / data sources, including the carry/DXY raw factor series and any economic-calendar / central-bank event feed (that is **FX-01**). Your FX regime helper consumes whatever factor data already exists; it does not ingest.
- FX feature-id registration / per-pair carry/rate-diff/DXY/cross/tsmom feature computation (that is **FX-03**). You consume the namespace; you do not author it.
- Any change to `engine/runtime/storage.py` schema (step 4 proves it is unnecessary).

### Verified anchors
(confirmed by reading the repo at HEAD `947c665`)
- `engine/strategy/labeling.py:16` — `HORIZONS_S = {"5m": 300, "1h": 3600}`; `:21` `def label_event(event_id, event_ts, price_series)`; `:36` calls `compute_return(series, event_ts, h_s * 1000)`; `:33` `regime = classify_regime(vol)`. Module is 69 lines, fully asset-agnostic. Imports `compute_return` from `engine.data.prices.returns` at `:11` and `classify_regime` from `engine.strategy.model_v2` at `:14`.
- `engine/data/prices/returns.py:18` — `def compute_return(series, event_ts, horizon_ms)`; `:19` `p0 = price_at_or_after(series, event_ts)`; `:20` `p1 = price_at_or_after(series, event_ts + horizon_ms)` — NO weekend/rollover awareness (naive `event_ts + horizon_ms`). This is the function whose forward window is wrong for FX. (`price_at_or_after` def at `:11`.) Do NOT edit `returns.py`; branch at the *callers*.
- `engine/data/jobs/backfill_labels_price_from_prices.py:59` — `HORIZONS = [300, 3600]`; `:84` `_nearest_price_at_or_after`; `:239` `ts_eval_target = ts_pred_ms + horizon_s * 1000` (the naive forward-eval timestamp to make FX-correct); `:240` `label_window_within_symbol_lifecycle(...)` gate; `:247` `exitp = _nearest_price_at_or_after(con, sym, ts_eval_target)`; `:261` `INSERT OR REPLACE INTO labels_price(... meta_json)` — i.e. **this job already writes a `meta_json` column** (the JSON payload is built at `:277`–`:281` as `{"entry_ts_ms":..., "exit_ts_ms":...}`). Job requires `ENGINE_SUPERVISED=1` (`:155`).
- `engine/data/jobs/calibrate_price_confidence.py:270` — a SECOND writer that also does `INSERT OR REPLACE INTO labels_price(... meta_json)`. Both writers already depend on a `meta_json` column existing on the sqlite `labels_price` table. (Informational — do not edit this file.)
- **`labels_price` schema reality (VERIFIED — important):** The ONLY `CREATE TABLE ... labels_price` in the entire repo is in PG at `engine/runtime/storage_pg.py:1492`, and its column list is EXACTLY `ts_pred_ms, ts_eval_ms, symbol, horizon_s, entry_price, exit_price, ret, ret_z, dir` with `PRIMARY KEY(ts_pred_ms, symbol, horizon_s)` — **there is NO `meta_json` (or any JSON) column on PG `labels_price`.** It indexes `ts_eval_ms` (`:1506`) and `symbol, ts_eval_ms` (`:1507`). On the sqlite test backend, `_ensure_labels_price_schema` (`engine/runtime/storage_sqlite.py:2722`) only ensures `labels` / `labels_exec` — it does NOT create `labels_price`; the sqlite `labels_price` is created lazily/generically (column types inferred by `_column_type` at `:264`, where a `*_json` name maps to `TEXT`), so the sqlite `meta_json` write at `:261` succeeds. Net: the audit flag has a home on sqlite (`meta_json` TEXT) and has NO home on PG. Step 4 uses this verified fact.
- `engine/strategy/regime_stack.py:453` `def compute_regime_vector(*, symbol, ts_ms, con, include_hmm=True)`; macro dict assembled at `:632`; COT signals are MERGED INTO the `macro` dict at `:651`–`:656` (e.g. `macro["cot_noncomm_net_z"] = ...`) — this is the exact pattern to follow for FX signals; **active code returns `result` at `:753`** (everything after the `finally` block starting ~`:766` inside this function is dead/unreachable duplicate code — do NOT edit the dead block, and do NOT rely on it).
- `engine/strategy/regime_stack.py:1038` `def _flatten_regime_vector(v)`; `:1042` iterates ONLY layers `("macro", "asset", "micro", "drift")` — a brand-new top-level `fx` layer would be invisible to `regime_compatibility` unless you nest the FX signals inside one of these flattened layers (merge into `macro`, as COT does).
- `engine/strategy/regime_stack.py:1072` `def regime_compatibility(profile, vector)`; `:384` `_cot_features_enabled()` returns `str(os.environ.get("USE_COT_FEATURES","0")...).lower() in {"1","true","yes","on"}`; `:388` `_load_cot_regime(con, symbol, ts_ms)` early-returns `{}` when the flag is off — this is the existing gating precedent to mirror. Factor reads go through `_get_factor_feature_asof` (imported at `:33` as an alias of `factor_universe._get_feature_asof`); `_clamp01` at `:72`; `_REGIME_MODEL_VERSION` at `:37`.
- `engine/strategy/hmm_regime.py:30` `DEFAULT_HMM_FEATURE_NAMES` (the 6 fixed training-input keys); `:779` `build_hmm_input_from_regime_vector` returns EXACTLY those 6 keys (`macro.risk_off`, `macro.vol_expansion`, `macro.credit_stress`, `macro.drawdown_shift`, `micro.vol_clustered`, `micro.liquidity_thin`) — **this is the FIXED persisted HMM training schema; do NOT add keys to it.** Separately, `:843` `build_hmm_feature_map` returns 14 `hmm_regime.*` keys (the regime *signal* projection, not the training input). Surface FX regime as a *separate additive signal in the regime vector*, NOT inside `build_hmm_input_from_regime_vector`. Safest default: leave `hmm_regime.py` entirely unchanged.
- `engine/strategy/predictor.py:54` imports `get_current_regime` from `engine.strategy.model_v2`; `:20` imports `asset_class_for_symbol`. `:2354` — `regime_at_trade = str(get_current_regime("SPY") or "MID").upper()` inside `_predict_resolved_model` (def at `:2327`; `:2356` fallback `regime_at_trade = "MID"`): the resolved-model path hardcodes `"SPY"` regardless of the symbol being predicted. This is the routing bug FX symbols inherit.
- `engine/strategy/predictor.py:511` `_model_family`; `:3272` `_ranker_equity_scope_symbol` — branches on `asset_class_for_symbol(...)` (`:3277`) and treats `{"CRYPTO","COMMODITY","FX","RATES","OPTION","OPTIONS"}` as non-equity (`:3282`), equity for `{"EQUITY","EQUITIES","US_EQUITY","STOCK","STOCKS"}` (`:3280`). Mirror this exact pattern for the new regime-anchor helper.
- `engine/data/asset_map.py:63` `asset_class_for_symbol`; `:81` returns `"FX"` for `DXY/EURUSD/USDJPY/GBPUSD`; honors `ASSET_CLASS_MAP_JSON` override (`:45`–`:61`).
- `engine/strategy/conformal.py:95` returns `"asset:FX"` for `{"FX","FOREX","CURRENCY"}`; `engine/strategy/model_feature_snapshots.py:521` maps `"FX": "SPY"` as a benchmark — evidence the codebase already treats FX as a recognized asset class.
- Tests live flat under `tests/*.py`, `unittest.TestCase` style collected by pytest (`pyproject.toml:42` `[tool.pytest.ini_options]`, `testpaths=["tests"]` at `:43`, `addopts="-q --strict-markers --strict-config --durations=20"` at `:46`, `required_plugins=["pytest-timeout>=2.4"]` at `:45`, per-test `timeout="120"` at `:60`). Reference style (REPO_ROOT sys.path insert at `tests/test_hmm_regime.py:12`–`:14`, `class ...(unittest.TestCase)` at `:25`): `tests/test_hmm_regime.py`, `tests/test_regime_detector.py`, `tests/test_conformal_prediction.py`. Validators: `tools/validate_repo.py`, `tools/git_worktree_triage.py` (both confirmed present).
- `zoneinfo` and `ZoneInfo("America/New_York")` are CONFIRMED available in this sandbox (the tz database resolves). Build the helper on real `zoneinfo` so DST transitions are correct; keep a fixed-offset fallback only as a defensive secondary, never as the primary path.

### Implementation steps
1. **Create the FX clock helper (the canonical session clock).** Add `engine/data/prices/fx_clock.py` (new, self-contained, stdlib-only — use `datetime` + `zoneinfo.ZoneInfo("America/New_York")`; `zoneinfo` tz data IS available here, so the real-DST path is the primary path. Keep a fixed-offset fallback ONLY if `ZoneInfo` construction raises, and if you fall back, log once and mark it a documented limitation — do not implement only the fallback). Provide:
   - `fx_market_closed(ts_ms: int) -> bool` — True during the weekend gap: from Friday 17:00 ET (inclusive) through Sunday 17:00 ET (exclusive at reopen). Be explicit in the docstring: closed at/after Fri 17:00 ET, open at/after Sun 17:00 ET.
   - `fx_forward_eval_ms(start_ts_ms: int, horizon_ms: int) -> int` — returns the timestamp `horizon_ms` of *open-market* time after `start_ts_ms`, advancing past any weekend gap so the realized window only counts time when the FX market was actually open. Document the 17:00-ET daily rollover handling assumption in the docstring.
   - `fx_window_spans_closed_gap(start_ts_ms: int, end_ts_ms: int) -> bool` — True if the naive window `[start, end]` overlaps a closed gap.
   - Module docstring must state the canonical-clock ownership (this module is the single source of truth for FX open/closed boundaries; FX-06/FX-08 derive from it) and the ET↔UTC equivalence.
   Keep all knobs (close hour, open hour, weekday boundaries) as module constants with env overrides (e.g. `FX_WEEK_CLOSE_HOUR_ET`, `FX_WEEK_OPEN_HOUR_ET`) so FX-06 can align without a rewrite. This module must have NO import dependency on storage, models, or network.
2. **Make `labeling.py` FX-aware (additive, behind asset-class branch).** In `engine/strategy/labeling.py::label_event`, for each `sym`, resolve its asset class via `from engine.data.asset_map import asset_class_for_symbol`. For NON-FX symbols, preserve the exact current behavior (`compute_return(series, event_ts, h_s * 1000)` unchanged). For FX symbols, compute the FX-correct evaluation timestamp with `fx_forward_eval_ms(event_ts, h_s * 1000)` and skip the label (`continue`) when `fx_window_spans_closed_gap(...)` indicates the realized return would be measured across a closed gap with no genuine post-gap price near the corrected target. Do NOT change the `labels` table schema or the z-normalization math (`impact_z = ret / vol`). Keep `classify_regime` usage as-is.
3. **Make the price-derived backfill FX-aware.** In `engine/data/jobs/backfill_labels_price_from_prices.py`, at `ts_eval_target = ts_pred_ms + horizon_s * 1000` (`:239`), branch on `asset_class_for_symbol(sym)`. For FX symbols, set the eval target via `fx_forward_eval_ms(ts_pred_ms, horizon_s * 1000)` and, before writing, `continue` (skip) when `fx_window_spans_closed_gap(ts_pred_ms, naive_target)` would have produced a gap-spanning label. Preserve the existing `label_window_within_symbol_lifecycle` gate (`:240`) and the `INSERT OR REPLACE INTO labels_price` write (`:261`). Record the FX correction in the existing `meta_json` payload built at `:277` (add `"fx_clock_corrected": true` and `"naive_eval_ms"` keys to the existing dict). This needs NO schema change because the sqlite `labels_price` row already carries `meta_json` (see step 4). For non-FX symbols, behavior is byte-for-byte unchanged.
4. **No schema change is needed — and here is the verified reason (do not add a column).** The audit flag rides the EXISTING sqlite `meta_json` column (the backfill job at `:261` and calibrate job already write `meta_json`; sqlite types `*_json` as `TEXT` via `_column_type` at `storage_sqlite.py:264`). **The PG `labels_price` table (`storage_pg.py:1492`) has NO `meta_json` / JSON column and you MUST NOT add one** — the PG write path for this job is sqlite-shaped (`INSERT OR REPLACE` is sqlite syntax) and the audit flag is a sqlite-side enrichment only. Therefore: carry the flag ONLY where the column already exists (sqlite), do NOT widen the primary key, do NOT `ALTER TABLE` PG, and do NOT add any migration. If you find that the FX correction needs to be *queryable* on PG (it does not for FX-04 — the realized window is already faithfully captured by `ts_eval_ms`, which stores the corrected eval timestamp), defer that to a later prompt and log once rather than altering the PG schema. Confirm in your self-audit that `git diff` over `engine/runtime/storage_pg.py` and `engine/runtime/storage_sqlite.py` is **empty**.
5. **Add the USD-strength / carry regime layer.** In `engine/strategy/regime_stack.py`, add a gated helper `_load_fx_regime(con, symbol, ts_ms) -> Dict[str, float]` (model it on `_load_cot_regime` at `:388`, including the early-return-`{}`-when-flag-off shape), enabled via a feature flag `USE_FX_REGIME` (default off; parse it with the same on/off style as `_cot_features_enabled` at `:384`). It should derive, from data already available through the existing factor-universe reads (USD-strength proxy such as a `DXY`/`DTWEXBGS` z-score via `_get_factor_feature_asof`, or a price-series fallback; and a carry/rate-differential proxy ONLY if FX-03 already registered one — otherwise zeros), bounded numeric signals like `fx_usd_strength_z` (clamped, e.g. `[-10,10]`), `fx_usd_strength_dir` (`[-1,1]`), and `fx_carry_pressure` (`_clamp01`). Inside `compute_regime_vector`, call it only when `asset_class_for_symbol(sym) == "FX"` (or the flag forces it), and **merge the resulting keys into the existing `macro` dict** (the same place COT keys are merged at `:651`–`:656`, NOT a new top-level layer) so they automatically flow through `_flatten_regime_vector` into `regime_compatibility`. Update `macro_conf` (`:684`) only additively and guard every read so a missing FX proxy yields zeros, never an exception. Bump `_REGIME_MODEL_VERSION` default string (e.g. append `+fx1`) ONLY if you change the emitted vector shape for existing (non-FX) symbols — for FX-only additive keys behind a default-off flag you do NOT need to bump it; if you do bump, note it.
6. **Do NOT modify the HMM training-input schema.** Leave `build_hmm_input_from_regime_vector` (`hmm_regime.py:779`, the 6 fixed keys) and the persisted HMM feature names (`DEFAULT_HMM_FEATURE_NAMES` at `:30`) untouched — adding keys there breaks persisted HMM model feature parity. If you want the FX regime visible to HMM-signal consumers, you *may* add additive, optional keys to `build_hmm_feature_map` (`:843`, the 14-key signal projection) that default to 0.0 and never change any of the existing 14 keys' values — but the safer default is to leave `hmm_regime.py` entirely unchanged and surface FX regime only via the regime vector returned by `compute_regime_vector`. Choose the minimal change and document it.
7. **Fix FX regime routing in the predictor.** In `engine/strategy/predictor.py::_predict_resolved_model` at the `regime_at_trade` assignment (`:2354`), replace the unconditional `get_current_regime("SPY")` with a symbol-aware resolution: for FX symbols (`asset_class_for_symbol(sym) == "FX"`), derive `regime_at_trade` from an FX-appropriate context (e.g. `get_current_regime(sym)` if defined for FX, else a neutral FX label such as `"FX_MID"`) and attach the FX regime signals from `compute_regime_vector(symbol=sym, ...)` into the explain/diagnostics payload, NOT into model math. Equity/crypto/other behavior must be identical to today (still `"SPY"`/`"MID"`). Add a tiny helper `_regime_anchor_symbol(sym) -> str` (mirroring `_ranker_equity_scope_symbol` at `:3272`) so the choice is testable in isolation. Do NOT change `_resolve_active_model`, `_model_family`, or any adapter's feature contract; FX symbols continue to use the asset-agnostic model families and the FX feature ids resolved by `resolve_feature_ids` (FX-03's responsibility). The routing change is purely about *regime context*, not model selection.
8. **Keep everything flag-gated and default-safe.** New FX behavior must be reachable in tests via env (`ASSET_CLASS_MAP_JSON`, `USE_FX_REGIME`, the FX clock overrides) and must be a no-op for existing equity/crypto symbols when flags are off. No new env var may be required for the system to boot.
9. **Update docs.** Add a short FX-clock + FX-regime section to the relevant existing docs (`docs/README_ARCHITECTURE.md` regime/labeling area and/or `docs/README_DATABASE_MAP.md` for the `labels_price` `meta_json.fx_clock_corrected` flag, noting it is sqlite-side only). State that `fx_clock.py` is the canonical FX session-boundary module that FX-06/FX-08 derive from. Do not create a new top-level doc framework.

### Tests to add
- `tests/test_fx_clock.py` — covers `fx_market_closed` at the Fri-17:00-ET close boundary, mid-weekend, and Sun-17:00-ET reopen; `fx_forward_eval_ms` advancing a Friday-16:30-ET + 1h window past the weekend; `fx_window_spans_closed_gap` true for a gap-spanning window and false for an intraday window; env-override of close/open hours. **Include at least one assertion pinned to a known US DST-transition date** (e.g. a timestamp just after the March spring-forward and one in standard time) proving the helper uses real `America/New_York` DST handling and not a fixed UTC offset.
- `tests/test_fx_labeling_clock.py` — `label_event` for an FX symbol skips/corrects a gap-spanning forward window while an equity symbol with the SAME inputs is unchanged (assert byte-identical non-FX behavior via the `ASSET_CLASS_MAP_JSON` override).
- `tests/test_fx_backfill_labels_clock.py` — exercises the FX branch of `backfill_labels_price_from_prices` (call the inner correction logic directly or via a seeded in-memory sqlite DB; set `ENGINE_SUPERVISED=1` only if invoking `main`, otherwise test the helper path) and asserts a gap-spanning FX label is skipped and `meta_json` carries `fx_clock_corrected` for a corrected one; assert a non-FX symbol writes exactly as before. Assert the test does NOT depend on any PG `labels_price` column being added.
- `tests/test_fx_regime_layer.py` — with `USE_FX_REGIME=1` and an FX symbol, `compute_regime_vector` emits the FX keys inside `macro`, they survive `_flatten_regime_vector`, and `regime_compatibility` reflects them; with the flag off, the vector is unchanged from baseline for an equity symbol. Assert all FX keys are finite and bounded even when the underlying proxy is absent (the sandbox case).
- `tests/test_fx_predictor_regime_routing.py` — `_regime_anchor_symbol` returns the FX-appropriate anchor for an FX symbol and `"SPY"`/equity behavior is unchanged; assert FX regime context appears in the predictor explain payload and that model family selection / feature ids for an FX symbol are NOT altered by this change (monkeypatch the model adapters; assert no live broker path is invoked).
- Each test must follow the existing `unittest.TestCase` + `REPO_ROOT` sys.path pattern from `tests/test_hmm_regime.py:12`–`:14`, and assert no canary secret appears in any logged or returned payload it triggers.

### Definition of Done
- [ ] `engine/data/prices/fx_clock.py` exists, is stdlib-only, has no storage/model/network imports, builds on real `zoneinfo` DST handling (fixed-offset only as a logged defensive fallback), exposes `fx_market_closed`, `fx_forward_eval_ms`, `fx_window_spans_closed_gap` with env-overridable boundaries, and declares itself the canonical FX session-boundary module with stated ET↔UTC equivalence.
- [ ] `labeling.py` and `backfill_labels_price_from_prices.py` apply the FX clock ONLY for FX-classified symbols; non-FX label output is provably unchanged (test-proven).
- [ ] FX gap-spanning forward windows are skipped/flagged, never paired against a stale post-gap price; corrected FX labels carry `fx_clock_corrected` in the sqlite `meta_json`.
- [ ] **No schema change at all**: `git diff` over `engine/runtime/storage_pg.py` and `engine/runtime/storage_sqlite.py` is empty; no column added, no PG `ALTER TABLE`, no migration, no primary-key change. (The audit flag rides the existing sqlite `meta_json`; PG has no JSON column and you did not add one.)
- [ ] `compute_regime_vector` emits bounded, finite USD-strength/carry signals for FX symbols under `USE_FX_REGIME`, merged into `macro` so they flow through `_flatten_regime_vector` and `regime_compatibility`; default-off is a no-op for existing symbols.
- [ ] `build_hmm_input_from_regime_vector`'s 6 fixed keys and persisted HMM training-feature parity are untouched (or `build_hmm_feature_map`'s 14 keys extended only additively with defaulted-0.0 keys, documented).
- [ ] `predictor.py` resolves `regime_at_trade` per asset class; FX symbols no longer inherit the hardcoded `"SPY"` regime; equity/crypto behavior is identical; model family selection and feature ids for FX are unchanged.
- [ ] No model class in `engine/strategy/models/` or `engine/strategy/ensemble/ridge_meta.py` was modified (verify with `git status`/`git diff`).
- [ ] No live broker order/cancel/replace/flatten path is reachable from any new code; tests assert this.
- [ ] All five new test files pass; the full suite still collects and passes against a captured baseline; validators pass.
- [ ] Docs updated for the FX clock (canonical) + FX regime + `meta_json.fx_clock_corrected` (sqlite-side).

### Validation commands
First, **capture a baseline** so you can distinguish regressions you caused from pre-existing reds:
- `git stash --include-untracked && python -m pytest tests/ -q > /tmp/fx04_baseline.txt 2>&1; echo "baseline exit=$?"; git stash pop` → record which tests (if any) were already failing BEFORE your change. Any failure present in the baseline is pre-existing and out of your scope; do not chase it. (If `git stash` is unavailable because there is nothing to stash yet, run the baseline before you start editing.)

Then, after implementation:
- `python -m pytest tests/test_fx_clock.py tests/test_fx_labeling_clock.py tests/test_fx_backfill_labels_clock.py tests/test_fx_regime_layer.py tests/test_fx_predictor_regime_routing.py -q` → exit 0
- `python -m pytest tests/test_hmm_regime.py tests/test_regime_detector.py tests/test_conformal_prediction.py tests/test_predictor_ensemble_blending.py tests/test_pit_backtest_predictor_regressions.py -q` → exit 0 (regression guard on touched subsystems)
- `python -m pytest tests/ -q` → exit 0, OR identical to the captured baseline failure set with your five new files green (confirm any residual red is byte-for-byte the pre-existing baseline set, unrelated to your diff)
- `python tools/validate_repo.py` → exit 0
- `python tools/git_worktree_triage.py` → exit 0 (no stray/untracked artifacts)
- `git status --short --untracked-files=all` → shows ONLY the intended new/modified files (the new module, the two label files, regime_stack.py, predictor.py, the five test files, doc files). It must NOT show `storage_pg.py` or `storage_sqlite.py`. (Note: `git status` always exits 0; the real gate is human/diff review of the listed file set, not the exit code.)
- `git diff --stat -- engine/strategy/models/ engine/strategy/ensemble/ridge_meta.py engine/runtime/storage_pg.py engine/runtime/storage_sqlite.py` → empty (model internals and storage schema untouched)
- `git diff --stat -- engine/strategy/hmm_regime.py` → empty (or, only if you chose the additive option, additive defaulted keys in `build_hmm_feature_map` only)

### Self-audit & NO-GO
After implementation, audit your own work and report:
1. **Exact files changed and why** — list every modified/created path with a one-line justification, and explicitly confirm `git diff` over `engine/strategy/models/`, `engine/strategy/ensemble/ridge_meta.py`, `engine/runtime/storage_pg.py`, and `engine/runtime/storage_sqlite.py` is empty.
2. **Enforcement is in runtime code, not just tests/docs** — point to the runtime lines that branch on asset class (`labeling.py`, `backfill_labels_price_from_prices.py`, `regime_stack.py::compute_regime_vector`, `predictor.py::_predict_resolved_model`) and confirm the FX clock correction and FX regime routing actually execute at runtime, not only under test.
3. **Parity & safety** — confirm non-FX label output and equity regime context are unchanged (cite the regression tests vs the captured baseline), the HMM 6-key persisted training schema is intact, NO schema change occurred on either backend, and no live broker path is reachable.
4. **Dependency gaps (state explicitly, do not paper over)** — (a) whether `asset_class_for_symbol` recognized your FX test pairs without the `ASSET_CLASS_MAP_JSON` override; (b) that the FX regime proxy fell back to zeros in this sandbox because FX-01's carry/DXY raw factor data is not ingested here (expected) — name this as the open data dependency, not a defect; (c) whether FX-03 had registered a carry/rate-diff proxy feature id (if not, you used the price-series/zeros fallback).
5. **Canonical clock** — confirm `fx_clock.py` declares itself the single FX session-boundary source of truth with stated ET↔UTC equivalence, so FX-06/FX-08 can derive from it.
6. **Docs** — confirm the FX clock / FX regime / `meta_json.fx_clock_corrected` (sqlite-side) doc updates landed.
7. If ANY Definition-of-Done item is not fully implemented (e.g. `ZoneInfo` construction failed forcing the fixed-offset fallback, or a non-baseline test went red), say **NO-GO** and enumerate the precise remaining work. Otherwise state **GO** with the passing validation evidence (and the baseline comparison).

---

## FX-05 — Currency-aware portfolio sizing + risk

**Mission:** Make portfolio sizing and the portfolio risk engine currency-aware so FX positions are sized by notional/lots in quote currency with instrument-record leverage, the FX risk sleeve is populated and enforced, FX pairs are correlation-clustered, and regulatory leverage caps are enforced as hard limits — all in runtime code, never broker-routed.

**Prerequisites:**
- **FX-02 (FX-first instrument model) — HARD prerequisite, must already be merged.** It is the single source of truth for FX symbol semantics. It provides a per-symbol FX instrument record and an accessor in `engine/data/universe.py`. **You MUST discover the real accessor name, module path, and field names by grepping — do NOT assume.** Run: `grep -rn "def get_instrument_metadata\|def get_fx_instrument\|InstrumentMetadata\|base_ccy\|base_currency\|quote_ccy\|quote_currency\|contract_size\|pip_size\|leverage_cap\|max_leverage" engine/data/universe.py engine/data/`.
  - **KNOWN NAMING DIVERGENCE — reconcile, do not guess.** FX-02 (as designed) exposes `engine/data/universe.py::get_instrument_metadata(con, symbol)` returning an `InstrumentMetadata` with fields named `base_ccy`, `quote_ccy`, `pip_size`, `contract_size`, `leverage_cap` (note: singular `universe`, `*_ccy`, `pip_size`, `leverage_cap`). Earlier brief language referred to `engine/data/fx_instruments.py::get_fx_instrument` with `base_currency`/`quote_currency`/`pip`/`max_leverage`. **These are the same concept under two naming conventions.** Use whatever names the merged FX-02 actually ships. To insulate FX-05 from the exact spelling, introduce a single thin internal adapter `_fx_instrument(con, symbol) -> dict | None` (inside `engine/strategy/fx_sizing.py`, re-used by the risk engine) that calls the discovered FX-02 accessor and normalizes its fields to a stable internal dict: `{"asset_class","base_ccy","quote_ccy","pip_size","contract_size","leverage_cap"}` (accept either `base_ccy`/`base_currency`, `pip_size`/`pip`, `leverage_cap`/`max_leverage`, etc.). All FX-05 code consumes this normalized dict, never the raw FX-02 fields directly.
  - If the FX-02 accessor is genuinely absent at integration time, treat it as a **blocking gap**: implement a minimal documented fallback inside the same adapter that reads the FX-02 instrument table/registry directly, mark the assumption in code comments and in your self-audit, and state exactly what FX-02 must expose. Do NOT invent leverage/contract/pip values inline.
  - **Canonical symbol form:** FX-02 defines the canonical stored symbol form (e.g. concatenated `EURUSD`, not `EUR_USD` or `EUR/USD`). Always resolve symbols through the FX-02 accessor/parser before keying any FX-05 logic; never re-implement base/quote splitting here.
- **FX-00 (FX enablement research dossier) — DOCUMENTATION REFERENCE ONLY, no runtime artifact.** FX-00 is a Markdown dossier and persists nothing machine-readable, so there is no caps file to read. FX-00 §6 documents the regulatory leverage caps you must enforce (e.g. ESMA/EU major-pair retail ~30:1, NFA/US ~50:1, minors lower, exotics lowest). **Do NOT grep for a persisted FX-00 caps artifact — that branch is always dead.** Instead, `engine/risk/fx_leverage_caps.py` (created in this prompt) is the de-facto source of truth, seeded from FX-00 §6's documented numbers. Pin the seed values with a comment and a test that cite "FX-00 §6" so the doc→code copy is auditable and drift-resistant.

**Global constraints (restate; obey all):**
- Target repo: `/home/david/gitsandbox/system/system`. All paths below are relative to it. This is the only repo. Activate `.venv` if present.
- Never print, log, echo, commit, or test with real secret values. Where a test needs a sentinel, use a generated canary string and assert it never appears in logs or any API/JSON output.
- Never call live broker order/cancel/replace/flatten paths. All FX work here is read-only or pure-computation. Do NOT touch broker routing or order placement — that is FX-06.
- Build on existing architecture; never replace it. Respect the model-vs-runtime contract: models propose intent (symbols/side/size), the runtime owns the safety gates. Sizing/leverage/sleeve enforcement added here is a runtime safety gate and must be enforced in runtime code, not only asserted in tests or docs.
- Preserve train/serve feature parity (`engine/strategy/feature_registry.py`) — do not add or rename features here (that is FX-03).
- `engine/runtime/storage.py` schema changes are high blast radius: any schema/migration change requires a migration plus tests. **Prefer to avoid schema changes entirely**; the FX risk sleeve is already a budget key, and instrument metadata comes from FX-02. If you find you need a new column, stop and reconsider — you almost certainly do not for FX-05.
- "Profitable" is proven through existing statistical/backtest gates net of realistic FX costs (spread in pips + swap/carry). FX-05 does NOT assert profitability and does NOT implement the cost model (that is FX-07). FX-05 only makes sizing/risk currency-correct.

### In scope
- Branch portfolio weight semantics on `asset_class == "FX"` so that an FX weight is interpreted as **notional fraction of base-currency equity converted correctly through the pair rate**, not as a share count. Concretely: for `EURUSD` quoted at 1.08, a 0.10 target weight must mean "10% of equity of EUR exposure" and must NOT be interpretable downstream as `0.10*equity/1.08` "shares". Provide the helper(s) and metadata that make the FX leg unambiguous (notional in quote currency, units/lots, effective leverage) and attach them to the target/intent payload so the broker layer (FX-06) can consume them without re-deriving.
- Populate and enforce the **FX risk sleeve**: ensure FX exposure is correctly bucketed into the `"FX"` asset-class budget in `engine/risk/portfolio_risk_engine.py` and that the budget is actually applied to FX symbols. Today `asset_class_for_symbol` only classifies four hardcoded pairs as FX; with FX-02 expanding classification, confirm the existing `"FX": 0.50` sleeve now binds.
- Add **currency-pair correlation clustering** to the existing correlation-cluster cap path so co-moving pairs (EURUSD/GBPUSD, etc.) are treated as one cluster even when price-correlation history is thin: add a structural "shared-currency" adjacency on top of the existing price-correlation graph in `_corr_graph_components`.
- Enforce **regulatory leverage caps as hard limits**: per-FX-symbol effective leverage (gross notional / equity for that leg) may not exceed the lesser of (instrument leverage from FX-02) and (regulatory cap from FX-00 §6 for that pair tier). Breach must clamp the weight down (soft) AND, if a hard ceiling cannot be satisfied (e.g. rate unavailable in a live runtime), set a fail-closed block reason in the risk-engine `info`/`block_reason`, consistent with how `drawdown_hard_block` / `portfolio_vol_hard_block` already block.

### Out of scope (do not touch)
- Order placement, broker routing, and the weight→qty/lots conversion at the broker boundary — that is **FX-06**. Specifically: `engine/execution/broker_sim.py` line 2388 (`target_qty = (to_w * equity) / float(px_mid)`; the module docstring at lines ~15-22 also states this share-conversion assumption), `engine/execution/broker_router.py`, and any `broker_*` module. **The weight→lots conversion site at broker_sim.py:2388 is explicitly FX-06's to fix; FX-05 only makes the weight + attached FX metadata unambiguous so FX-06 can convert to lots, not shares.** You may read these to understand the contract but must not modify them.
- FX data ingestion / price feeds — **FX-01**. (Note: the live pair-rate availability that FX-05's leverage stage depends on is sourced by FX-01's OANDA feed, which is default-off/operator-gated; see "Known data dependency" below.)
- The FX instrument model itself (table/registry/accessor in `engine/data/universe.py`) — **FX-02**. You consume it; you do not redefine its schema.
- FX feature groups / `feature_registry.py` — **FX-03**.
- Prediction routing, FX regime, FX labels — **FX-04**.
- FX backtest cost model (spread/swap/carry) and profitability evidence — **FX-07**.
- Operator UI surfacing (`ui/*`, `routes/*`, dashboard/terminal JS) — **FX-08**. You MAY add fields to the risk-engine `info` dict; they are already exposed to FX-08 via the existing read-model API (see "Read-model exposure" below) with no UI edit from you.
- `engine/rl/*` leverage logic — leave the RL env's `leverage_cap` alone; it is a separate, shadow-only path.

### Verified anchors
(Confirmed by reading the repo at the stated lines/symbols on 2026-06-23; corrections vs. the brief are noted. Re-grep before editing — line numbers drift.)
- `engine/strategy/position_sizing.py:119` — `def position_from_signal(...) -> Dict[str, Any]`. Returns a weight fraction clamped to `MAX_POS` (`MAX_POSITION_FRACTION`, default 0.20). **No asset-class branch, no leverage, no lots, no shares here.**
- `engine/strategy/portfolio_targets.py:8` `def desired_weight(...)` and `:22` `def resolve_desired_weight(...)`. Both compute a weight fraction (`weight = score/score_norm * gross_cap`, clamped by `symbol_cap_fn`). No asset-class awareness.
- `engine/strategy/portfolio.py:2983` `_desired_weight(...)` and `:2994` `_resolve_desired_weight(...)` delegate to `_portfolio_targets`. **Correction to brief:** the literal `qty = weight*equity/price` share math is NOT in `portfolio.py`/`position_sizing.py`/`portfolio_targets.py`; those produce *weights*. The share conversion lives at the broker boundary — `engine/execution/broker_sim.py` docstring (~lines 15-22) and `engine/execution/broker_sim.py:2388` (`target_qty = (to_w * equity) / float(px_mid)`). That conversion site is FX-06's to fix.
- `engine/risk/portfolio_risk_engine.py:135-142` — `_DEFAULT_ASSET_CLASS_BUDGETS` dict containing `"FX": 0.50` (the dict spans 135-142; the env-override loop is lines 144-152). `ASSET_CLASS_BUDGETS` (line 144) is the effective table; gate `USE_ASSET_CLASS_BUDGETS` at line 125.
- `engine/risk/portfolio_risk_engine.py:645` `_exposure_snapshot(...)` calls `asset_class_for_symbol(s)` at line 674 to bucket `by_asset_class`. The post blob exposed downstream is `info["asset_class_gross_post"]`.
- `engine/risk/portfolio_risk_engine.py:1041` `_post_constraint_checks(...)` and `:1186` `_apply_asset_class_budgets(...)` already enforce the `"FX"` budget — but only for symbols `asset_class_for_symbol` classifies as FX (direct calls at lines 1194, 1208, 1232). **Correction to brief:** the sleeve math exists and is equity-notional; the gap is (a) nothing routes FX exposure so it is de facto unpopulated, and (b) there is no per-leg leverage notion.
- `engine/risk/portfolio_risk_engine.py:1363` `_corr_graph_components(...)` builds the price-correlation adjacency (threshold `CLUSTER_CORR_TH`=0.85 at line 120) then DFS components; `:1416` `_apply_corr_cluster_caps(...)` applies `CLUSTER_MAX_GROSS`=0.45 (line 121), bounded by `CLUSTER_MAX_COMPONENTS`=12 (line 122). Structural currency-pair clustering must be added inside `_corr_graph_components` (after the price-corr adjacency loop, before DFS).
- `engine/risk/portfolio_risk_engine.py:778` `_last_price(con, symbol)` and `:806` `_equity_reference(con) -> (float, str)` — the rate proxy and equity reference for per-leg leverage math.
- `engine/risk/portfolio_risk_engine.py:1740` `apply_portfolio_risk_engine(...)` — the ordered pipeline. In it: `_apply_asset_class_budgets` is invoked at line 1842, `_apply_corr_cluster_caps` at line 1862. The block flow uses local `blocked`/`block_reason` (initialized at 1822); `drawdown_hard_block` is set at line 1834; `portfolio_vol_hard_block` around 1870. There is no per-stage `set_state` call — `info` (including `block_reason`) is serialized once to the `portfolio_risk_info` state at line 1949 (and `portfolio_risk_block` at 1948). **New FX leverage enforcement must set the same `blocked`/`block_reason` locals between the `_apply_asset_class_budgets` call (1842) and the `_apply_corr_cluster_caps` call (1862).**
- `engine/data/asset_map.py:63` `def asset_class_for_symbol(symbol: str) -> str` — returns `"FX"` for the hardcoded set `("DXY","EURUSD","USDJPY","GBPUSD")` today (line ~85). FX-05 must NOT hardcode more pairs here — it must consult the FX-02 instrument record when classifying, falling back to this function.
- No `engine/risk/fx_leverage_caps.py` and no `engine/strategy/fx_sizing.py` exist today (confirmed absent). The only `leverage_cap` constant in `engine/` is the unrelated `engine/rl/*` env.
- **Read-model exposure (FX-08 seam, already wired):** `engine/api/api_system.py:4691` `def api_get_portfolio_risk(...)` reads the `portfolio_risk_info` state (set at portfolio_risk_engine.py:1949) and returns it as the API `info` field (parsed at api_system.py:~4740). **Therefore any FX field you add to the risk-engine `info` dict is automatically surfaced to FX-08 through this existing API — you do not edit any API/UI/route file.** State this in your self-audit so FX-08's "FX sleeve/leverage fields present in read-model" prerequisite is satisfiable through this path.
- Tests live in `tests/` (flat, ~377 `.py` files), run via `pyproject.toml` `[tool.pytest.ini_options]` (`testpaths=["tests"]`, `addopts="-q --strict-markers --strict-config --durations=20"`). Marker `safety_critical` is declared (`"safety_critical: money-path control test that must pass on every merge"`) and used by money-path tests (e.g. `tests/test_drawdown_fail_closed.py`). Validators: `python tools/validate_repo.py`, `python tools/syntax_check_workspace.py`, `python tools/git_worktree_triage.py`. Existing risk-engine test to model patterns on: `tests/test_portfolio_risk_engine_live_thresholds.py` (uses `monkeypatch.setattr` on the stage functions and a captured `set_state`).

### Implementation steps
1. **Resolve FX-02's instrument accessor first.** Grep (command in Prerequisites) for the real accessor name and field names in `engine/data/universe.py`. Record the exact symbol you will call. Build the normalizing adapter `_fx_instrument(con, symbol) -> dict | None` (in `engine/strategy/fx_sizing.py`) that wraps the discovered FX-02 accessor and returns the stable internal dict `{"asset_class","base_ccy","quote_ccy","pip_size","contract_size","leverage_cap"}`, or `None` for non-FX. Accept both naming conventions (`*_ccy`/`*_currency`, `pip_size`/`pip`, `leverage_cap`/`max_leverage`). Document the assumption in a comment. If the FX-02 accessor is absent, implement the documented direct-table fallback inside this adapter and mark the gap.
2. **Create the leverage-cap source of truth.** Add `engine/risk/fx_leverage_caps.py` exposing:
   - `regulatory_leverage_cap(symbol: str, *, jurisdiction: str | None = None) -> float` — returns the regulatory max leverage for the pair tier (major/minor/exotic) under the active jurisdiction. Use a documented default table **seeded from FX-00 §6** (comment each constant with the FX-00 §6 source), overridable via env `FX_REGULATORY_LEVERAGE_CAPS_JSON` with selector env `FX_LEVERAGE_JURISDICTION` (default `"EU"`; provide a `"US"` override profile). Do NOT grep for a persisted FX-00 artifact — there is none.
   - `effective_leverage_cap(symbol: str, instrument: dict | None) -> float` — `min(instrument["leverage_cap"], regulatory_leverage_cap(symbol))`, defaulting safely (to the regulatory cap) when instrument metadata is missing. Fail-closed: unknown/exotic ⇒ the most conservative cap, never unbounded.
   - Keep this module pure (no DB writes, no network, import-safe with zero side effects).
3. **Add a currency-aware sizing helper** in the new module `engine/strategy/fx_sizing.py` (do not bloat `position_sizing.py`). Provide:
   - the `_fx_instrument(con, symbol)` adapter from step 1.
   - `fx_weight_to_notional(symbol, weight, equity, instrument, *, pair_rate) -> dict` returning `{"asset_class":"FX","base_ccy","quote_ccy","weight":w,"base_notional","quote_notional","units","lots","effective_leverage","pair_rate","instrument_leverage","regulatory_leverage","effective_leverage_cap"}`. `base_notional = abs(weight)*equity`; `quote_notional = base_notional * pair_rate` (document the EURUSD-style `quote = base*rate` convention); `units = base_notional` scaled into instrument units; `lots = units / contract_size`. **The whole point: this never produces a share count and never does `weight*equity/price`.**
   - `clamp_fx_weight_to_leverage(symbol, weight, equity, instrument) -> tuple[float, dict]` returning the leverage-capped weight and a reason blob, using `effective_leverage_cap`. This is the soft clamp used inside the risk engine.
   - All helpers must be import-safe with zero side effects and must degrade gracefully (return the input weight unchanged + a `reason` noting `"fx_instrument_missing"`) when FX-02 metadata is unavailable, rather than raising.
4. **Wire asset-class resolution through FX-02 in the risk engine.** In `engine/risk/portfolio_risk_engine.py`, introduce a single helper `_asset_class_for(con, symbol)` that prefers the FX-02 instrument record's `asset_class` (via the `_fx_instrument` adapter) and falls back to `asset_class_for_symbol`. Replace the direct `asset_class_for_symbol(...)` calls inside `_exposure_snapshot` (line 674), `_apply_asset_class_budgets` (lines 1194/1208/1232), and the asset-class branch of `_post_constraint_checks` with `_asset_class_for(...)` so FX exposure is correctly bucketed into the existing `"FX": 0.50` sleeve. (If a call site lacks a `con`, thread it through or use a module-level connection helper already used by neighboring stages.) Do NOT change the sleeve value (it is an env-overridable default at line 139); just make it bind.
5. **Add FX leverage enforcement as a new risk stage.** Add `_apply_fx_leverage_caps(con, desired, info)` to `engine/risk/portfolio_risk_engine.py` that, for every FX symbol, (a) computes per-leg effective leverage using `_equity_reference(con)` and the pair rate via `_last_price(con, symbol)` (if missing, treat as data-unavailable and fail-closed for that leg), (b) soft-clamps weight to the effective leverage cap via `clamp_fx_weight_to_leverage`, writing a `reason["fx_leverage_cap"]` blob on the row, and (c) records any *post-clamp* residual breach or missing-rate leg into a list. Insert the call into `apply_portfolio_risk_engine` **after the `_apply_asset_class_budgets` call (line 1842) and before the `_apply_corr_cluster_caps` call (line 1862)**, wrapped in the same `try/except _warn_nonfatal(...)` pattern as the sibling stages. If any leg cannot be made compliant, set the local `blocked = True` and `block_reason = {"type":"fx_leverage_hard_block", ...}` exactly like the existing `drawdown_hard_block` pattern (lines 1822-1834) so it flows into `info["block_reason"]` and the `portfolio_risk_info`/`portfolio_risk_block` state at lines 1948-1949. Gate the whole stage behind env `PORTFOLIO_RISK_USE_FX_LEVERAGE_CAPS` (default `"1"`), mirroring sibling toggles.
6. **Add currency-pair structural correlation clustering.** In `_corr_graph_components` (line 1363), after building the price-correlation adjacency loop and before the DFS, add structural edges: for any two FX symbols sharing a base or quote currency (derived from FX-02 records via `_fx_instrument`), add an undirected edge regardless of price-corr history (guard behind env `PORTFOLIO_RISK_FX_CURRENCY_CLUSTERS`, default `"1"`). This makes EURUSD/GBPUSD (shared USD) and EURUSD/EURGBP (shared EUR) cluster together so `_apply_corr_cluster_caps` caps their combined gross. Annotate the cluster `reason` blob with `{"fx_shared_currency": [...]}` so the basis of the edge is auditable. Keep `CLUSTER_MAX_COMPONENTS` bounding intact. (`_corr_graph_components` currently takes `(con, syms)`, so the connection is already available.)
7. **Keep the model→runtime contract intact.** Do not give models leverage/lot authority. The FX sizing metadata is attached at the portfolio/risk layer to the target payload (into the existing `reason` dict and a new `fx` sub-dict on the desired/target row) so FX-06 reads it; models still only emit symbol/side/weight.
8. **No schema change.** Confirm you introduced no new `storage.py` columns/tables. The FX sleeve reuses the existing asset-class budget machinery; instrument metadata is read from FX-02. Persisting new keys into the existing `info` JSON (serialized to `portfolio_risk_info` state) is fine — it is a JSON blob — but do not alter any table DDL.
9. **Update docs.** Add an FX sizing/risk subsection to an existing risk/architecture doc (grep for where `portfolio_risk_engine`/asset-class budgets are documented: `grep -rln "portfolio_risk_engine\|asset.class budget\|ASSET_CLASS_BUDGETS" docs/ engine/risk/README.md` — likely `engine/risk/README.md` or `docs/README_ARCHITECTURE.md`), describing: FX weight semantics (notional, not shares), the FX sleeve, currency-pair clustering, the dual leverage hard cap (FX-00 §6 regulatory + FX-02 instrument), and that FX `info` fields surface to FX-08 via the existing `api_get_portfolio_risk`. Do not create a brand-new top-level doc if an existing risk doc is the right home.

### Tests to add
Add focused unit tests under `tests/` (flat layout; no subdir). Mark money-path enforcement tests with `@pytest.mark.safety_critical`. Where a test produces logs or serialized `info`, generate a unique canary string and assert it never appears in captured logs or the serialized JSON.
- `tests/test_fx_sizing_notional.py` — `fx_weight_to_notional` for EURUSD@1.08: a 0.10 weight on equity=100000 yields `base_notional==10000`, `quote_notional==10800`, and `lots == base_notional/contract_size`; assert the result NEVER equals the legacy share count `0.10*equity/1.08` and that `asset_class=="FX"`. Cover a JPY-quoted pair (USDJPY) for pip/contract differences. Cover the FX-02-missing fallback path (returns unchanged + reason, no raise).
- `tests/test_fx_leverage_caps.py` — `regulatory_leverage_cap`/`effective_leverage_cap`: a major pair under `EU` jurisdiction caps at the FX-00 §6 documented value; `min(instrument, regulatory)` wins; unknown/exotic ⇒ most conservative; env override `FX_REGULATORY_LEVERAGE_CAPS_JSON` and `FX_LEVERAGE_JURISDICTION` (US profile) respected; missing instrument ⇒ regulatory cap (never unbounded). Pin at least one numeric value with a comment citing FX-00 §6.
- `tests/test_fx_portfolio_risk_sleeve.py` (`safety_critical`) — drive `apply_portfolio_risk_engine` with a `desired` map of FX symbols whose combined gross exceeds the `"FX"` sleeve (0.50) and confirm `_apply_asset_class_budgets` scales them down and `info["asset_class_gross_post"]["FX"] <= 0.50`. Use FX-02 metadata (real accessor or a monkeypatched `_fx_instrument`/FX-02 accessor) so `_asset_class_for` returns `"FX"`. Use an in-memory/sqlite test connection consistent with `tests/test_portfolio_risk_engine_live_thresholds.py` (monkeypatch the sibling stages you are not exercising, as that file does).
- `tests/test_fx_leverage_hard_block.py` (`safety_critical`) — cover BOTH branches: (1) **positive/correct-math path** — an FX leg with a REAL pair rate (seed `_last_price` via a monkeypatch or a sqlite price row) whose requested weight implies effective leverage above the FX-00 §6 regulatory cap is soft-clamped to exactly the effective cap (assert the clamped weight and the `effective_leverage` in the row's `fx`/`reason` blob match the expected arithmetic — this proves the leverage math, not just the fail-closed path); (2) **fail-closed path** — a leg whose rate is unavailable in a simulated live runtime sets `info["blocked"] is True` with `block_reason["type"]=="fx_leverage_hard_block"`. Assert enforcement happens in the engine output, not just the helper.
- `tests/test_fx_currency_cluster_caps.py` — EURUSD+GBPUSD (shared USD) with thin/zero price-corr history still land in one cluster via the structural edge and get capped by `CLUSTER_MAX_GROSS`; assert the cluster `reason` shows `fx_shared_currency`. Assert a non-sharing pair is NOT structurally linked.

### Definition of Done
- [ ] `engine/strategy/fx_sizing.py` exists with the `_fx_instrument` normalizing adapter, `fx_weight_to_notional`, and `clamp_fx_weight_to_leverage`; FX weights convert to base/quote notional + lots, never to a `weight*equity/price` share count.
- [ ] `engine/risk/fx_leverage_caps.py` is the single source of regulatory caps, seeded from FX-00 §6 (no grep-for-persisted-artifact); `effective_leverage_cap = min(instrument, regulatory)`; unknown ⇒ most conservative; env-overridable via `FX_REGULATORY_LEVERAGE_CAPS_JSON`/`FX_LEVERAGE_JURISDICTION`.
- [ ] `engine/risk/portfolio_risk_engine.py` uses `_asset_class_for` (FX-02-aware) for bucketing in `_exposure_snapshot`, `_apply_asset_class_budgets`, and `_post_constraint_checks`, so the existing `"FX": 0.50` sleeve actually binds to FX exposure.
- [ ] A gated `_apply_fx_leverage_caps` stage is wired into `apply_portfolio_risk_engine` between the asset-class-budgets call (1842) and the corr-cluster-caps call (1862); per-leg leverage is clamped (soft) and a `fx_leverage_hard_block` fail-closed `block_reason` is set (via the local `blocked`/`block_reason` flowing to the `portfolio_risk_info`/`portfolio_risk_block` state) when a leg cannot be made compliant, mirroring `drawdown_hard_block`.
- [ ] `_corr_graph_components` adds structural shared-currency edges (env-gated, after the price-corr adjacency, before DFS) so co-moving pairs cluster even with thin price history; cluster reason records `fx_shared_currency`.
- [ ] No `engine/runtime/storage.py` schema/DDL change (verify by diff). FX metadata rides in existing JSON/`reason` blobs and the `portfolio_risk_info` state.
- [ ] Enforcement is in runtime code (risk engine + sizing helpers), not only in tests/docs. Models gained no leverage/lot authority.
- [ ] FX-02 field-name divergence is reconciled through the `_fx_instrument` adapter (consumes whatever FX-02 actually ships; no hardcoded assumption about `base_ccy` vs `base_currency`, etc.).
- [ ] FX `info` fields are confirmed to surface through the existing `api_get_portfolio_risk` (no API/UI/route file edited).
- [ ] All new tests added under `tests/` and passing; money-path tests carry `@pytest.mark.safety_critical`; the leverage test covers both the correct-math and fail-closed branches; canary assertions present.
- [ ] Docs updated in an existing risk/architecture doc describing FX weight semantics, sleeve, clustering, the dual leverage hard cap, and the read-model exposure path.
- [ ] No canary/secret string leaks into logs or serialized risk `info`.
- [ ] All new env flags default to the system's existing behavior being a strict superset (FX paths only activate for FX symbols; non-FX equity behavior is byte-for-byte unchanged when no FX symbol is present).

### Known data dependency (mark, do not fake)
The leverage stage's pair rate comes from `_last_price(con, symbol)`, which depends on FX-01's OANDA pricing feed being enabled (default-off, operator-gated) to have FX rows in the sandbox. In the sandbox there are no FX price rows, so the live-rate path is exercised only via monkeypatched/seeded rates in tests. Treat a missing live rate as **data-unavailable ⇒ fail-closed for that leg**, never as a pass. Account-currency conversion (e.g. converting a non-USD-quoted leg's quote-ccy notional back to account ccy) is out of FX-05's runtime scope here; if you surface it at all, surface it as metadata only and mark it as requiring an FX-01 cross-rate source. Do not assert profitability — that is FX-07.

### Validation commands
Run from `/home/david/gitsandbox/system/system` (activate `.venv` if present). Expected exit codes in parentheses.
- **Baseline first (so you can distinguish your changes from pre-existing reds):** `python -m pytest tests/ -q -m safety_critical 2>&1 | tail -20` — capture the pre-change pass/fail set BEFORE editing. Note any pre-existing red money-path tests; you must not introduce new ones.
- `python -m pytest tests/test_fx_sizing_notional.py tests/test_fx_leverage_caps.py tests/test_fx_portfolio_risk_sleeve.py tests/test_fx_leverage_hard_block.py tests/test_fx_currency_cluster_caps.py -q` → 0
- `python -m pytest tests/ -q -m safety_critical` → 0 (existing money-path suite still green vs. your captured baseline, including your new safety_critical tests). If a pre-existing unrelated safety_critical test was already red in the baseline, note it as pre-existing rather than masking it or declaring a false NO-GO.
- `python -m pytest tests/test_portfolio_risk_engine_live_thresholds.py -q` → 0 (no regression in existing risk-engine behavior)
- `python -c "import engine.strategy.fx_sizing, engine.risk.fx_leverage_caps, engine.risk.portfolio_risk_engine"` → 0 (import-safe, no side effects)
- `python tools/syntax_check_workspace.py` → 0
- `python tools/validate_repo.py` → 0 (canonical repo validation; if it fans out the full pytest suite and a pre-existing unrelated test is red in the sandbox, capture the output and note it as pre-existing rather than masking it)
- `python tools/git_worktree_triage.py` → 0 / `{"ok": true}`
- `git status --short --untracked-files=all` — human-review check (exit code is not a correctness gate): the output must show ONLY your intended files — the five test files, `engine/strategy/fx_sizing.py`, `engine/risk/fx_leverage_caps.py`, edits to `engine/risk/portfolio_risk_engine.py`, optionally a documented fallback in `engine/strategy/fx_sizing.py` for missing FX-02, and one doc file. No stray edits to broker/execution/API/UI/route/feature-registry/storage files.

### Self-audit & NO-GO
After implementation, audit your own work and report:
1. **Exact files changed and why** — list every created/modified file with a one-line justification. Confirm you did NOT modify any `broker_*`, `engine/execution/*`, `engine/api/*`, `routes/*`, `ui/*`, `feature_registry.py`, or `engine/runtime/storage.py` DDL.
2. **Runtime enforcement proof** — point to the specific lines in `engine/risk/portfolio_risk_engine.py` where (a) FX exposure is bucketed via `_asset_class_for`, (b) `_apply_fx_leverage_caps` is invoked in the pipeline (between 1842 and 1862), (c) `fx_leverage_hard_block` sets `blocked`/`block_reason` so it flows to the `portfolio_risk_info`/`portfolio_risk_block` state (1948-1949), and (d) structural FX edges are added in `_corr_graph_components`. Enforcement must be in these runtime paths, not only in the test files.
3. **Dependency honesty** — state exactly which FX-02 accessor (name, module, field spellings) you consumed and how the `_fx_instrument` adapter normalized them. If FX-02 was missing and you used the documented fallback, say so and describe what FX-02 must provide for the real wiring. Confirm the regulatory caps were seeded from FX-00 §6 (docs-only; no machine-readable artifact exists) and that you did not include a dead grep-for-persisted-caps branch.
4. **No-fake-success** — confirm: the live pair-rate path was exercised only via mocked/seeded rates (sandbox has no FX price rows) and the missing-rate path fails closed; the leverage test includes a positive correct-math case (not only the fail-closed path); you captured a pre-change safety_critical baseline and did not mask or fabricate green; you did not assert profitability (deferred to FX-07).
5. **Read-model seam** — confirm FX `info` fields reach FX-08 via the existing `api_get_portfolio_risk` (api_system.py:4691 reading `portfolio_risk_info`) with no API/UI/route edit from you.
6. **Docs updated** — name the doc file and section you added.
7. **NO-GO check** — if any Definition-of-Done item is not fully implemented and enforced in runtime code, declare **NO-GO**, list the unmet items, and describe the precise remaining work. Only declare GO if every checkbox is objectively satisfied and the validation commands return the expected exit codes (with any pre-existing reds explicitly attributed to the captured baseline, not your change).

---

## FX-06 — FX execution + broker routing + 24/5 session

**Mission:** Teach the execution layer to construct and place FX orders as IBKR CASH/IDEALPRO Forex contracts, route FX symbols to the FX-capable broker, and apply 24/5 session + rollover/swap-aware timing — all strictly inside shadow/paper/sim, with every live order/cancel/replace/flatten path left untouched.

**Scope decision (no fork):** Implement FX execution via **IBKR CASH/IDEALPRO contracts only**. Do **NOT** build an OANDA REST execution adapter in this task — it is deferred. (Rationale: a second live-capable broker module is unnecessary to deliver FX execution, doubles the credential/gate surface, and creates a conditional file set / validation list that a headless runner resolves inconsistently. IBKR is the FX-capable broker for this task.) The README must state this explicitly.

**Prerequisites (merge order):**
- **FX-02 (HARD — must be merged first):** first-class FX instrument model and the single source of truth for FX symbol semantics. FX-02 owns the canonical stored symbol form and the symbol parser/accessor. You MUST consume FX-02's accessor/parser (e.g. `is_fx_symbol` / `parse_fx_symbol` / `get_instrument_metadata` — grep FX-02's merged module for the exact names) to classify FX symbols and split base/quote. **Do NOT re-implement a base/quote pair parser inline** (the codebase already has too many; FX-02 is canonical). Only if FX-02's accessor is genuinely absent at runtime, fall back to `engine/data/asset_map.py::asset_class_for_symbol(symbol) == "FX"` plus a minimal 6-letter split (`EURUSD` → base `EUR`, quote `USD`), and **fail closed** (treat as non-FX → existing STK path) for any symbol that is not a recognizable FX pair. Read whatever FX-02 actually merged before coding.
- **FX-04 (HARD — must be merged first):** FX-04 owns `engine/data/prices/fx_clock.py`, the **canonical FX session-boundary source of truth** (America/New_York 17:00-ET week boundaries). **You MUST derive your session boundaries from FX-04's `fx_clock.py` rather than inventing an independent clock.** If `fx_clock.py` exposes a boundary/session function you can call, call it. If it only exposes ET-based constants, your `fx_session.py` must (a) reuse those same boundary values via import, and (b) document the exact ET↔UTC equivalence (Sunday 17:00 ET open ≈ Sunday 21:00–22:00 UTC; Friday 17:00 ET close ≈ Friday 21:00–22:00 UTC, DST-dependent). Under no circumstances hardcode a UTC boundary that can disagree with FX-04's ET clock for the same instant — derive, do not duplicate. State in your self-audit that FX-04's clock is the source of truth and how you derived from it.
- **FX-05 (HARD — must be merged first):** currency-aware sizing. FX-05 attaches FX sizing metadata (notional / quote-currency context, and where available lots/contracts) into the risk-engine `reason`/`fx` sub-dict on each order row. **Handoff contract:** you CONSUME these orders read-only; you do NOT re-size them. Order dicts already carry `qty`; they may carry an `fx` sub-dict and/or FX notional fields. Read attached FX context defensively (treat missing fields as absent → fall back to existing behavior); never write sizing fields back.

**Global constraints (restate — these bind this task):**
- Target repo: `/home/david/gitsandbox/system/system`. All paths below are relative to it. There is only this one repo.
- **No live broker mutation, anywhere, including tests.** You may NOT call live order-submit, cancel, replace, or flatten paths for IBKR/Alpaca. All FX order placement must be reachable only through `dry_run=True` and/or the sim/paper/shadow broker. New FX code paths must remain behind the same `dry_run`/execution-mode/kill-switch/reconcile gates that already protect equities — they may not introduce a new bypass. Tests use mocks/stubs only; no test may open a real socket to a broker (the repo already installs a socket guard in `tests/conftest.py`).
- Never print, log, echo, commit, or test with real secret values. Read any broker credential from env/secret-file indirection only.
- Build on existing architecture; never replace it. Respect the model-vs-runtime contract (models propose intent; runtime owns safety gates) and train/serve parity. Do not give models direct order authority.
- `engine/runtime/storage.py` schema changes are high blast radius. This task should **not** require a schema change; if you believe one is needed, stop and redesign to avoid it (FX session suppression must reuse the existing audit/suppression path — see step 4). If genuinely unavoidable, add a migration + migration test and flag **NO-GO-pending-review**.
- "FX-capable" routing must be deterministic and gate-respecting: routing FX to the FX broker must not weaken any existing gate (execution-mode boundary, kill switch, pre-live reconcile, options block, RL-source block, failover-chain validation).

### In scope
- Add FX contract construction in `engine/execution/broker_ibkr_gateway.py`: a new `_mk_fx_contract(symbol)` building an IBKR `Contract` with `secType="CASH"`, `exchange="IDEALPRO"`, and base/quote currency split, plus a `_mk_contract_for_symbol(symbol)` dispatcher that chooses FX vs STK construction per symbol at the four existing `_mk_stock_contract(symbol)` call sites (1209, 1729, 2049, 2112).
- Make `engine/execution/broker_router.py` prefer the FX-capable broker (`ibkr`) for FX symbols: add an FX-aware selection helper that, when a batch contains FX symbols, prefers the FX-capable broker name at the front of the existing validated chain — without bypassing `configured_failover_chain()` / `validate_live_failover_chain` validation or any gate.
- Add 24/5 FX session handling + rollover/swap-aware timing to the execution policy/timing layer. The timing decision today lives in `engine/execution/execution_decision_engine.py::decide_execution_strategy(...)` (latency/aggressiveness tiers), consumed by `engine/execution/execution_policy_engine.py::apply_execution_policy(...)`. Add a pure, side-effect-free session module that, for FX symbols, classifies the current time into a 24/5 session state (open weekday, weekend-closed, daily-rollover window) — boundaries derived from FX-04's `fx_clock.py` — and exposes a swap/rollover-aware timing adjustment that the policy engine applies for FX orders only. Equity behavior must be byte-for-byte unchanged.
- Update `engine/execution/README.md` to document the IBKR CASH/IDEALPRO FX contract path, the FX routing preference, and the 24/5 session module.

### Out of scope (do not touch)
- **OANDA execution adapter.** Not built in this task (see scope decision above). Do not create `engine/execution/broker_oanda_rest.py`. (Note: FX-01 owns OANDA read-only *pricing* under `engine/data/`, with credentials registered as `OANDA_ACCESS_TOKEN`/`OANDA_API_KEY` — do not touch those either.)
- Sim cost model — spread-in-pips / swap-carry P&L accounting belongs to FX-07. You may read/pass-through carry/swap fields and a `rollover` flag, but you must NOT implement cost/P&L math here.
- **`engine/execution/broker_sim.py` weight→qty conversion (`broker_sim.py:2388`, `target_qty = (to_w * equity) / float(px_mid)`).** Do **NOT** modify the sim weight→shares math. This is the share-vs-lot conversion seam: FX-05 delegates the eventual "convert weight to FX lots, not shares" change downstream, but it is **explicitly out of scope here** and unowned across the FX series. Your `broker_ibkr_gateway.py` change must consume whatever `qty`/lot context is already attached to the order at the broker boundary — it must NOT reach into the sim qty math. **Flag this orphaned seam in your self-audit as a known cross-prompt gap (NO-GO-pending-owner for the lot-conversion site), but do not implement it.**
- Operator UI / dashboard surfacing of FX — FX-08.
- FX data ingestion (FX-01), feature groups/parity (FX-03), prediction routing/labels/clock authoring (FX-04), and sizing/risk (FX-05). Consume their outputs; do not modify them beyond what is strictly required to read instrument metadata and import FX-04's session-boundary helper.
- Any live order/cancel/replace/flatten implementation. Do not modify the actual `app.placeOrder`/cancel/flatten call mechanics; only change which `Contract` is constructed and add session gating in the policy layer.
- `engine/runtime/storage.py` schema. No new tables/columns.
- The champion/challenger and governance frameworks.

### Verified anchors
(confirmed by reading the repo on 2026-06-23)
- `engine/execution/broker_ibkr_gateway.py:820` — `def _mk_stock_contract(symbol: str)`; `:824` sets `c.secType = "STK"` (also `:825` `c.exchange = "SMART"`, `:826` `c.currency = "USD"`). `_mk_market_order` at `:830`, `_mk_limit_order` at `:840`. **VERIFIED exact.**
- `engine/execution/broker_ibkr_gateway.py` — `_mk_stock_contract(symbol)` is called at **1209, 1729, 2049, 2112** (the four contract-construction sites). **VERIFIED exact.**
- `engine/data/asset_map.py:63` — `def asset_class_for_symbol(symbol) -> str`; returns `"FX"` (`:82`) for FX symbols and supports `ASSET_CLASS_MAP_JSON` override (`:46`). **VERIFIED** — fallback classification hook (FX-02 is canonical; this is the floor).
- `engine/execution/broker_router.py` — dispatch is `_apply_one(name, ...)` (`:1089`); chain comes from `_parse_failover_chain()` (`:718`). Recognized live brokers: `alpaca`/`alpaca_rest`, `ibkr`/`interactivebrokers`/`interactive_brokers`/`ib_gateway`/`ibgateway`/`tws` (`:1158-1160`, `:1238`). Gates in the path: `_execution_gate_or_block` (`:452`), `_real_trading_gate_or_block` (`:485`, called at `:1164`), `live_broker_environment_contract` (imported `:41`, called `:1359`). Public entry: `apply_new_portfolio_orders_router(...)` (`:1287`); `_execution_gate_or_block` called at `:1373`. **VERIFIED.**
- `engine/execution/broker_failover_policy.py:61` — `def configured_failover_chain(...)` reads `BROKER_FAILOVER` (`:63`); `def validate_live_failover_chain(...)` at `:77`. **VERIFIED.**
- `engine/execution/execution_decision_engine.py:249` — `def decide_execution_strategy(...) -> Dict[str, Any]` (returns a dict; `PASSIVE_DELAY_MS` at `:19`, env `EXECUTION_PASSIVE_DELAY_MS`). **There is no calendar/session/market-hours/rollover/swap awareness anywhere in the execution timing path** — the gap is the *absence* of any 24/5 session model, not an equity-hours model to replace. Implement session-awareness additively for FX only. **VERIFIED.**
- `engine/execution/execution_policy_engine.py:587` — `def apply_execution_policy(orders=None, *, ..., now_ms=None, ...)`. The module already owns suppression + a structured audit trail: `execution_policy_audit` table is created in-module (`:167`), `log_suppression` is imported from `trade_attribution_ledger` (`:44`) and `evaluate_trade_suppression` from `trade_suppression_engine` (`:42`). **Reuse this existing suppression/audit path — do not add a new table.** **VERIFIED.**
- `engine/execution/broker_oanda_rest.py` — **does not exist**; no OANDA module under `engine/execution/` (OANDA only appears as pricing credentials in `engine/runtime/runtime_bootstrap.py:72` and `services/data_source_manager.py:106`, both as `OANDA_ACCESS_TOKEN`/`OANDA_API_KEY`). **VERIFIED.**
- `engine/data/prices/fx_clock.py` — **does not exist in the sandbox yet** (FX-04 authors it); treat it as the canonical session-boundary owner per FX-04's prerequisite. If absent at runtime, fall back to env-configured UTC boundaries (below) and flag the missing-canonical-clock dependency in the self-audit. **VERIFIED absent.**
- Money-path / validators: `broker_router.py` is an explicitly-listed money-path file in `tools/pyright_money_path_gate.py:28`; the whole `engine/execution` directory is also gated (`:56`, `:70`) — so `broker_ibkr_gateway.py`, `fx_session.py`, and `execution_policy_engine.py` are covered by the directory entry. Validators present: `tools/syntax_check_workspace.py`, `tools/coverage_gate.py`, `tools/pyright_money_path_gate.py`, `tools/git_worktree_triage.py`. **VERIFIED.**
- Existing broker/router safety tests: `tests/test_broker_router_dry_run_gates.py`, `tests/test_broker_apply_orders_modes.py`. `tests/conftest.py` installs a network/socket guard (`install_socket_guard`, `:40`/`:56`). Tests use `pytest`+`unittest`, mark safety tests with `pytestmark = pytest.mark.safety_critical`. **VERIFIED.**

### Implementation steps
1. **Capture baselines first (so "no regression" is objectively provable).** Before editing, capture the pyright money-path baseline so you can diff against it:
   - `python tools/pyright_money_path_gate.py > /tmp/fx06_pyright_baseline.txt 2>&1 || true` — record the pre-change diagnostic count for `broker_router.py` and `engine/execution`.
   - `python -m pytest tests/test_broker_router_dry_run_gates.py tests/test_broker_apply_orders_modes.py -q > /tmp/fx06_router_baseline.txt 2>&1 || true` — record which (if any) existing broker/router safety tests are already red so you can distinguish a pre-existing failure from a regression you caused. Cite both baselines in your self-audit.
2. **Add FX contract construction in `broker_ibkr_gateway.py`.**
   - Add `def _is_fx_symbol(symbol: str) -> bool` that returns True only when the symbol is FX per FX-02's accessor (prefer FX-02's `is_fx_symbol`/metadata accessor; import lazily to avoid import cycles; fall back to `asset_class_for_symbol(symbol) == "FX"`) **and** the symbol resolves to a base/quote pair.
   - Add `def _mk_fx_contract(symbol: str)` building `Contract()` with `secType="CASH"`, `exchange="IDEALPRO"`, `c.symbol = base_ccy`, `c.currency = quote_ccy` (IBKR Forex convention). Obtain base/quote from FX-02's parser; only if absent, split the 6-letter pair. Keep it a small pure helper mirroring `_mk_stock_contract` shape.
   - Add `def _mk_contract_for_symbol(symbol: str)` that returns `_mk_fx_contract(symbol)` when `_is_fx_symbol(symbol)` else `_mk_stock_contract(symbol)`.
   - Replace the four `_mk_stock_contract(symbol)` call sites (1209, 1729, 2049, 2112) with `_mk_contract_for_symbol(symbol)`. Do **not** otherwise alter the surrounding gating, audit, idempotency, or placement logic. The live placement mechanics stay identical — only the constructed contract changes.
3. **FX-aware routing in `broker_router.py`.**
   - Add a helper `def _fx_capable_broker(chain: list[str]) -> Optional[str]` returning `"ibkr"` if any IBKR alias is present in the configured chain, else None.
   - Add `def _batch_has_fx(orders) -> bool` using FX-02's accessor (fallback `asset_class_for_symbol`).
   - In `apply_new_portfolio_orders_router(...)`, when `_batch_has_fx(override_orders)` is true and an FX-capable broker exists, reorder/prefer that broker **at the front of the existing validated chain** — but route through the *same* `validate_live_failover_chain` / `live_broker_environment_contract` / `_execution_gate_or_block` / `_real_trading_gate_or_block` / pre-live reconcile / options-block / RL-source-block logic. Never construct a path that skips a gate. If no FX-capable broker is available, fall back to existing behavior (do not silently route FX to an FX-incapable broker outside the existing failover semantics).
   - Keep `engine/execution/broker_router.py` pyright-clean (money-path gated): full type hints on new helpers, no new `Any`-leaks that regress the baseline captured in step 1.
4. **24/5 session + rollover/swap timing.**
   - Add a new pure module `engine/execution/fx_session.py` with no DB/network/global side effects. **Derive session boundaries from FX-04's `engine/data/prices/fx_clock.py`** (import its boundary helper/constants; if `fx_clock.py` is absent at runtime, fall back to env-configured UTC boundaries and flag it). Expose:
     - `def fx_session_state(symbol: str, now_ms: int) -> dict` returning `{ "is_fx": bool, "session": "open"|"weekend_closed"|"rollover", "is_open": bool, "in_rollover_window": bool, "next_open_ms": int|None }`. Encode the standard 24/5 FX week using FX-04's boundaries (open ~Sunday 17:00 ET, close ~Friday 17:00 ET; document the UTC equivalence) and a configurable daily rollover window (default ~21:00–22:00 UTC) via env knobs (`FX_WEEK_OPEN_*`, `FX_WEEK_CLOSE_*`, `FX_ROLLOVER_*`) with safe defaults. Do **not** compute swap/carry P&L (FX-07) — only flag `in_rollover_window` and pass through any provided `swap`/`carry` fields.
     - `def fx_timing_adjustment(symbol: str, now_ms: int, base_decision: dict) -> dict` that, for FX orders only: (a) when `weekend_closed`, annotates `fx_session_blocked=True` plus a reason so the policy layer suppresses placement; and (b) when `in_rollover_window`, applies a more-passive / extra-delay bias to avoid the widened-spread rollover spike. Equities pass through unchanged. (Note `base_decision` is the `Dict[str, Any]` shape returned by `decide_execution_strategy`.)
   - **Integrate in `execution_policy_engine.py::apply_execution_policy(...)`:** for each order, if FX, call `fx_timing_adjustment` and apply its annotations to the shaped order metadata; if `fx_session_blocked`, suppress that order from the execution-ready output **via the existing suppression/audit path** (use the already-imported `log_suppression` / `evaluate_trade_suppression` style and the existing `execution_policy_audit` table — do NOT add a table or column). Guard with `EPE_FX_SESSION_ENFORCE` (default on) so it can be disabled in tests for non-FX baselines. Non-FX orders must take an identical code path to today (assert via golden test).
5. **Docs.** Update `engine/execution/README.md`: add bullets for the IBKR CASH/IDEALPRO FX contract path, the FX-capable (`ibkr`) routing preference, and the `fx_session.py` 24/5 + rollover module (noting it derives boundaries from FX-04's `fx_clock.py`). State explicitly that **OANDA execution is NOT implemented in this task — IBKR CASH/IDEALPRO is the FX execution path** — and that FX cost/P&L lives in FX-07 and UI in FX-08.
6. **No schema change.** Confirm none of the above alters `engine/runtime/storage.py`. FX session suppression reuses the existing `execution_policy_audit` table + `log_suppression` helper; no new persistence.

### Tests to add
Place under `tests/`, `unittest.TestCase`-style with `pytestmark = pytest.mark.safety_critical`, fully mocked (no sockets, no real ibapi calls):
- `tests/test_fx_ibkr_contract_construction.py` — `_mk_fx_contract("EURUSD")` yields `secType=="CASH"`, `exchange=="IDEALPRO"`, `symbol=="EUR"`, `currency=="USD"`; `_mk_contract_for_symbol` returns CASH for an FX symbol and STK for `"AAPL"`; `_is_fx_symbol` is True for FX pairs and False for equities/unknown. Verify FX-02's accessor is consulted (patch it and assert the dispatch follows it).
- `tests/test_fx_ibkr_call_sites_use_dispatcher.py` — assert (via dispatcher-level/source-level test, since the four call sites also run under live mode) that `_mk_contract_for_symbol` is what builds the contract: an FX symbol constructs CASH, an equity constructs STK, **all with `dry_run=True` / live placement mocked** — assert no live `placeOrder` is invoked. (Residual: a source/dispatcher-level test does not exercise the live, non-dry_run branches of those four sites — call this out in the self-audit as an accepted GAP; the contract change is byte-identical regardless of mode.)
- `tests/test_fx_broker_routing.py` — with a chain containing `ibkr`, an FX batch prefers it at the chain front; with an equity batch, routing is unchanged; routing never bypasses gates (assert `validate_live_failover_chain` / `_execution_gate_or_block` / `_real_trading_gate_or_block` are still consulted via mocks); no FX-capable broker → safe fallback. All `dry_run=True`.
- `tests/test_fx_session.py` — `fx_session_state` classifies a Wednesday-midday timestamp as open, a Saturday timestamp as `weekend_closed` with `is_open=False`, and a timestamp inside the rollover window as `in_rollover_window=True`; env knob overrides respected; function is pure (same input → same output, no side effects). Pin at least one boundary case to a known DST date so the FX-04-derived ET↔UTC equivalence is actually exercised (not a fixed-offset approximation).
- `tests/test_fx_session_policy_integration.py` — `apply_execution_policy` suppresses an FX order during `weekend_closed` (and records the suppression via the existing audit path), biases passive/adds delay during rollover, and leaves an equity order byte-for-byte identical to the pre-change path (golden compare with `EPE_FX_SESSION_ENFORCE` on and a non-FX order). Capture the equity golden from the current behavior so the comparison proves match-to-pre-change, not just self-consistency.

### Definition of Done
- [ ] `engine/execution/broker_ibkr_gateway.py` has `_mk_fx_contract`, `_is_fx_symbol`, `_mk_contract_for_symbol`; all four former `_mk_stock_contract(symbol)` call sites (1209/1729/2049/2112) now call `_mk_contract_for_symbol(symbol)`; equity behavior unchanged. FX classification/parse goes through FX-02's accessor (fallback only if absent).
- [ ] FX symbols build `secType="CASH"`/`exchange="IDEALPRO"` contracts; equities still build `secType="STK"`.
- [ ] `broker_router.py` prefers `ibkr` for FX batches at the front of the validated chain without bypassing any existing gate or `validate_live_failover_chain`; equity routing unchanged; router pyright money-path diagnostics not increased over the step-1 baseline.
- [ ] A 24/5 FX session module (`engine/execution/fx_session.py`) exists, is pure, derives its boundaries from FX-04's `fx_clock.py` (single source of truth; documented ET↔UTC equivalence), and is integrated into `apply_execution_policy` for FX orders only; weekend-closed FX orders are suppressed via the existing audit/suppression path; rollover-window FX orders get a passive/delay bias; equities are byte-for-byte unchanged.
- [ ] OANDA execution adapter NOT created; README states IBKR CASH/IDEALPRO is the FX execution path.
- [ ] **No live order/cancel/replace/flatten path is reachable in any new code or test.** All exercised order paths are `dry_run=True`/sim/shadow with mocks. No real socket opened in tests.
- [ ] No real broker secret value is logged, returned, or committed.
- [ ] No `engine/runtime/storage.py` schema change; FX suppression reuses `execution_policy_audit` + `log_suppression`.
- [ ] `engine/execution/README.md` updated.
- [ ] The orphaned `broker_sim.py:2388` weight→lots conversion seam is left untouched and explicitly flagged in the self-audit (NO-GO-pending-owner for that cross-prompt seam).
- [ ] All new tests pass; the two existing broker/router safety tests still pass (vs the step-1 baseline); validators below exit 0 (or pre-existing reds are explicitly distinguished from regressions using the captured baselines).

### Validation commands
Run from `/home/david/gitsandbox/system/system` (expected exit code in parentheses):
- `python -m pytest tests/test_fx_ibkr_contract_construction.py tests/test_fx_ibkr_call_sites_use_dispatcher.py tests/test_fx_broker_routing.py tests/test_fx_session.py tests/test_fx_session_policy_integration.py -q` → 0
- `python -m pytest tests/test_broker_router_dry_run_gates.py tests/test_broker_apply_orders_modes.py -q` → 0 (existing broker/router safety tests must still pass — compare against `/tmp/fx06_router_baseline.txt`)
- `python tools/syntax_check_workspace.py` → 0
- `python tools/pyright_money_path_gate.py` → 0 (no new diagnostics over `/tmp/fx06_pyright_baseline.txt`; `broker_router.py` and the `engine/execution` dir are gated)
- `python tools/coverage_gate.py` → 0 (do not introduce new zero-covered execution modules; `fx_session.py` must be covered by its tests)
- `python tools/git_worktree_triage.py` → 0
- `git status --short --untracked-files=all` → human-review (not an exit-code gate): only the intended files should appear — `engine/execution/broker_ibkr_gateway.py`, `engine/execution/broker_router.py`, `engine/execution/execution_policy_engine.py`, new `engine/execution/fx_session.py`, the five new tests, and `engine/execution/README.md`. Nothing under `engine/runtime/storage.py`, and **no** `broker_oanda_rest.py` and **no** change to `engine/execution/broker_sim.py`.

### Self-audit & NO-GO
After implementing, audit your own work and report, in your final message:
1. The exact list of files changed/added and one line each on why. Cite the step-1 pyright and router-test baselines and confirm no regression against them.
2. Proof that FX safety is enforced in **runtime code**, not only tests/docs: cite the line where the IBKR call sites now dispatch via `_mk_contract_for_symbol`, the router lines where FX preference is applied without skipping gates (name the gate functions still invoked), and the `apply_execution_policy` lines where FX session suppression/bias is enforced via the existing audit path. Confirm equities take an unchanged path.
3. An explicit statement that **no live order/cancel/replace/flatten call is reachable** in any new code or test, and that no test opens a real broker socket (mocks only). Note the accepted GAP that the dispatcher source-test does not exercise the four sites' live (non-dry_run) branches, and why the contract change is mode-invariant.
4. Confirmation that no real secret value can be logged or returned.
5. Confirmation that no `engine/runtime/storage.py` schema change was made and that FX suppression reuses `execution_policy_audit` + `log_suppression`.
6. Confirmation that FX-04's `fx_clock.py` is treated as the canonical session-boundary source of truth, and exactly how `fx_session.py` derives from it (and what happens if it is absent at runtime).
7. Confirmation that OANDA was NOT implemented (IBKR-CASH-only) per the scope decision.
8. Explicit flag that the `broker_sim.py:2388` weight→lots conversion seam is left untouched and is an unowned cross-prompt gap (NO-GO-pending-owner for that site), per the out-of-scope contract.
If any Definition-of-Done item is not fully implemented, or any gate could be bypassed, or any live path is reachable, declare **NO-GO** and enumerate the exact remaining work instead of claiming success.

---

## FX-07 — FX backtest realism, governance, profitability evidence

**Mission:** Add FX-realistic transaction costs (per-pair spread in pips, overnight swap/carry, weekend-gap risk) to the offline broker-sim and CPCV cost path, run FX challengers through the existing CPCV / PBO / deflated-Sharpe / Reality-Check / promotion gates net of those costs, and produce a per-pair/per-factor pass/fail profitability report — promoting nothing that fails and never bypassing the champion/challenger governance path.

**Prerequisites (merge-order):**
- **FX-02 — HARD prerequisite (must be merged first).** FX-02 is the single source of truth for FX symbol semantics: it provides the FX instrument model (canonical stored pair symbols, an `asset_class`/instrument-class tag such as `FX_MAJOR`/`FX_MINOR`, pip-size/quote metadata) and the accessor/parser (`engine/data/fx_instrument.py`, e.g. `get_instrument_metadata(con, symbol)`, `is_fx_symbol(symbol)`, `parse_fx_symbol(symbol)`). This is FX-07's real code dependency for pip-size and symbol normalization.
- **FX-03, FX-04, FX-05, FX-06 — SOFT prerequisites (not imported; defensive fallbacks specified below).** FX-07 consumes only *predictions / realized FX returns* and *order/intent shapes* — defensively, with fallbacks — so it does not import FX-03/04/05/06 symbols and does not force them ahead of it in the merge order. The one exception is the session-boundary clock (see below): if FX-04's `engine/data/prices/fx_clock.py` is present, the weekend-gap logic MUST derive its Fri→Sun roll boundary from it (canonical session-clock owner) rather than inventing a third boundary model.

**Defensive-fallback contract (when a prerequisite is not yet merged in this sandbox):** Do NOT invent a prerequisite's public API. Prefer FX-02's accessor for all FX metadata (pip size, canonical symbol form, FX detection). If `engine/data/fx_instrument.py` (or its accessor) is absent, detect the FX instrument defensively (symbol matches a 6-letter currency-pair pattern, with or without a `_` separator; or an explicit `asset_class`/`instrument_class` field whose upper-stripped value starts with `FX`), normalize the symbol to the canonical underscore form yourself, fall back to the local pip-size table, fall back to FX-04's clock only if present, and fall back to existing non-FX behavior when no FX context is detectable. Mark every such assumption in a code comment AND in the NO-GO section. This workstream provides: FX cost realism in the sim + cost config, FX cost coverage in the gate stack, and the profitability evidence report.

**Global constraints (restate, condensed):**
- Target repo: `/home/david/gitsandbox/system/system`. All paths below are relative to that directory. This is the only repo.
- Never print, log, echo, commit, or test with real secret values. Use generated canary strings in any test that touches config/credentials and assert they never appear in logs or outputs.
- Never call live broker order/cancel/replace/flatten paths. All FX work here is simulated/offline only. No network, no live credentials. If a step seems to need them, stub/mock and mark the gap — do not fake success.
- Build on existing architecture; never replace it. Respect the model-vs-runtime contract (models propose intent; runtime owns safety gates). Preserve train/serve feature parity via `engine/strategy/feature_registry.py`. Do NOT add any path that bypasses champion/challenger promotion governance.
- `engine/runtime/storage.py` schema changes are high blast radius: prefer NO schema change. If unavoidable, add a migration plus tests. This workstream is designed to require no schema change.
- "Profitable" means net of realistic FX costs (spread in pips + swap/carry, with weekend-gap risk modeled) proven through the existing statistical/backtest gates — never asserted by hand.

### In scope
- Extend the offline FX cost model in `engine/execution/broker_sim.py` (`_offline_ac_cost_components`, consumed by `simulate_weight_order_batch`) so that, when an FX instrument/asset-class is in effect, it adds: (a) per-pair spread converted from pips to bps, (b) per-pair overnight swap/carry applied for overnight/weekend holds, (c) a weekend-gap risk surcharge for positions held across the Fri→Sun roll.
- Add FX cost defaults/resolution to the CPCV cost config in `engine/strategy/cpcv.py` (`_default_commission_bps`, `cpcv_cost_config_from_env`) so an `asset_class` whose upper-stripped value starts with `FX` (e.g. `FX`, `FX_MAJOR`, `FX_MINOR`) resolves FX-appropriate spread/swap defaults instead of the equity/crypto branches. Use the same `str(asset_class or "").upper().strip().startswith("FX")` test everywhere so the contract is tag-agnostic between `FX` and `FX_MAJOR`/`FX_MINOR`.
- Add an FX coefficient/spread table (pip spread per pair, swap pips long/short per pair) as a small, well-documented data structure (a new module `engine/execution/fx_costs.py`) with conservative, clearly-labeled placeholder values and an env/config override hook. Mark placeholders as "calibration TODO".
- Thread the FX cost config through `engine/strategy/gated_backtest.py:run_gated_backtest` (it already forwards `cost_config` to `simulate_weight_order_batch`) and through `engine/strategy/cpcv.py:cpcv_backtest` so FX challengers are scored net of FX costs end-to-end into the existing gates (`passes_promotion_gate`, `assess_challenger`).
- Add a reporting helper that runs a provided set of FX pair/factor challengers through the existing gate stack net of FX costs and emits a structured pass/fail report (which pairs/factors pass CPCV/PBO/DSR/Reality-Check/promotion, which fail, and why). Put it in a new module `engine/strategy/fx_profitability_report.py`. The report is data only; it must call the real gate functions, not reimplement them.
- Tests under `tests/` proving: pip→bps conversion (including a pinned known-correct bps value for one specific pair), swap applied on overnight holds, weekend-gap surcharge on Fri→Sun holds, FX cost config resolution, FX costs reduce net Sharpe vs frictionless, a failing FX challenger is NOT promoted, and the report distinguishes pass from fail.
- Docs: a new `docs/FX_COST_REALISM.md` describing the FX cost model, the env knobs, the placeholder-calibration caveat, and how to read the profitability report.

### Out of scope (do not touch)
- Any UI / dashboard / operator surfacing of FX (that is FX-08). Do not edit `dashboard_server.py` or `routes/` or `ops/` UI code.
- Live trading, live broker order/cancel/flatten paths, broker router live execution (`engine/execution/broker_router.py` live paths). No network calls.
- The FX instrument model itself, FX feature groups, FX labels, FX sizing, FX execution session (FX-02..FX-06). Consume their outputs; do not redefine them. In particular, do NOT re-implement FX symbol parsing or pip-size derivation — consume FX-02's accessor (`get_instrument_metadata`/`is_fx_symbol`/`parse_fx_symbol`); the local tables in `fx_costs.py` are fallback-only.
- The broker-sim weight→qty/lots conversion (`engine/execution/broker_sim.py` around line 2388, `target_qty = (to_w * equity) / px_mid`) — that handoff belongs to FX-06, not here. FX-07 only touches the *cost* path (`_offline_ac_cost_components`), never the qty/lot math.
- The session-clock boundary model: if FX-04's `engine/data/prices/fx_clock.py` exists, derive the Fri→Sun roll boundary from it; do NOT author a new canonical clock here.
- `engine/runtime/storage.py` schema and migrations (no schema change intended here).
- The statistical math inside the gates (`deflated_sharpe_ratio`, `white_reality_check`, `compute_pbo`, BH-FDR, Harvey-Liu-Zhu). Do not modify the gate algorithms — only feed them cost-adjusted FX returns.
- Equity/crypto cost behavior and their existing tests must remain byte-for-byte unchanged in output when `asset_class` is not FX.

### Verified anchors
(confirmed by reading the repo at `/home/david/gitsandbox/system/system` on 2026-06-23)
- `engine/execution/broker_sim.py:387-389` — knobs `BROKER_SPREAD_BPS=2.0`, `BROKER_SLIPPAGE_BPS=1.0`, `BROKER_FEE_BPS=0.5` (env-overridable module constants).
- `engine/execution/broker_sim.py:1135` — `def _exec_px(mid_px, side, trade_notional=0.0, equity=0.0, slip_bps_override=None, spread_bps_override=None)` (live-fill exec price; bps-on-price spread+slippage at `:1157-1159`). Live-path only — do NOT add overnight/weekend logic here.
- `engine/execution/broker_sim.py:1298` — `def _offline_ac_cost_components(turnover, *, cost_config=None, cost_model=None)`; reads `commission_bps` at `:1327`, returns dict with `commission_bps`, `half_spread_bps`, `temporary_impact_bps`, `total_cost_bps`, `cost_return`. THIS is the offline cost path to extend for FX.
- `engine/execution/broker_sim.py:1340` — `def simulate_weight_order_batch(*, orders, realized_returns_by_symbol, previous_weights=None, cost_config=None, cost_model=None)` calls `_offline_ac_cost_components` at `:1372` and returns `gross_return`, `cost_return`, `net_return`, `turnover`, `costs`.
- `engine/execution/broker_sim.py:~2388` — `target_qty = (to_w * equity) / float(px_mid)` (the weight→qty conversion). OUT OF SCOPE here — owned by FX-06; do not touch.
- `engine/strategy/cpcv.py:165` — `def _default_commission_bps(asset_class: str) -> float` has only `CRYPTO` and equity branches; NO FX branch (this is the drift/gap to fix).
- `engine/strategy/cpcv.py:179` — `def cpcv_cost_config_from_env(config: Mapping[str, Any] | None = None) -> Dict[str, Any]` resolves `asset_class` (default `US_EQUITY`), `commission_bps`, `half_spread_bps`, `notional`, `adv`, `sigma_daily`, `participation`.
- `engine/strategy/cpcv.py:205` — `def _cost_components_for_turnover(turnover, *, cost_config, cost_model=None)`; `:248` `def _apply_transaction_costs_to_returns(...)` (calls `cpcv_cost_config_from_env` at `:259`); `:558` `def cpcv_backtest(...)` (accepts `cost_config`, calls `cpcv_cost_config_from_env` at `:675`).
- `engine/strategy/gated_backtest.py:114` — `def run_gated_backtest(...)` forwards `cost_config` into `simulate_weight_order_batch` at `:244`.
- `engine/strategy/statistical_gates.py:344` `white_reality_check`, `:619` `deflated_sharpe_ratio(sharpe, n_trials, n_obs, skew, kurt)`, `:676` `def passes_promotion_gate(returns, n_competing_trials, config=None, models_returns=None) -> tuple[bool, dict]`.
- `engine/strategy/promotion_guard.py:912` — `def assess_challenger(*, model_id, ..., challenger_returns, champion_returns, ...) -> Tuple[bool, Dict]` (non-bypassable Reality-Check + BH-FDR/HLZ gate).
- `engine/strategy/champion_manager.py:25` imports `assess_challenger, evaluate_statistical_promotion_gate, promotion_allowed`; `:1772` `_promotion_return_series`; `:2198` calls `assess_challenger(... challenger_returns=challenger_returns ...)`. This is the governance path — feed it cost-adjusted FX returns; do not bypass it.
- `engine/execution/cost_models/almgren_chriss.py:14` — `_DEFAULT_OVERRIDES: Dict[str, tuple[float, float]]` has only `US_EQUITY` (line 15) and `EQUITY` (line 16); no FX entry (gap). `:62` `asset_class_coefficients` field; `:64` `coefficients_for(self, asset_class=None)`; `:71` `components_bps(...)`.
- Tests: `tests/test_cpcv_cost_realism.py`, `tests/test_broker_sim_contract.py`, `tests/test_gated_backtest.py`, `tests/test_promotion_guard_fdr.py`, `tests/test_champion_promotion_identity.py` exist and show the patterns to mirror. Pytest config in `pyproject.toml:43` (`testpaths = ["tests"]`); `:46` `addopts = "-q --strict-markers --strict-config --durations=20"`; `:53` marker `safety_critical` defined for money-path control tests.
- Pyright money-path gate `tools/pyright_money_path_gate.py`: the explicit per-file list is `TARGET_PATHS` (lines 21-52), with `engine/strategy/champion_manager.py` at line 41. `engine/execution/broker_sim.py` is NOT in the explicit file list — it is covered only by the directory-level `HIGH_RISK_ROOTS` entry `engine/execution` (lines 54-60; `engine/execution` at line 56). Keep new code in `broker_sim.py` (directory-covered) and any edits to `champion_manager.py` (explicitly listed) fully type-annotated; before claiming "not regressed", capture a pre-change baseline (see Validation).

### Implementation steps
1. **Create `engine/execution/fx_costs.py`** — a self-contained FX cost data + helper module (no imports from broker_sim/cpcv to avoid cycles):
   - `FX_PIP_SPREAD: dict[str, float]` — typical bid/ask spread in pips per pair, keyed by the **canonical underscore form** (e.g. `EUR_USD`, `USD_JPY`, `GBP_USD`, `AUD_USD`, `USD_CHF`, `USD_CAD`, `NZD_USD`, and a few minors). Conservative placeholder values; add a module-level docstring stating these are CALIBRATION TODO placeholders, not live broker quotes.
   - `FX_SWAP_PIPS_LONG`/`FX_SWAP_PIPS_SHORT: dict[str, float]` — overnight swap/carry in pips per pair per night (signed; long and short differ), keyed by canonical underscore form. Placeholder + TODO.
   - `FX_PIP_SIZE: dict[str, float]` — **fallback only.** pip size per pair (0.0001 for most, 0.01 for JPY crosses). FX-07 MUST first try to read pip size from FX-02's accessor (`engine.data.fx_instrument.get_instrument_metadata(con, symbol)` → `pip_size`); use this dict only when FX-02's accessor is unavailable or returns no value, and note that fallback in a comment and in NO-GO. Do not duplicate FX-02's authority.
   - `def normalize_fx_symbol(symbol: str) -> str` — normalize any accepted form (`EURUSD`, `EUR/USD`, `EUR_USD`) to the canonical underscore form used by the tables. Prefer FX-02's `parse_fx_symbol`/accessor for the base/quote split when present; the local normalizer is a fallback so cost-table lookups never silently miss a differently-cased symbol.
   - `def is_fx_symbol(symbol: str) -> bool` and `def is_fx_asset_class(asset_class: str | None) -> bool` — prefer FX-02's `is_fx_symbol` when importable; otherwise fall back to the 6-letter currency-pair pattern. `is_fx_asset_class` uses `str(asset_class or "").upper().strip().startswith("FX")`.
   - `def pip_spread_bps(symbol: str, *, half: bool = True) -> float` — convert the pair's pip spread to bps of price: `bps = (pips * pip_size / mid_px_proxy) * 10000`. Look up the pair via `normalize_fx_symbol`. Since the offline cost path is price-agnostic (works in return space), use a documented mid-price proxy per pair (a `FX_REF_PRICE` table, clearly labeled CALIBRATION-TODO placeholder) so the pip→bps conversion is deterministic; return half-spread when `half=True`. Document that this is a proxy and real conversion uses live mid in the live path.
   - `def swap_bps(symbol: str, side_sign: float, nights: int) -> float` — swap pips → bps over `nights` overnight holds (long vs short sign-aware), pair looked up via `normalize_fx_symbol`.
   - `def weekend_gap_bps(symbol: str, *, crosses_weekend: bool) -> float` — a conservative surcharge (bps) added when a position is held across the Fri→Sun roll; 0 otherwise. Make the surcharge an env-overridable scalar applied to a per-pair volatility proxy. If FX-04's `engine.data.prices.fx_clock` is importable, the *caller* (step 2) should compute `crosses_weekend` from that clock; this helper only applies the surcharge given the flag.
   - All three knobs (`FX_PIP_SPREAD_OVERRIDE_JSON`, `FX_SWAP_PIPS_OVERRIDE_JSON`, `FX_WEEKEND_GAP_BPS`) overridable via env with safe parsing (mirror the `_safe_float_env`/safe-JSON style already in `cpcv.py`). Never read secrets; malformed input must be ignored (fall back to defaults), never raised.

2. **Extend the offline cost path in `engine/execution/broker_sim.py:_offline_ac_cost_components`** (line 1298):
   - Accept FX context from `cost_config`: `asset_class`, optional `symbol`, optional `nights` (overnight holds since last rebalance, default derived or 1), and optional `crosses_weekend` flag. If FX-04's `fx_clock` is importable and `crosses_weekend` is not explicitly supplied, you may derive it from the clock for the rebalance window; otherwise default to the explicit flag (or `False`).
   - When `engine.execution.fx_costs.is_fx_asset_class(cost_config.get("asset_class"))` is true, compute the half-spread component from `pip_spread_bps(symbol)` instead of the equity `half_spread_bps` default, and ADD a new `swap_carry_bps` term (from `swap_bps`) and a `weekend_gap_bps` term (from `weekend_gap_bps`) into `total_cost_bps`. Return them as explicit named keys in the result dict (`fx_pip_spread_bps`, `swap_carry_bps`, `weekend_gap_bps`) so they are auditable and testable.
   - Keep the equity/crypto branch unchanged: if not FX, behavior and return keys must be identical to today (existing `tests/test_cpcv_cost_realism.py` and `tests/test_broker_sim_contract.py` must still pass unchanged).
   - Do NOT touch `_exec_px` (live fill path) or the weight→qty conversion near line 2388. Annotate types; this file is directory-covered by the pyright money-path gate.

3. **Add the FX branch to cost config in `engine/strategy/cpcv.py`:**
   - In `_default_commission_bps` (line 165): add a branch — when `str(asset_class or "").upper().strip().startswith("FX")`, return an FX commission default (most retail FX has spread-as-cost and ~0 explicit commission, but keep an env knob `CPCV_FX_COMMISSION_BPS` defaulting to a small conservative value). Use the same `startswith("FX")` test so `FX`, `FX_MAJOR`, and `FX_MINOR` all route here.
   - In `cpcv_cost_config_from_env` (line 179): when the asset class starts with `FX`, set `half_spread_bps` from the FX pip-spread proxy (via `engine.execution.fx_costs.pip_spread_bps`) when a `symbol` is present in the incoming config, and carry through `symbol`, `nights`, `crosses_weekend` into the returned config so they reach `_offline_ac_cost_components`. Preserve all existing keys for non-FX.
   - Optionally register FX coefficients in `AlmgrenChrissCost` (`engine/execution/cost_models/almgren_chriss.py` `_DEFAULT_OVERRIDES`, line 14) with conservative FX `(eta, gamma)` impact coefficients so the temporary-impact term is sensible for FX; if you add them, add a matching test and keep equity/crypto unchanged.

4. **Build `engine/strategy/fx_profitability_report.py`:**
   - `def evaluate_fx_challengers(challengers, *, n_competing_trials, cost_config_base=None) -> dict` where each challenger provides `pair`/`symbol`, `factor` label, `predictions`, `realized_returns`, optional `sample_times_ms`. For each: normalize the symbol via `engine.execution.fx_costs.normalize_fx_symbol`, build an FX `cost_config` (asset_class FX, symbol = normalized pair), run it through `engine.strategy.gated_backtest.run_gated_backtest` (or `cpcv.cpcv_backtest` for the CPCV/PBO path) to get net-of-FX-cost returns, then call the REAL gates: `engine.strategy.statistical_gates.passes_promotion_gate(net_returns, n_competing_trials, ...)` for DSR/HLZ/FDR/Reality-Check, and surface `compute_pbo` from `cpcv` when CPCV score matrices are available.
   - Emit `{ "pairs": {pair: {factor: {"passed": bool, "net_sharpe": float, "deflated_sharpe": float, "pbo": float, "reality_check_passed": bool, "frictionless_sharpe": float, "cost_drag_bps": float, "reason": str}}}, "summary": {"n_pass": int, "n_fail": int} }`.
   - The report ONLY reports; it must not promote. Promotion remains via `champion_manager`→`assess_challenger`. Add a docstring stating that anything in this report still must clear `assess_challenger` before any promotion, and that the module imports only sim/gate modules (never a live broker path).

5. **Wire-through sanity (no governance bypass):** Confirm `run_gated_backtest` and `cpcv_backtest` already accept and forward `cost_config`; pass the FX cost config through them. Do NOT add a new promotion entrypoint. The champion path (`champion_manager.py:2198 assess_challenger`) is unchanged structurally — it consumes cost-adjusted returns produced upstream. Add a code comment at the `_promotion_return_series` / `assess_challenger` call site only if needed to clarify that FX returns must already be net of FX costs (no behavioral change). If you touch `champion_manager.py` at all, keep edits fully type-annotated (it is on the explicit pyright money-path list at line 41).

6. **Docs:** Write `docs/FX_COST_REALISM.md`: the FX cost components (pip spread, swap/carry, weekend-gap), the env knobs and their defaults, the placeholder-calibration caveat (values are conservative TODO placeholders, not broker-calibrated; pip→bps uses a documented mid-price proxy in the offline path), the canonical symbol-normalization rule (cost lookups normalize via FX-02/`normalize_fx_symbol`), how `evaluate_fx_challengers` produces the pass/fail report, and the explicit statement that nothing that fails the gates is promoted and the champion/challenger path is not bypassed.

### Tests to add
- `tests/test_fx_costs_unit.py` — pip→bps conversion (`pip_spread_bps`) including **one pinned, asserted-to-a-tolerance bps value for a specific pair** (e.g. assert `pip_spread_bps("EUR_USD", half=False)` equals the hand-computed `pips * pip_size / FX_REF_PRICE["EUR_USD"] * 10000` to validate the arithmetic, not merely that costs are positive); JPY-pair pip size 0.01 handling; `normalize_fx_symbol` maps `EURUSD`/`EUR/USD`/`EUR_USD` to the same canonical key (so a non-underscore symbol does NOT silently miss the table); `swap_bps` sign-awareness long vs short and scaling by `nights`; `weekend_gap_bps` zero unless `crosses_weekend`; env-override JSON parsing is safe and ignores malformed input; and a canary string placed in an override env var never appears in any returned value or log.
- `tests/test_broker_sim_fx_cost_realism.py` — `_offline_ac_cost_components` with FX `cost_config` returns `fx_pip_spread_bps`, `swap_carry_bps`, `weekend_gap_bps` keys and a higher `total_cost_bps` than the same config with `nights=0`/no weekend; and that a NON-FX `cost_config` yields byte-identical output to current behavior (regression guard). Mark `@pytest.mark.safety_critical`.
- `tests/test_cpcv_fx_cost_config.py` — `cpcv_cost_config_from_env({"asset_class":"FX_MAJOR","symbol":"EUR_USD"})` resolves FX half-spread from pips and carries `symbol`/`nights`/`crosses_weekend`; a bare `"FX"` asset_class routes the same way (assert the `startswith("FX")` contract is tag-agnostic between `FX` and `FX_MAJOR`); `_default_commission_bps("FX_MAJOR")` honors `CPCV_FX_COMMISSION_BPS`; non-FX asset classes are unchanged.
- `tests/test_fx_gated_backtest_net_costs.py` — running `run_gated_backtest` with an FX cost config produces `net_return < gross_return` and lower net Sharpe than the frictionless series for a profitable-gross FX signal; a marginal FX signal that is gross-profitable but cost-negative flips to net-negative.
- `tests/test_fx_profitability_report.py` — `evaluate_fx_challengers` returns a report where a strong synthetic FX signal passes `passes_promotion_gate` net of FX costs and a weak/cost-eaten one fails; assert the failing one has `passed=False` and is reported (not promoted). Include an assertion that the report function never calls any live broker path (it imports only sim/gate modules), and that a `EURUSD`-keyed (non-underscore) challenger is normalized and still hits the cost table.
- `tests/test_fx_no_promotion_bypass.py` — a failing FX challenger fed through `assess_challenger` (using cost-adjusted returns from the FX path) returns `passed=False`, proving governance is not bypassed. Mark `@pytest.mark.safety_critical`.

### Definition of Done
- [ ] `engine/execution/fx_costs.py` exists with pip-spread, swap, weekend-gap helpers, `normalize_fx_symbol`, env overrides, FX-symbol/asset-class detection (preferring FX-02's accessor), and explicit CALIBRATION-TODO docstrings; pip size is read from FX-02 first and the local `FX_PIP_SIZE`/`FX_REF_PRICE` tables are fallback-only.
- [ ] `_offline_ac_cost_components` adds FX pip-spread + swap/carry + weekend-gap to `total_cost_bps`, exposes them as named keys, and leaves non-FX output byte-identical (proven by regression test). `_exec_px` and the weight→qty conversion (~line 2388) are untouched.
- [ ] `cpcv._default_commission_bps` and `cpcv_cost_config_from_env` have an FX branch (via `startswith("FX")`, tag-agnostic for `FX`/`FX_MAJOR`/`FX_MINOR`) resolving FX spread from pips and carrying `symbol`/`nights`/`crosses_weekend`; non-FX unchanged.
- [ ] `engine/strategy/fx_profitability_report.py` runs FX challengers through the REAL `run_gated_backtest`/`cpcv_backtest` + `passes_promotion_gate`/`compute_pbo` net of FX costs and emits a per-pair/per-factor pass/fail report; it normalizes symbols via `normalize_fx_symbol`, never promotes, and never calls live broker paths.
- [ ] FX costs flow end-to-end into the existing gates via `cost_config` with NO new promotion entrypoint and NO bypass of `assess_challenger`/`champion_manager`.
- [ ] All six new test files added under `tests/`, with `safety_critical` markers on the two governance/regression-critical ones; pip→bps arithmetic is pinned by an asserted value; all pass.
- [ ] `docs/FX_COST_REALISM.md` added describing the model, knobs, placeholder caveat, canonical symbol-normalization rule, report usage, and the no-bypass guarantee.
- [ ] No `engine/runtime/storage.py` schema/migration change. No secret value ever printed/logged; canary test passes.
- [ ] New code in money-path-gated files (`broker_sim.py` via the `engine/execution` directory entry; `champion_manager.py` if touched, explicit list) is fully type-annotated; pyright money-path gate is not regressed versus the captured baseline.
- [ ] Existing tests `tests/test_cpcv_cost_realism.py`, `tests/test_broker_sim_contract.py`, `tests/test_gated_backtest.py`, `tests/test_promotion_guard_fdr.py`, `tests/test_champion_promotion_identity.py` still pass unchanged.

### Validation commands
Run from `/home/david/gitsandbox/system/system` (expected exit code in parentheses).
First capture a money-path-types baseline so "not regressed" is objective:
- `python tools/pyright_money_path_gate.py > /tmp/pyright_fx07_baseline.txt 2>&1; echo "baseline exit: $?"` (run BEFORE any edit; this is the comparison point, not a pass/fail gate)

Then, after implementation:
- `python -m pytest tests/test_fx_costs_unit.py tests/test_broker_sim_fx_cost_realism.py tests/test_cpcv_fx_cost_config.py tests/test_fx_gated_backtest_net_costs.py tests/test_fx_profitability_report.py tests/test_fx_no_promotion_bypass.py -q` -> 0
- `python -m pytest tests/test_cpcv_cost_realism.py tests/test_broker_sim_contract.py tests/test_gated_backtest.py tests/test_promotion_guard_fdr.py tests/test_champion_promotion_identity.py -q` -> 0  (no regression)
- `python -m pytest tests/ -m safety_critical -q` -> 0
- `python -m pytest tests/ --collect-only -q` -> 0  (collection clean, strict markers)
- `python tools/pyright_money_path_gate.py` -> 0  (and diff against `/tmp/pyright_fx07_baseline.txt` to confirm no NEW diagnostics; if the baseline run itself was non-zero, report the pre-existing diagnostics and prove your edits added none)
- `python tools/validate_repo.py` -> 0  (canonical repo validation; run without `--live`)
- `python tools/git_worktree_triage.py` -> 0
- `git status --short --untracked-files=all`  (review every added/changed path by hand — `git status` exits 0 regardless, so this is a human-review step, not an exit-code gate; expect ONLY the new `engine/execution/fx_costs.py`, `engine/strategy/fx_profitability_report.py`, the six `tests/test_fx_*.py` files, `docs/FX_COST_REALISM.md`, and the targeted edits to `engine/execution/broker_sim.py`, `engine/strategy/cpcv.py`, and optionally `engine/execution/cost_models/almgren_chriss.py`)

### Self-audit & NO-GO
After implementation, audit your own work and report:
1. **Exact files changed and why** — list every added/modified path from `git status --short --untracked-files=all` and one sentence per file justifying it; confirm you did NOT edit UI (FX-08), live broker, the weight→qty conversion (broker_sim ~2388, FX-06), storage schema, or the gate math.
2. **Enforcement is in runtime code, not just tests/docs** — show the concrete lines where FX costs enter `total_cost_bps` (in `_offline_ac_cost_components`) and where the FX cost config is resolved (`cpcv_cost_config_from_env`), and confirm the profitability report calls the REAL gate functions rather than reimplementing them. Confirm there is NO new promotion path and that `assess_challenger`/`champion_manager` remain the only promotion authority.
3. **No-bypass proof** — point to `tests/test_fx_no_promotion_bypass.py` showing a failing FX challenger is rejected by `assess_challenger`.
4. **Honesty about placeholders/limits** — explicitly state that the pip-spread/swap/weekend-gap numbers and the `FX_REF_PRICE` mid-price proxies are conservative CALIBRATION-TODO placeholders (not broker-calibrated), and that the pip→bps conversion uses the documented proxy in the offline path (and is pinned by the asserted test value). State which symbol-convention/pip-size source actually fired: FX-02's accessor (if `engine/data/fx_instrument.py` was present) or the local fallback tables. If FX-02..FX-06 artifacts were missing in the sandbox and you used defensive fallbacks (including a fallback session-boundary instead of FX-04's `fx_clock.py`), list each assumption.
5. **Docs updated** — confirm `docs/FX_COST_REALISM.md` exists and describes knobs, the canonical normalization rule, and the no-bypass guarantee.
6. **Pyright baseline diff** — report the baseline exit code and confirm your edits introduced no new money-path diagnostics (or, if the baseline was already red, that none of the new diagnostics are in your changed files).
7. If ANY Definition-of-Done item is not fully implemented, or any validation command does not return its expected exit code, declare **NO-GO** and enumerate the precise remaining work. Otherwise declare GO with the validation command results.

---

## FX-08 — FX surfacing in operator UI surfaces

**Mission:** Surface FX end-to-end in the operator UI — FX feed status/test in the data-sources page, FX positions + sleeve exposure + leverage + pip/lot sizing in the dashboard, and FX symbols in the browser terminal with correct pip/price formatting and 24/5 session display — without leaking secrets and without touching backend feature/model/risk logic.

**Prerequisites:** Must be merged first:
- **FX-01 (FX data source + ingestion)** — provides an FX price/quote data source registered in `services/data_source_manager.py` (an FX-class source template + `test_connection` result) and FX rows in the price tables read by the terminal/dashboard APIs. The OANDA pricing feed is default-off / operator-gated, so in this sandbox FX price rows will typically be absent — see the placeholder contract below.
- **FX-05 (currency-aware portfolio sizing + risk)** — provides FX sleeve exposure, leverage, and per-instrument pip/lot sizing fields **AND owns serializing those fields out of the risk-engine `info_json` blob into the dashboard read-model API responses** (`/api/portfolio`, `/api/risk/portfolio`, and/or `/api/ui/metrics`). IMPORTANT: this read-model exposure is FX-05's responsibility, NOT this prompt's. FX-08 is forbidden from adding fields to read-model payloads (see Out of scope). If the FX-05 fields are not present in the read payloads you fetch (because FX-05's read-model serialization is not merged), that is the expected degraded state — render the placeholder and record the gap; do not edit a backend read API to add them.
- **FX-06 (FX execution + positions)** — provides FX positions in the broker/terminal position read models (e.g. `broker_positions`, `/api/terminal/positions`, `/api/broker`).
- **Canonical-clock coupling (FX-04):** the single session-boundary source of truth for the whole FX system is FX-04's `engine/data/prices/fx_clock.py` (24/5 calendar, weekly open/close). FX-08's browser-side `ui/fx_session.js` is a presentation MIRROR of that calendar and MUST use the same boundary model and the same configurable open/close hours so the UI never disagrees with FX-04 (labels) or FX-06 (execution session gate) about whether the market is open. Express the boundaries in UTC and make the open/close hours overridable so they can be pinned to FX-04's configured values; do not invent a third, divergent boundary model.

If any prerequisite payload field this prompt expects is absent (because that prompt is not merged, or its exact field names differ), DO NOT invent backend data: render a clearly-labeled "FX data not yet available" placeholder in the UI, read the field defensively with a fallback, and mark the gap in your NO-GO notes rather than fabricating values.

**Expected sandbox outcome (read this before you start):** None of FX-01/FX-04/FX-05/FX-06 may actually be merged in this sandbox, and the OANDA feed is default-off. The realistic outcome is therefore that EVERY FX dashboard/terminal value hits the "data not yet available" placeholder path and the FX watchlist symbols may not appear. That is acceptable and expected — your job is to ship correct, unit-tested formatting/session logic and defensive UI wiring that lights up automatically when the data arrives. Do NOT fabricate FX prices, positions, sleeve, or leverage numbers to make Definition-of-Done checkboxes look satisfied. State plainly in the self-audit which DoD items are placeholder-only because upstream data was absent.

**Global constraints (condensed, restated so this task is self-contained):**
- Target repo is `/home/david/gitsandbox/system/system`. All paths below are relative to that directory. This is the only repo.
- Never print, log, echo, commit, or test with real secret values. Tests must use generated canary strings and assert those canaries never appear in any API response body or UI payload. No credential value may ever be placed into an HTML/JS payload.
- Never call live broker order / cancel / replace / flatten paths. This task is UI-surfacing only and is read-only with respect to trading; do not wire any new mutate/execution action.
- Build on existing architecture; never replace it. Respect the model-vs-runtime contract (models propose, runtime owns gates) — this is a presentation task and must not move any gate logic into the UI.
- `engine/runtime/storage.py` and storage schema are high blast radius. Do NOT change schema or migrations in this task. Read existing read-model APIs only.
- "Profitable" is proven through existing backtest/statistical gates, never asserted in the UI. Do not add any UI claim of profitability.
- Respect the `npm run check:ui` and `npm run test:ui` gates. New JS unit tests and Python contract tests MUST be registered in the gate's allowlists (see Implementation steps) or they will silently not run.

### In scope
- Data-sources page (`ui/data_sources.html`, `ui/data_sources.js`, `ui/data_sources.css`): display the FX feed's status and an operator-runnable connectivity test using the EXISTING `/api/data_sources/test` and `/api/data_sources` routes — no new routes, no new backend logic.
- Dashboard (`ui/dashboard.html`, `ui/dashboard.js`): in the existing "Positions & Exposure" card (`positionsExposureSummaryCard`), surface FX positions, FX sleeve exposure, FX leverage, and pip/lot sizing read from existing dashboard read APIs.
- Browser terminal (`ui/terminal/terminal.html`, `ui/terminal/terminal.js`): make FX symbols first-class — correct pip-aware price/qty formatting (e.g. 5-decimal majors, 3-decimal JPY pairs, lot-aware quantity) and a 24/5 session status indicator. Reads only from existing `/api/terminal/*` endpoints.
- New importable ES helper module(s) under `ui/` containing the PURE FX formatting/session logic so they can be unit-tested (the existing `ui/terminal/terminal.js` and `ui/data_sources.js` are side-effect entry modules with NO exports and cannot be unit-tested directly).
- New JS (`.mjs`) and Python (`.py`) tests, registered in the UI gate allowlists.
- Doc updates in `ui/README.md` and `docs/README_OPERATOR_GUIDE.md`.

### Out of scope (do not touch)
- Any backend feature/model/risk/execution/sizing logic. Do NOT edit `engine/strategy/**`, `engine/risk/**`, `engine/execution/**`, `engine/runtime/storage.py`, or add fields to read-model payloads. The FX numbers must already exist in the payloads (provided by FX-01/FX-05/FX-06, with FX-05 owning the `info_json`→read-model serialization); this task only displays them. If a needed field is missing, render a placeholder and mark a gap — do not add it to the backend here.
- New API routes. Reuse only the verified existing routes listed in Verified anchors.
- `routes/data_sources_routes.py` — read it for the route contract, but do not modify it.
- The data source manager (`services/data_source_manager.py`) — do not modify; FX source registration belongs to FX-01.
- Any broker order/cancel/replace/flatten wiring; any kill-switch or governance change.
- `package.json` dependency/engine changes, `package-lock.json` regeneration.

### Verified anchors
(confirmed by reading the repo on 2026-06-23; line numbers re-verified — use these, not the stale ones from earlier drafts)
- `ui/data_sources.html` — `#sourceCards` (line 91), `#providerAccounts` (line 81), `#detailPane` (line 124), `#sourcesBody` table (line 114); loads `ui/data_sources.js` as a module at line 234.
- `ui/data_sources.js` — `renderProviderAccounts()` (line **473**), `renderSourceCards()` (line **520**), `renderTable()` (line **560**). `renderSourceCards()` renders one `.source-card` per source using `guideForSource(source)` (line 197) and `deriveSourceState(source, template)` (line 209). The module has **NO `export` statements** (verified: it is a side-effect entry module — confirms the need for the separate pure helper modules below).
- `routes/data_sources_routes.py` — `ROUTE_SPECS_DATA_SOURCES` (line 19). Existing routes to reuse: `GET /api/data_sources` (`api_get_data_sources`, line 64), `POST /api/data_sources/test` (`api_post_data_source_test`, line **323**). The test route returns the provider-specific result from `manager.test_connection(...)`; `test_connection` results carry `status`/`ok` keys (verified in `services/data_source_manager.py`, e.g. result dict at lines 320-321 and the `ConnectionTestResult` serialization at lines 487-489). Do NOT modify this file.
- `ui/dashboard.html` — `#positionsExposureSummaryCard` (line 4185); pill row `#positionsGrossPill`/`#positionsNetPill`/`#positionsDrawdownPill`/`#positionsBarrierPill`/`#positionsFreshnessPill` (lines 4189-4193); `#positionsExposureGrid` (line 4237) is an `.opsStatGrid` whose tiles use `.opsStat`/`.opsStatLabel`/`.opsStatValue` markup (template tile at line 4238); `#positionsExposureNotes` (line 4241). Portfolio card `#portfolioCard` at line 4145.
- `ui/dashboard.js` — `loadPositionsExposureScreen()` (line 5058) does a single `Promise.allSettled([...])` (starts line 5063) fetching, in order, `/api/ui/metrics`, `/api/portfolio`, `/api/risk/portfolio`, `/api/risk/summary`, `/api/broker`, `/api/terminal/positions`, `/api/regime/context?symbol=...`. Grid lookup `document.getElementById("positionsExposureGrid")` at line 5059. Notes render via `renderNotes("positionsExposureNotes", ...)` at line 5284.
- `engine/api/api_dashboard_reads.py` — `api_get_portfolio` (line 94) backing `/api/portfolio`. (Read-only; do not modify. Verified it does NOT currently emit FX sleeve/leverage/pip/lot fields — confirming those are placeholder-path until FX-05 wires them in.)
- `engine/terminal/api/api_terminal.py` — `ROUTE_SPECS_TERMINAL` (line 56) including `GET /api/terminal/watchlist` (`api_get_terminal_watchlist`, line 421), `GET /api/terminal/snapshot`, `GET /api/terminal/positions` (`api_get_terminal_positions`, line 512), `GET /api/terminal/orders`, `GET /api/terminal/fills`, `GET /api/terminal/equity`. (Read-only; do not modify.)
- `ui/terminal/terminal.html` — `#symInput` (line 29), `#terminalStatusBanner` (line 79), `#chartTitle` (line 97), `#chartHealth` (line 98), `#acctMeta` (line 111), positions table `#posTbl` (line 159). Loads `ui/terminal/terminal_theme.css` (line 15) and `ui/terminal/terminal.js` (module, line 16).
- `ui/terminal/terminal.js` — controller with helpers `esc` (line 82), `fmtNum` (line 91), `fmtTs` (line 97); imports several `ui/*.js` modules. **NO `export` statements** (side-effect entry module). NOTE: the browser terminal *frontend* is `ui/terminal/` (HTML/JS/CSS); `engine/terminal/api/` is the read-only backend. All UI changes in this task go in `ui/terminal/`.
- `tools/run_ui_checks.mjs` — the `npm run check:ui` / `npm run test:ui` runner. Test allowlists are the arrays `NODE_TESTS` (declaration at line **11**) and `PYTEST_UI_TESTS` (declaration at line **30**). New tests run in the gate ONLY if added to these arrays. The runner also invokes `tools/check_local_asset_refs.py` and `tools/check_dashboard_ui_contract.py`.
- `tools/check_dashboard_ui_contract.py` — `DASHBOARD_HTML_SURFACES` (line 30) is `("ui/dashboard.html", "ui/data_sources.html", "ui/terminal/terminal.html")` — all three target surfaces are scanned; any asset ref you add must resolve to a tracked file.
- Existing `.mjs` test pattern: `tests/test_terminal_decision_overlays.mjs` imports pure helpers from `../ui/decision_overlays.js` (which uses named `export`, e.g. line 54 onward) using `node:test` + `node:assert/strict`. Follow this exact pattern.
- Existing secret-leak test pattern: `tests/test_data_source_log_redaction.py` and `tests/test_secret_redaction.py` (generate a canary, assert it never appears). Follow this pattern.
- Environment check (verified now): `node --version` = v20.19.4, `npm --version` = 10.8.2, so the UI gate CAN run here. Still verify this yourself at the start of Implementation step 0 — if a future sandbox lacks Node 20 / npm 10, the gate is unrunnable and you must mark the gate DoD items BLOCKED rather than leaving an unsatisfiable checkbox.

### Implementation steps
0. **Verify the gate environment first.** Run `node --version` and `npm --version`. If Node is not >=20 or npm not >=10, the `check:ui`/`test:ui` gate cannot run; proceed with all code/test/doc work, run the `node --test` and `pytest` validation commands directly (they only need Node and Python), and mark the two `npm run …` DoD items as BLOCKED-ENVIRONMENT in your NO-GO notes instead of claiming pass.
1. **Create a pure, importable FX formatting module** `ui/fx_format.js` (ES module with named exports). Implement and export at least:
   - `isFxSymbol(symbol)` — true for FX pairs (e.g. `EURUSD`, `USD/JPY`, `EUR_USD`). NOTE on symbol convention: FX-02 is the system-wide single source of truth for the canonical stored symbol form, but this is a browser module and cannot import the Python FX-02 accessor. So accept all three forms tolerantly (`/`, `_`, and concatenated 6-letter) and treat that tolerance as a deliberate, test-pinned mirror of FX-02's convention. Document in a comment that the canonical stored form is owned by FX-02 and this JS only normalizes for display.
   - `pipDecimals(symbol)` — returns price decimal places: 3 for JPY-quoted pairs, 5 for other majors; a safe default for non-FX.
   - `formatFxPrice(symbol, price)` — formats a price using `pipDecimals`, returning a placeholder (e.g. `"—"`) for non-finite input.
   - `pipValueDisplay(symbol, priceA, priceB)` — pip distance between two prices (in pips, JPY-aware), for showing spread in pips.
   - `formatLotQty(symbol, units, lotSize)` — render quantity in lots and units (e.g. `1.5 lots (150,000)`), defaulting `lotSize` to 100,000 for standard FX lots when not supplied, and degrading gracefully for non-FX (plain qty).
   - Keep this module free of DOM and of any `import` from side-effect entry modules so it is trivially unit-testable. Note: pip-size truth in Python lives in FX-02; this JS table is an accepted display-only duplicate pinned by tests.
2. **Create a pure, importable FX session module** `ui/fx_session.js` (ES module) that MIRRORS FX-04's `fx_clock.py` calendar. Implement and export at least:
   - `fxSessionStatus(nowMs, opts)` — returns `{ open: boolean, label: string, nextChangeMs: number|null }` implementing the 24/5 FX calendar: market opens ~Sunday 22:00 UTC and closes ~Friday 22:00 UTC (use UTC; make the open/close weekday+hours overridable via `opts` so they can be pinned to FX-04's configured boundaries and so tests are deterministic). `label` should be human-readable, e.g. `"FX market open"` / `"FX market closed — opens Sun 22:00 UTC"`.
   - The function must be deterministic given an injected `nowMs`; do NOT read wall-clock inside the pure function.
   - Add a comment stating that FX-04's `engine/data/prices/fx_clock.py` is the canonical session-boundary owner and this module must stay boundary-equivalent to it.
3. **Data-sources page (status + test):**
   - In `ui/data_sources.js`, inside `renderSourceCards()` (line 520), when a source is FX-class (detect via the source's `source_type`/`provider_name`/template `category` — reuse `guideForSource`/`deriveSourceState`; do not hardcode a single key), add an FX badge/pill and surface the latest test/status text already present in the payload (`status`/`ok` from the source row). Do not add new fetches; rely on the existing `/api/data_sources` payload already loaded into `state`.
   - Ensure the existing per-source "Test" action in the detail pane (which POSTs to `/api/data_sources/test`) is reachable for the FX source. If the detail pane already renders a generic Test button for every source, no new button is needed — just confirm and, if necessary, label the rendered test result as "FX feed connectivity". Render only `status`/`ok`/latency/`detail`/`message` fields from the response; NEVER render credential fields.
   - Add minimal styling to `ui/data_sources.css` for the FX badge (reuse existing `.pill` classes where possible).
4. **Dashboard (positions / sleeve / leverage / pip-lot):**
   - In `ui/dashboard.js` `loadPositionsExposureScreen()` (line 5058), after the existing `Promise.allSettled` reads (line 5063), compute FX-specific views from the ALREADY-FETCHED payloads (`portfolio`, `riskPortfolio`, `broker`, `terminal`, `uiMetrics`). Do not add new fetch calls unless a required field lives in a payload not already fetched here; if so, add the read to the existing `Promise.allSettled` array (only an existing `/api/...` read route from Verified anchors).
   - Read FX sleeve exposure and FX leverage defensively from the payload fields FX-05's read-model serialization provides (e.g. `riskPortfolio`/`uiMetrics`/`portfolio` sleeve/leverage fields surfaced from FX-05's `info_json`). Use the `ui/fx_format.js` helpers to render pip/lot sizing for FX positions.
   - Add FX rows/tiles into the existing `#positionsExposureGrid` (line 4237) using the same `.opsStat`/`.opsStatLabel`/`.opsStatValue` markup already in `ui/dashboard.html` (template tile at line 4238). Add at minimum: FX sleeve gross/net exposure, FX leverage, and an FX positions count. Append FX context to `#positionsExposureNotes` via the existing `renderNotes("positionsExposureNotes", ...)` call (line 5284).
   - If FX-05/FX-06 fields are absent (the expected sandbox state), render an explicit "FX exposure: data not yet available" tile (not zeros that could be mistaken for real values) and record the gap.
   - Add new HTML mount points only if needed; prefer reusing `#positionsExposureGrid`. Any new element must keep `id`s stable for the contract test.
5. **Browser terminal (FX symbols + formatting + session):**
   - In `ui/terminal/terminal.js`, import the pure helpers from `../fx_format.js` and `../fx_session.js`. Replace/augment the price and quantity rendering for the chart title (`#chartTitle`, line 97), positions table (`#posTbl`, line 159), and account/preview areas so that when the active symbol `isFxSymbol(...)`, prices use `formatFxPrice` and quantities use `formatLotQty`; non-FX symbols keep current `fmtNum` (line 91) behavior.
   - Add a 24/5 session indicator: render `fxSessionStatus(Date.now(), ...)` `.label` into the terminal status banner (`#terminalStatusBanner`, line 79) or `#chartHealth` (line 98) when the active symbol is FX. Keep it out of the way / hidden for non-FX symbols.
   - Ensure FX symbols appear in the watchlist: the watchlist comes from `/api/terminal/watchlist` (FX-01 puts FX symbols in the price tables that endpoint reads). No backend change — just confirm FX symbols render and are selectable, and that selecting one applies FX formatting. Add a filter-safe rendering path (the existing watchlist filter must still work with `/`-containing FX symbols). In this sandbox FX rows may be absent from the watchlist; that is acceptable — confirm the rendering path is correct, do not fabricate symbols.
   - Do NOT add or wire any order action for FX beyond what already exists; the BUY/SELL/FLATTEN controls remain governed by the existing safety gate and are out of scope to change.
6. **Asset refs:** Every new `<script type="module" src="...">` or `import` you add must point at a tracked file (`tools/check_local_asset_refs.py` + `tools/check_dashboard_ui_contract.py` enforce this). `git add` `ui/fx_format.js` and `ui/fx_session.js` so they are tracked before running the gate.
7. **Register tests in the gate:** Add your new `.mjs` test paths to the `NODE_TESTS` array (line 11) and your new Python contract test paths to the `PYTEST_UI_TESTS` array (line 30) in `tools/run_ui_checks.mjs`. Tests not in these arrays do not run in `check:ui`/`test:ui`.
8. **Docs:** Update `ui/README.md` (document the FX surfacing: which page shows what, the new modules, the "data not yet available" degradation contract, and that `ui/fx_session.js` mirrors FX-04's `fx_clock.py` boundaries) and add an FX operator subsection to `docs/README_OPERATOR_GUIDE.md` (how to read FX feed status, FX sleeve/leverage on the dashboard, and the 24/5 session indicator). Note explicitly that no secret values are ever shown.

### Tests to add
- `tests/test_fx_format.mjs` — `node:test` unit tests for `ui/fx_format.js`: JPY pairs format to 3 decimals; majors to 5; `isFxSymbol` accepts `EURUSD`/`EUR/USD`/`EUR_USD` and rejects `SPY`/`BTCUSD`-style equities/crypto if those are non-FX in this repo's convention; `formatLotQty` renders lots+units and degrades for non-FX; `pipValueDisplay` computes JPY-aware pip distance; non-finite inputs return the placeholder.
- `tests/test_fx_session.mjs` — `node:test` unit tests for `ui/fx_session.js`: open during a Wednesday UTC midday timestamp; closed during Saturday; closed before Sunday 22:00 UTC and open after; `nextChangeMs` is in the future; deterministic given injected `nowMs` and overridden open/close hours; one assertion pins the overridable boundaries to a known value to demonstrate FX-04 boundary-equivalence.
- `tests/test_fx_ui_contract.py` — Python static contract test (no server, no credentials) asserting: `ui/data_sources.html`, `ui/dashboard.html`, `ui/terminal/terminal.html` still reference all required `id`s; `ui/fx_format.js` and `ui/fx_session.js` are tracked and referenced (imported) by the entry modules; the new modules contain no `fetch(`/no network calls and no `process.env`/credential tokens; and that `tools/run_ui_checks.mjs` `NODE_TESTS`/`PYTEST_UI_TESTS` arrays include the new test paths (regex-scan the runner file).
- `tests/test_fx_ui_no_secret_leak.py` — secret-canary test following `tests/test_data_source_log_redaction.py`: assert that no FX UI source file (`ui/fx_format.js`, `ui/fx_session.js`, and the FX-touched sections of `ui/data_sources.js`/`ui/dashboard.js`/`ui/terminal/terminal.js`) embeds any credential-shaped literal, and that the data-sources/test render path only references whitelisted result keys (`status`, `ok`, `latency`, `detail`, `message`) — never `secret`/`token`/`key`/`password`/`credential` value fields. Use a generated canary and assert it is absent.

### Definition of Done
- [ ] `ui/fx_format.js` and `ui/fx_session.js` exist, are tracked in git, are pure (no DOM, no fetch, no env), and export the functions listed in steps 1-2; `ui/fx_session.js` is boundary-equivalent to FX-04's `fx_clock.py` (documented + test-pinned).
- [ ] Data-sources page shows an FX badge + latest status for the FX source and exposes the existing connectivity test, rendering only `status`/`ok`/`latency`/`detail`/`message` — never any credential field. (If no FX source is registered because FX-01 is unmerged, the badge path is wired but inactive — record as placeholder.)
- [ ] Dashboard "Positions & Exposure" card shows FX sleeve exposure, FX leverage, FX positions count, and pip/lot-formatted FX position sizing — or an explicit "data not yet available" tile if FX-05/FX-06 fields are absent (gap recorded). In this sandbox the placeholder path is the expected result; that still satisfies this item only as a placeholder, not as live data.
- [ ] Browser terminal formats FX prices with pip-correct decimals (3 JPY / 5 majors), shows lot-aware quantities, displays a 24/5 session indicator for FX symbols, and leaves non-FX rendering unchanged.
- [ ] No new API routes; only the verified existing `/api/data_sources`, `/api/data_sources/test`, `/api/portfolio`, `/api/risk/*`, `/api/broker`, `/api/terminal/*`, `/api/ui/metrics` reads are used.
- [ ] No backend file changed (`engine/**`, `services/**`, `routes/**`, `engine/runtime/storage.py` untouched).
- [ ] New `.mjs` tests added to `NODE_TESTS` (line 11) and new `.py` tests added to `PYTEST_UI_TESTS` (line 30) in `tools/run_ui_checks.mjs`.
- [ ] All new tests pass via direct `node --test` / `pytest` runs. If Node 20 / npm 10 are present, `npm run check:ui` and `npm run test:ui` pass (exit 0); if absent, those two items are marked BLOCKED-ENVIRONMENT (step 0).
- [ ] No credential/secret value appears in any UI file or test fixture; canary leak test passes.
- [ ] `ui/README.md` and `docs/README_OPERATOR_GUIDE.md` updated.
- [ ] Working tree contains only intended UI/test/doc files (verified by human review of `git status --short --untracked-files=all`, not by its exit code); `python tools/git_worktree_triage.py` is clean.

### Validation commands
Run from `/home/david/gitsandbox/system/system` (expected exit code in parentheses):
- `node --version && npm --version` -> 0 (gate-environment precheck; if Node <20 or npm <10, mark the two `npm run …` items BLOCKED-ENVIRONMENT below)
- `node --test tests/test_fx_format.mjs tests/test_fx_session.mjs` -> 0
- `python -m pytest tests/test_fx_ui_contract.py tests/test_fx_ui_no_secret_leak.py -q` -> 0
- `python tools/check_local_asset_refs.py` -> 0
- `python tools/check_dashboard_ui_contract.py --node-executable "$(command -v node)"` -> 0
- `npm run check:ui` -> 0 (only if Node 20 / npm 10 available; else BLOCKED-ENVIRONMENT, not a pass)
- `npm run test:ui` -> 0 (only if Node 20 / npm 10 available; else BLOCKED-ENVIRONMENT, not a pass)
- `git status --short --untracked-files=all` — review output by eye: ONLY intended `ui/`, `tests/`, `tools/run_ui_checks.mjs`, and docs files may appear. (NOTE: `git status` returns exit 0 regardless of contents, so its exit code is NOT a correctness signal — the human/agent review of the listed files is the real check.)
- `python tools/git_worktree_triage.py` -> 0
- `python tools/validate_repo.py` -> 0 (run to confirm no repo-wide regression. This is a pure-UI task; if `validate_repo` fans out heavy backend/repo-wide gates that fail for environment reasons unrelated to your UI change, capture the exact failing gate names and state explicitly that they are pre-existing/environmental and unrelated — do NOT paper over them and do NOT claim this DoD item passed if it did not.)

### Self-audit & NO-GO
After implementing, audit your own work and report:
1. Exact list of files changed/created and one line each on why (expect: `ui/fx_format.js`, `ui/fx_session.js`, edits to `ui/data_sources.js`, `ui/data_sources.css`, `ui/dashboard.js`, `ui/terminal/terminal.js`, possibly small `ui/dashboard.html`/`ui/terminal/terminal.html` mount additions, `tools/run_ui_checks.mjs` allowlist edits, the four test files, and the two docs). Confirm NO backend file (`engine/**`, `services/**`, `routes/**`, `engine/runtime/storage.py`) was modified.
2. Confirm the FX logic that must be correct (pip decimals, lot math, 24/5 session boundaries) lives in the runtime-shipped JS modules (`ui/fx_format.js`, `ui/fx_session.js`) and is exercised by the UI code paths — NOT only asserted in tests. Point to the call sites in `ui/dashboard.js` and `ui/terminal/terminal.js`. Confirm `ui/fx_session.js` boundaries are documented as a mirror of FX-04's `fx_clock.py`.
3. Confirm with the canary leak test output that no credential value can reach any payload, and that the data-sources test render path whitelists result keys (`status`/`ok`/`latency`/`detail`/`message`).
4. Confirm the new tests are actually wired into `NODE_TESTS` (line 11) / `PYTEST_UI_TESTS` (line 30) and that they ran. Quote the runner output lines: from `npm run check:ui`/`npm run test:ui` if the gate ran, otherwise from the direct `node --test`/`pytest` invocations (and state the gate was BLOCKED-ENVIRONMENT).
5. If any prerequisite field from FX-01/FX-05/FX-06 was missing and you rendered a placeholder, list each missing field, the file/line of the placeholder, and what FX-0x must supply. In particular, note whether the FX-05 `info_json`→read-model serialization was present; if absent, state that the dashboard FX tiles are placeholder-only and that FX-05 owns wiring those fields into `/api/portfolio` / `/api/risk/portfolio` / `/api/ui/metrics`.
6. If ANY Definition-of-Done item is not fully implemented — e.g. FX symbols could not be verified flowing into the watchlist because FX-01 is not merged in this sandbox; dashboard/terminal FX values are placeholder-only because FX-05/FX-06 data is absent; or `npm run check:ui` could not run because Node 20 / npm 10 was unavailable — declare **NO-GO**, state exactly which items remain (and whether each is BLOCKED-UPSTREAM-DATA or BLOCKED-ENVIRONMENT), and what is needed to close them. Do not claim success if the gate did not actually pass or if FX values shown are placeholders rather than live data.

---
