Integration ID: INT-02
Scope: S09-S12
Goal: Verify that the execution boundary, projector reads, control-plane entrypoint split, and post-bind runtime-boot extraction work together on one repo state.

Slices covered:
- `S09` durable `order_commands` / `order_events`
- `S10` post-trade projector reads
- `S11` control-plane entrypoint split
- `S12` post-bind runtime-boot extraction

Files reviewed:
- `engine/execution/order_command_boundary.py`
- `engine/execution/execution_ledger.py`
- `engine/runtime/trade_lifecycle_projection.py`
- `engine/runtime/trade_lifecycle.py`
- `engine/execution/trade_attribution_ledger.py`
- `engine/api/server.py`
- `engine/runtime/dashboard_runtime_boot.py`
- `dashboard_server.py`

Checks performed:
- Confirmed the durable execution boundary still feeds `S10` projector reads through `order_events`.
- Confirmed lifecycle and attribution reads can reconstruct from projected events without requiring legacy execution tables.
- Confirmed `engine.api.server` now owns startup entrypoint behavior while `dashboard_server` remains the compatibility surface for route contracts.
- Confirmed post-bind runtime boot ownership moved into `engine/runtime/dashboard_runtime_boot.py` without disturbing dashboard route imports or validation entrypoint checks.

Verification:
- `python -m pytest tests/test_broker_order_idempotency_regressions.py -q`
- `python -m pytest tests/test_broker_apply_orders_modes.py -q`
- `python -m pytest tests/test_trade_lifecycle_regressions.py -q`
- `python -m pytest tests/test_dashboard_route_contracts.py -q`
- `python -m pytest tests/test_validate_repo_contract.py -q`
- `python -m pytest tests/test_runtime_graph_check.py -q`

Result:
- Findings: none.
- Integration gate status: passed.

Notes:
- `dashboard_server.py` remains a large compatibility surface. `S11-S12` intentionally moved ownership seams first rather than attempting a route-registry rewrite.
- The remaining repo work after `S12` is operational rollout toward external services and production migration, not unfinished slice scaffolding.
