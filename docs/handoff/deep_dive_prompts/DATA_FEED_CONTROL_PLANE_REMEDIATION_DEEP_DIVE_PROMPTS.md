# Data Feed Control-Plane Remediation Deep Dive Prompts

Use one prompt per implementation workstream. These prompts are derived from the live data-feed audit and the follow-up credential-path review. Each prompt asks for design, implementation, production enforcement, documentation, and validation for one recommendation.

Global constraints for every prompt:
- Target repo: `/home/david/gitsandbox/system/system`.
- Never print, log, echo, commit, or test with real secret values. Use generated canary strings in tests and assert they never appear in logs or API responses.
- Never call live broker order, cancel, replace, or flatten paths. Alpaca and IBKR work is read-only only.
- Keep changes production-enforced in runtime code, not only in tests or docs.

## DF-01 - P0 data-source credential log leak and redaction

Review the full data-source credential save, audit, logging, telemetry mirror, and log-read paths, especially `services/data_source_manager.py`, `engine/runtime/data_source_log_store.py`, `engine/runtime/telemetry_read_router.py`, Timescale telemetry mirroring, and `/api/data_sources/logs`. Fix the P0 leak where `_upsert_source()` builds a record containing plaintext `credentials` and passes it into `log_event()`. Implement a single production redaction/sanitization boundary for data-source log details that drops or masks credential-bearing fields before persistence and also redacts on read as defense in depth. Include nested-key redaction for names such as `credentials`, `api_key`, `api_token`, `client_secret`, `secret`, `token`, `password`, and `credentials_enc`, without corrupting non-secret status fields. Add a one-time cleanup or migration path that removes or redacts existing leaked `data_source_logs.detail_json` rows in SQLite and Timescale, and make that cleanup idempotent and safe to run in production. Add regression tests that save a source with a unique generated canary secret and prove the canary does not appear in `data_source_logs`, `/api/data_sources/logs`, telemetry read-router results, audit details, mirrored Timescale rows if covered by the repo test harness, or warning/error logs. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## DF-02 - Enriched declarative provider catalog as the UI source of truth

Review `services/data_source_manager.py`, `ui/data_sources.js`, `ui/data_sources.html`, `ui/data_sources.css`, `routes/data_sources_routes.py`, and any docs that explain data-source setup. Replace the current split-brain model where backend `SourceDefinition` only emits field names while frontend `SOURCE_GUIDES` hardcodes operator guidance. Design and implement an enriched catalog schema, using dataclasses or equivalent structured objects, so each credential and setting field carries field name, env var, label, help text, signup or docs URL, paid/free plan note, required/optional state, secret flag, validation hint or regex, placeholder guidance, and safety warnings where needed. Emit this metadata from `GET /api/data_sources` and render it directly in the data-sources UI. Remove or reduce hardcoded frontend guidance so adding a provider does not require editing JavaScript copy. Keep builtin source identity and routing locked. Add server-side validation for invalid fields and optional format checks, with UI inline validation that never exposes secret values. Include tests for template payload shape, UI rendering of metadata, source update validation, and backward compatibility for existing rows. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## DF-03 - Shared provider-account credential layer

Review the data-source catalog, credential encryption helpers, data-source persistence schema, runtime environment projection, and the UI flow for sources that reuse the same provider key. Design and implement a provider-account layer so the operator enters common credentials once and dependent feeds inherit them. Cover at least Polygon (`POLYGON_API_KEY` for REST, WebSocket, options, ETF flows, CUSIP lookup), FMP (`FMP_API_KEY` for transcripts, earnings, ETF fallback, 13F CUSIP fallback), SEC identity (`SEC_USER_AGENT`, `SEC_FROM` for SEC filings, Form 4, 13F), Reddit OAuth, Quiver, SimFin/Sharadar, Tradier, FRED, and optional OpenAI embeddings. The account layer must store credentials with the same AES-GCM master-key path, mask all responses, support source-level overrides when explicitly needed, and project one effective credential per env var to jobs without duplicating or conflicting values. Add UI affordances for account status, inherited vs overridden credentials, and "used by these feeds" references. Update tests to prove a Polygon account enables all Polygon-dependent feeds, source overrides work, masked values are not resubmitted literally, clearing an account withdraws inherited runtime credentials, and the credential cache is cleared before retest. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## DF-04 - Complete catalog coverage for every discovered feed

Review the authoritative feed inventory from provider definitions, ingest jobs, `get_data_credential()` callers, source keys, and storage tables. Add data-source catalog entries for every feed that is currently env-only or orphaned: ETF flows, institutional 13F, CFTC COT, FINRA short volume and short interest, crypto positioning/funding, Polygon options, news-flow embeddings, Alpaca broker data, and any other discovered source without a first-class data-source row. For keyless feeds, implement enable/disable, truthful test, populate/status, and documentation. For keyed feeds, wire credentials through the enriched catalog and provider-account layer. For broker data, keep the surface explicitly read-only and separate order authority from data-source permissioning. Each catalog entry must map source key, provider name, job name, credentials, settings, storage table(s), consumer(s), default enabled state, and whether the source is safe to auto-enable. Add tests proving `GET /api/data_sources` lists every feed, no feed with `get_data_credential()` remains without UI guidance unless intentionally excluded with a documented reason, and default-off/default-on states match production safety. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## DF-05 - Truthful universal connection tests and removal of fake-green status

Review `services/data_source_manager.py:test_connection`, every provider's existing liveness function, and the UI handling of test results. Remove the generic successful `connection_test_not_required` fall-through for real feeds. Design a provider-test registry where each source type has an explicit test implementation or an explicit unsupported/not-testable status that is not counted as success. Implement minimal, rate-limited, provider-specific probes for Polygon REST and WebSocket auth, Tradier options, Finnhub company news, FMP transcripts and earnings, Reddit PRAW OAuth, Quiver, SimFin/Sharadar, FRED with ALFRED fallback clearly marked degraded, yfinance, ccxt, GDELT, SEC filings, Form 4, congressional trades, CFTC, FINRA, crypto positioning, RSS feeds, weather forecasts and alerts, StockTwits, ETF flows, 13F, news-flow embeddings when OpenAI backend is enabled, Alpaca read-only account/positions/orders, and IBKR authenticated market-data or historical-data read. Stop immediately on 429/503 and report retry guidance. Never place, replace, cancel, or flatten orders. Tests must classify missing credentials, wrong credentials, provider unreachable, rate limited, entitlement missing, empty payload, and real success distinctly. The UI must show pass/fail/degraded/unsupported with evidence and next steps, not just green/red. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## DF-06 - Credential resolution parity and one-click Test & Save

Review `engine/data/_credentials.py`, `services/data_source_manager.py`, `routes/data_sources_routes.py`, `ui/data_sources.js`, and any test fixtures around credential storage. Today connection tests read stored credential dicts while ingestion resolves credentials through `<NAME>_FILE`, `<NAME>_SECRET`, secret provider, then plain env, with a 300 second cache. Design and implement parity so data-source tests use the same effective credential resolution as ingestion, including UI-stored credentials, provider-account credentials, runtime credential files in strict/prod mode, existing env/file/secret-provider credentials, and cache clearing. Add a "Test & Save" flow that validates input, stores encrypted credentials, projects or resolves the effective credential, clears `get_data_credential()` cache, runs the provider's real liveness probe, and records a truthful status in one operator action. Prevent masked `***` values from being saved as real credentials. Expose exact missing env vars and where to obtain them from the catalog metadata. Add tests for UI-stored credentials, env-only credentials, `_FILE` credentials, missing master key, strict projection, masked-value rejection, cache clearing after rotation, and a failed test not leaving the source in a misleading healthy state. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## DF-07 - Runtime permissioning, job gating, and runnable-state truth

Review `DataSourceManager.build_job_environment`, `apply_runtime_environment`, `get_desired_ingestion_jobs`, provider-registry overrides, supervisor/job reconciliation, and data-source runtime snapshots. Fix the gap where enabled rows can add desired jobs even when required credentials are unavailable or projection skipped the source. Implement a first-class runnable state per source and per job: off, enabled-missing-credential, enabled-credentialed-not-scheduled, scheduled-waiting, running, degraded, failed, and healthy. Make desired job calculation credential-aware for credentialed feeds while still allowing keyless feeds. Disable must withdraw projected credentials, remove or stop the relevant managed job, and update runtime status. Enable must not report success as "running" until the job is desired, projected, and either started or explicitly pending. Surface these states in `/api/data_sources`, `/api/data_sources/logs`, provider health, and the UI. Include tests for enabling a missing-key feed, entering a key and enabling it, clearing a key, disabling a feed, multi-source shared jobs such as `ingest_now`, and no enabled-but-unrunnable job silently looping. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## DF-08 - Unified broker data sources with read-only safety enforcement

Review Alpaca and IBKR broker modules, broker config routes, data-source routes, kill/live execution gates, and existing tests around broker order safety. Add broker data sources to the data-sources UI for read-only data operations while preserving a hard separation from trading authority. Alpaca support should accept key ID, secret key, base URL with paper as the safe default, and read-only tests for account, positions, open orders listing, and optionally trade-updates WebSocket observation without any order mutation. IBKR support should accept host, port, client ID, market data type, and perform an authenticated market-data or historical-data read against a running TWS/Gateway, not only a socket check. Implement production guardrails that make it impossible for data-source test, enable, populate, or health paths to call Alpaca submit/cancel/flatten or IBKR place/cancel/flatten/order-submit helpers. Add code-level wrappers, dependency injection, or static allowlists so tests can assert forbidden functions are never reachable. Update the UI copy to make broker data read-only and to explain gateway/account prerequisites. Add tests for read-only success, missing credentials, rejected credentials, live Alpaca base URL warning or block according to policy, IBKR gateway absent, and forbidden order-path call prevention. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## DF-09 - Populate-now, storage evidence, and data-contract verification

Review ingestion jobs, provider tests, storage schemas, data-source status/logging, and dashboard reads. Implement a safe "Populate Now" or equivalent one-shot path for each feed that runs the smallest provider-specific ingestion required to prove real rows land in the expected storage table. The path must obey provider rate limits, never backfill broadly by default, never call broker order paths, and stop on 429/503. For every source, define the data contract in code: normalized shape, required fields, units, symbol namespace, timestamp timezone, point-in-time availability semantics, unique key/idempotent upsert, storage table, and consumer. Record per-source evidence after populate: provider evidence, row count, storage table, latest timestamp, latency, missing/null counts, duplicate drops, stale/gap status, and contract pass/warn/fail. Surface this evidence in `/api/data_sources`, logs, and the UI. Add tests for at least one price feed, one credentialed API feed, one keyless external feed, one PIT feed, one RSS feed, and one broker read-only feed using mocked provider responses and real test storage. Include production checks that block "healthy" status when no rows landed or the contract failed. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## DF-10 - Hardening public endpoint feeds and degraded-mode reporting

Review the public/keyless or weakly-authenticated feeds that failed or degraded in live validation: Form 4, congressional trades, FINRA short volume and short interest, GDELT, StockTwits, RSS feeds, FRED/ALFRED macro fallback, SEC identity, and NWS weather alerts. For each feed, design the smallest robust production fix: correct URLs and headers, provider-specific user agents, endpoint fallback or disable behavior, 401/403/429/503 classification, no retry storms, schema validation, empty-payload detection, and clear degraded status. Fix Form 4 document discovery so it locates and parses the actual XML information document rather than treating an HTML primary document as success. Fix congressional and FINRA fetches or mark them disabled/degraded with operator instructions if provider access has changed. Add GDELT cooldown/backoff on 429 and RSS per-feed status so one bad source does not make all RSS appear failed. Make macro status explicitly say FRED key missing and ALFRED fallback used when that happens. Include tests with mocked 200, 401/403, 429, 503, empty, malformed, and valid payloads for these feeds. Add optional live-smoke commands behind an explicit env flag so CI does not hit public services by default. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.
