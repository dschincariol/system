# Ingestion Performance and Reliability Deep Dive Prompts

Use one prompt per implementation workstream. Each prompt asks for design, implementation, production enforcement, documentation, and validation for the named recommendation.

## C1 - Price write path: COPY/staging upsert

Review the current price persistence hot path, especially `engine/runtime/storage_pg_prices.py`, and design the optimal bulk write implementation for large fast ingestion. Implement binary `COPY` into pre-created session TEMP or fixed UNLOGGED staging tables, followed by one set-based `INSERT ... SELECT DISTINCT ON (...) ... ON CONFLICT DO UPDATE` per target table. Preserve idempotency, deduplicate intra-batch conflict keys, avoid DDL per flush, retain a true multi-row VALUES fallback under the Postgres bind-parameter limit, and make the default path observable with metrics for COPY usage, fallback usage, row counts, and failures. Include tests that prove `executemany` is not used for the bulk upsert path, duplicate keys in one batch do not fail, fallback works, and schema setup does not run per flush. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## C2 - Storage placement and WAL archive outage guard

Review the production compose, env, backup, and preflight paths that control TimescaleDB, Redis, MinIO, runtime state, and WAL archive placement. Implement a production-enforced storage placement gate that prevents TimescaleDB from starting when `PGDATA`, `pg_wal`, Redis, MinIO, or backup roots are on root Docker storage instead of the approved ZFS-backed mount. Fix active and example env defaults, add a compose-level preflight or init step that gates the DB container before startup, keep WAL archiving real rather than `/bin/true`, and add checks/alerts for `pg_stat_archiver`, `pg_wal` growth, mount identity, and free space. Include tests that prove bad `/var/lib/docker` placement blocks startup/preflight and good ZFS placement passes. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## C3 - Durable async writer buffer

Review `engine/runtime/async_writer.py`, the dead-letter path, shutdown handling, and any existing SQLite helpers. Replace the volatile in-memory queue as the source of truth with a durable SQLite WAL spool: enqueue persists the serialized envelope, writer workers read committed batches, database upsert commits first, then spool rows are deleted. Use idempotent replay on startup, bounded disk usage, corruption detection, explicit metrics for queued/spooled/deleted/replayed/dropped rows, and a shutdown drain with a hard deadline and residual-loss metric. Choose the fastest safe default durability for fast market data, document the tradeoff, and make any stricter mode explicit. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## C4 - Options poll batching and parallelism

Review `engine/data/options_poll.py` end to end. Remove N+1 symbol state reads, N+1 provider fetch serialization, and per-symbol fsync commits by bulk-loading symbol state once per cycle, using a bounded provider fetch pool, batching commits at 25 to 50 symbols or one transaction per cycle, and routing high-volume options writes through the same COPY/staging or durable buffered write pattern used for price ingestion. Preserve provider failover, entitlement handling, rate limits, idempotency, and partial-cycle recovery. Add tests for 600-symbol cycles proving bounded HTTP concurrency, one state load query, batched commits, and no per-symbol write path for bulk contracts/quotes. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## H1 - Relaxed durability for refetchable ingestion writes

Review all Postgres write paths and classify writes by durability requirement. Implement an explicit refetchable-data durability tier that applies `SET LOCAL synchronous_commit = off` only to approved price, market-data, and telemetry writes, never order, ledger, risk, capital, or audit-critical writes. Make the setting env-gated, allowlisted by production code, visible in telemetry, and covered by tests that prove non-approved tables cannot opt into relaxed durability. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## H2 - Multi-worker async price writer

Review async price persistence concurrency. Implement a small configurable writer-worker pool with deterministic sharding by symbol or conflict key so ordering-sensitive upserts remain stable while throughput uses multiple database connections. Validate pool sizing against `TIMESCALE_PRICES_POOL_MAX_SIZE`, preserve graceful shutdown, integrate with the durable spool, and expose per-worker metrics for lag, batch size, write latency, failures, and backpressure. Include tests for shard determinism, concurrent workers, pool-capacity rejection, and clean shutdown. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## H3 - Backpressure instead of silent data loss

Review every caller of `enqueue_price_persistence()` and every producer loop that can generate fast market data. Implement explicit high-watermark backpressure before overflow, make enqueue rejection visible to the producer, throttle or pause providers when the durable buffer is saturated, and ensure failed flushes spill/retry from durable storage rather than disappearing into a write-only dead-letter path. Add metrics and health state for active backpressure, rejected rows, oldest spooled age, and recovery. Include tests proving producers consume the enqueue result and slow down instead of dropping silently. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## H4 - yfinance batched live quotes

Review `engine/data/live_prices/yfinance_live.py` and replace the one-request-per-symbol hot path with a batched quote/download path. Use `yf.download` or the Yahoo quote endpoint in chunks with one shared session, keep the v8 chart path only as bounded fallback, reduce fallback range to `1d`, remove the 64-symbol cap for the batch path, and make partial batch failures observable. Include tests for multi-symbol batch parsing, fallback behavior, timeout handling, and no silent symbol drops past the old cap. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## H5 - ccxt exchange reuse and batched tickers

Review `engine/data/live_prices/ccxt_live.py`. Cache exchange instances, markets, sessions, and rate-limit state across cycles, then use `fetch_tickers([...])` when supported with a safe per-symbol fallback when not. Preserve exchange-specific symbol normalization, timeout behavior, and rate limits. Add metrics for batch vs fallback usage and tests proving one exchange instance and one batch call serve a multi-symbol cycle when the exchange supports it. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## H6 - Horizontal ingestion sharding

Review supervisor, polling, options, and child process lock ownership. Add `INGESTION_SHARD_INDEX` and `INGESTION_SHARD_COUNT`, partition symbol universes by a stable hash, include shard identity in job locks and liveness records, propagate shard env to child processes, and make stale-lock takeover work per shard. Ensure singleton jobs remain singleton and sharded jobs only process their assigned symbols. Add tests for stable partitioning, no overlap, full coverage, invalid env rejection, per-shard locks, and standby takeover behavior. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## H7 - Cache single-flight on Redis miss

Review `engine/cache/store.py` and all hot wrappers using it. Implement per-key single-flight miss handling: after a Redis miss, acquire a bounded per-key lock, re-check Redis, let exactly one caller run the loader, and have waiters reuse the loaded value. Add jittered TTLs to prevent synchronized expiry storms, bounded lock cleanup, loader-failure behavior, and metrics for single-flight waits/wins/failures. Include tests that simulate many concurrent callers and prove only one database loader executes. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## H8 - Cache read_many with Redis MGET and batch loaders

Review hot cache read call sites that fetch one symbol/key at a time. Add a production `read_many(keys, batch_loader)` API that uses one Redis `MGET`, one batch loader for misses, and one pipeline to backfill missed keys with jittered TTLs. Convert the hottest symbol-read paths to use it, including feature snapshot/latest-price reads where applicable. Preserve ordering, missing-key semantics, serialization behavior, and single-flight protection for batch misses. Include tests for hit/miss mixtures, loader errors, duplicate keys, and one database query per batch. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## H9 - Read-router Postgres pooling and short TTL caching

Review telemetry and market read routers that open Postgres connections per request. Implement module-level lazy connection pools keyed by DSN and role, honor configured min/max pool sizes, memoize env parsing, and apply tiny TTL caches to dashboard/API reads where stale data under one second is acceptable. Ensure pools close cleanly in tests and process shutdown. Include tests proving repeated reads reuse pooled connections and hot reads are cached without affecting write paths. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## H10 - L1 process cache for hot typed wrappers

Review typed cache wrappers such as kill switch, execution mode, and feature snapshots. Add a small in-process L1 cache in front of Redis for safe hot reads with TTLs around one second, explicit invalidation on writes, and safety exceptions for values that must never be cached while armed/live. Preserve correctness for live trading gates. Include tests for L1 hits, invalidation, TTL expiry, Redis outage behavior, and no caching for unsafe armed states. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## H11 - live_cache Lua/msgpack one-RTT writes

Review `engine/runtime/live_cache.py` and `engine/data/price_cache.py`. Replace WATCH/MULTI read-modify-write and stdlib JSON on the hot path with a loaded Lua script that atomically overwrites only newer values in one Redis RTT, using msgpack bytes and `decode_responses=False`. Keep safe fallback behavior, preflight msgpack availability, NOSCRIPT recovery, and metrics for evalsha/write path/fallback. Remove confirmatory GETs that do not affect correctness. Include tests for newer/older timestamp races, script reload, legacy JSON compatibility if retained, and no SET-then-GET readback. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## H12 - Supervisor hot-loop snapshot caching

Review `engine/runtime/ingestion_runtime.py` supervisor loops for repeated full-table scans and decrypt-heavy config reads. Add bounded TTL snapshot caches for latest price state, data source config, heartbeat scans, and other hot loop queries, with explicit invalidation when config changes. Preserve fast detection of dead children and stale providers. Include tests proving the loop reuses snapshots within TTL, refreshes after TTL/invalidation, and reduces query count without hiding liveness failures. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

Implementation note: the supervisor now uses the bounded `INGESTION_RUNTIME_SNAPSHOT_CACHE_TTL_S` cache for price/provider rows, enabled price-provider config, child heartbeat scans, desired child jobs, and per-child config hashes. `services.data_source_manager.mark_runtime_dirty()` invalidates same-process supervisor config snapshots and writes `data_sources_reload_ts_ms`; `ingestion_runtime` reads that marker directly during control-plane refresh, bypassing long-lived `runtime_meta` caching, and invalidates child-control-plane/provider snapshots before reconciliation.

## H13 - Parallel REST provider polling

Review `engine/data/poll_prices.py` provider orchestration. Replace strictly serial provider polling with a bounded `ThreadPoolExecutor` over independent REST providers while preserving per-provider rate limits, error isolation, deterministic merge behavior, and serialized database writes. Remove hot-loop pool teardown that defeats connection reuse. Include tests for provider parallelism, timeout isolation, deterministic results, and no `close_pooled_connections()` call in the heartbeat loop. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## H14 - Durable buffered writes for non-price ingestion

Review non-price ingestion jobs under `engine/data` and `engine/jobs` that write synchronously or buffer unbounded data during database slowdown. Design and implement a shared durable buffered writer interface for high-volume non-price data, reusing the async writer spool/backpressure model where appropriate while keeping table-specific schemas and idempotency. Convert the riskiest non-price paths first and add metrics for queue depth, oldest age, retries, drops, and backpressure. Include tests proving a simulated database outage does not cause unbounded memory growth or silent data loss. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## H15 - Feature registry TTL caches

Review `engine/strategy/feature_registry.py` resolve paths. Add TTL-bounded process caches keyed by `include_shadow` and stage for registered feature IDs and allowlists, use `frozenset` for membership checks, guard schema setup with a process-level readiness flag, and invalidate caches from discovery registration paths. Do not use an unbounded cache that can hide fresh feature changes. Include tests for cache hit behavior, TTL refresh, explicit invalidation, stage separation, and schema setup only once per process. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## H16 - asyncpg Timescale COPY/staging

Review `engine/runtime/timescale_client.py` append-heavy write paths. Raise batch sizing to a safe 2000 to 5000 range where appropriate, then implement asyncpg `copy_records_to_table` into staging tables followed by set-based `INSERT ... SELECT DISTINCT ON ... ON CONFLICT` upserts. Avoid DDL per flush by preparing session TEMP or fixed staging tables once per connection where possible, retain fallback for unsupported tables, and expose COPY/fallback metrics. Include tests for batch sizing, duplicate conflict keys, fallback behavior, and no per-flush staging DDL on the optimized path. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## PGW-4 - Cache SQL normalization

Review the Postgres SQL normalization/probing logic in `engine/runtime/storage_pg.py`. Add a bounded thread-safe cache keyed by raw SQL text so repeated statements do not pay regex normalization and classification work on every execute. Preserve existing telemetry labels, avoid unbounded memory growth, and include a way to clear the cache in tests. Add tests proving repeated identical SQL normalizes once while different SQL still classifies correctly. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## PG-04 - Hypertable chunk interval tuning

Review Timescale hypertable creation for price sidecar and high-rate ingestion tables. Source chunk intervals from the table classification/schema policy layer instead of defaulting to broad 7-day chunks, and assign smaller chunks for high-rate ticks while preserving sensible defaults for low-rate tables. Add migrations or idempotent ALTER behavior for existing deployments, telemetry for actual chunk settings, and tests proving each table class receives the intended interval. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## PG-05 - Timescale compression orderby

Review Timescale compression policy setup. Add `compress_orderby` for each compressed table using its real time column and preserve existing `segmentby` choices. Make the setup idempotent for fresh and existing hypertables, document compression ordering by table class, and add tests that inspect generated SQL/options for expected `compress_orderby` values. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## ING-07 - Persist restart-storm accounting

Review supervisor restart-storm protection in `engine/runtime/ingestion_runtime.py`. Move restart accounting from process memory into the database control plane using job locks or a dedicated table, implement a token-bucket or sliding-window policy that survives supervisor restarts, and expose operator metrics/events when jobs are suppressed. Preserve manual override and recovery behavior. Add tests proving restart counts survive process restart and stale windows expire correctly. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

Implementation note: child restart attempts are now persisted as expiring
`job_locks` rows under `ingestion_restart_guard/v1::` and counted as a
sliding window by `ingestion_runtime`. Suppression emits
`ingestion_child_restart_suppressed_total`, an
`ingestion_child_restart_suppressed` event-log row, and
`market_data/child_restart_guard_triggered`; `runtime_meta.ingestion_state`
children include the active `restart_guard` snapshot. Stable child runs,
config changes, and operator `restart_feeds` clear the persisted accounting.

## CONC-03 - Reuse yfinance session and executor

Review yfinance fallback/network code for per-fetch session and thread-pool churn. Introduce a module-level `requests.Session` with an HTTPAdapter sized for expected concurrency and a reused bounded executor for fallback per-symbol work, with safe shutdown/reset hooks for tests. Preserve timeout, retry, and provider behavior. Add tests proving repeated cycles reuse session/executor resources and do not leak threads. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

Implementation note: yfinance live polling now uses a process-level `requests.Session`
with an `HTTPAdapter` pool sized to at least `YFINANCE_LIVE_MAX_WORKERS`, and batch
download timeout wrappers plus per-symbol chart fallback both submit work to one
reused bounded module-level executor. `shutdown_yfinance_resources()` and
`reset_yfinance_resources_for_tests()` close the session and executor deterministically
for tests and controlled teardown.

## CONC-06 - Scope Polygon WS locks

Review Polygon WebSocket message handling in `engine/jobs/stream_prices_polygon_ws.py`. Move JSON parsing, validation, microstructure math, and other CPU work outside the per-symbol/shared lock, leaving only minimal mutation of shared `_last` state under lock. Preserve ordering guarantees and flush interaction. Add tests or concurrency harnesses proving lock hold time shrinks and concurrent messages for different symbols do not block on unnecessary CPU work. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

Implementation note: live Polygon websocket handling is enforced in
`engine/data/provider_sessions/polygon_ws_session.py`, which is the
`stream_prices_polygon_ws.py` production path, and the legacy `_WsIngest`
callback in `engine/jobs/stream_prices_polygon_ws.py` follows the same contract.
Incoming payloads are parsed and normalized before the shared session lock is
acquired. Numeric validation/coercion, quote spread calculation, and legacy
trade/quote microstructure updates run outside the lock against a copied symbol
snapshot. The locked sections only reject already-stale/duplicate stream events,
copy the current `_last` record, commit the replacement record, and mutate the
pending flush queue/telemetry counters. Commit-time stale/duplicate checks remain
authoritative after the CPU work; if a same-symbol update wins between snapshot
and commit, the record is recomputed from the newer snapshot before commit.
Regression coverage in `tests/test_polygon_ws_backpressure.py` blocks lock-held
normalization/record building, proves different-symbol message CPU work can
overlap while `snapshot()` remains available to flush, verifies stale/duplicate
events skip record construction, verifies same-symbol stale drops, and checks
flush queue order.

## CONC-07 - Cap BLAS, NumExpr, and torch threads per process

Review CPU thread configuration across supervised ingestion, inference, and runtime startup. Implement a concurrency-aware policy that caps `OMP_NUM_THREADS`, `MKL_NUM_THREADS`, `OPENBLAS_NUM_THREADS`, `NUMEXPR_NUM_THREADS`, and torch thread counts based on process role and total supervised process count. Enforce the policy before heavy libraries initialize, expose it in runtime snapshots, and allow explicit operator override. Add tests for env generation, role-specific defaults, and no oversubscription under many child processes. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## CONC-08 - Remove yfinance silent symbol cap

Review the yfinance symbol cap and truncation behavior. Ensure the batched path supports the configured universe without silent drops, convert any remaining hard caps into explicit operator-configured limits with warnings/metrics, and make fallback caps visible as degraded mode rather than normal success. Add tests proving more than 64 symbols are processed on the batch path and any configured cap reports exactly which symbols were skipped. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## PHL-03 - Batch news_flow embedding reads and writes

Review `engine/data/news_flow.py` for per-event recent-embedding queries and SELECT-then-write transactions. Implement one prefetch for all event embeddings needed in a cycle, batch writes/upserts, and preserve duplicate detection/idempotency. Add metrics for batch size and database round trips. Include tests proving a multi-event cycle performs one recent-embedding query and one batched write path rather than per-event queries. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## PHL-04 - Batch poll_prices recent history reads

Review `engine/data/poll_prices.py` recent-price history loading and split-detection dependencies. Replace unconditional per-symbol reads with one batch query for all symbols in the cycle, while preserving split detection and stale-data logic. Add tests proving one query handles many symbols and split detection output remains unchanged. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## PHL-06 - Polygon batch snapshot fallback

Review `engine/data/live_prices/polygon_live.py`. Replace per-symbol `get_latest` fallback and redundant entitlement probes with Polygon batch snapshot endpoints where available, and degrade cleanly to per-symbol fallback only for unsupported cases. Preserve provider rate limits and error classification. Add tests for one batch call serving many symbols, entitlement failure handling, and no repeated 3N request pattern. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## PHL-08 - Batch price_cache Redis read-modify-write

Review `engine/data/price_cache.py` for per-symbol Redis read-modify-write behavior. Remove confirmatory GETs that do not affect correctness, pipeline or Lua-batch updates where freshness comparisons are required, and batch multi-symbol writes from ingestion cycles. Preserve stale-timestamp protection and metrics. Add tests proving a multi-symbol write uses one pipeline/script operation rather than sequential GET/SET calls. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## ING-06 - Keep Postgres pools warm in poll_prices

Review `engine/data/poll_prices.py` for calls that close or reset pooled database connections inside heartbeat or polling loops. Remove hot-loop pool teardown, keep the pool warm across cycles, and reserve pool reset for fatal connection states. Add tests proving normal heartbeats do not call `close_pooled_connections()` and database slowdown does not trigger connection stampedes. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## PGW-6 - Coalesce small write transactions

Review `run_write_txn` and small-write call sites in `engine/runtime/storage_pg.py`. Design a safe batching or connection-reuse strategy for repeated small writes that avoids acquire/commit/close per call while preserving transaction boundaries for critical operations. Apply it only where semantics allow, add metrics for coalesced writes, and include tests proving critical writes still commit independently while batchable writes reuse a connection or transaction. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

Status: Implemented for Postgres job-liveness small writes. `touch_job_lock(..., best_effort=True)` and `put_job_heartbeat(..., best_effort=True)` now enqueue by `job_name`, merge repeated heartbeat metadata, and flush through one `run_write_txn` connection/commit per batch. Critical lock acquire/release and non-best-effort liveness writes keep independent transactions. Metrics emitted from flushes: `storage_pg_liveness_flush_batches_total`, `storage_pg_liveness_flushed_rows_total`, and `storage_pg_liveness_coalesced_writes_total`.

## REL-03 - Circuit breaker for price write retries

Review retry and pool-reset behavior in `engine/runtime/storage_pg_prices.py`. Stop resetting the entire pool on every transient retry, classify fatal vs retryable failures, and add a circuit breaker around `write_batch` so sustained DB slowdown triggers backpressure instead of connection stampedes. Preserve eventual replay from durable spool. Add tests for transient failures, fatal failures, circuit-open behavior, and no pool reset on ordinary retryable errors. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## REL-06 - Supervisor drain before SIGKILL

Review supervisor process stop/escalation behavior. Replace the 0.10 second pre-flush grace with a proper drain protocol that signals children, waits for async writer and telemetry buffers up to a configurable deadline, records residual risk, and only then escalates to SIGKILL. Preserve emergency kill behavior for hung processes. Add tests for graceful drain, timeout escalation, and residual-drop metrics. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## REL-07 - Producer-side deterministic raw event keys

Review raw-event idempotency key generation across producers and storage backends. Compute a deterministic `event_key` once at the producer edge using stable identifiers and timestamps, exclude mutable floats, carry it through every downstream write, and align conflict targets across SQLite/Postgres/Timescale paths. Add migration or compatibility handling for existing rows. Include tests proving retries and backend cutover do not create duplicate raw events. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## REL-08 - Telemetry shutdown drain policy

Review `engine/runtime/telemetry_append_buffer.py` shutdown behavior. Implement a distinct shutdown-drain policy with more generous attempts/deadline than steady-state flushing, observable residual drops, and retry behavior that does not block indefinitely. Preserve fast normal flushes. Add tests for shutdown contention, successful delayed drain, timeout, and metrics emitted on residual loss. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## REL-10 - Fail-closed recovery and gap metrics

Review ingestion and crash recovery loops for broad swallowed exceptions. Replace silent exception swallowing with explicit failure states, gap metrics, and fail-closed behavior for live order authority when recovery cannot prove continuity. Preserve best-effort recovery for non-critical telemetry. Add tests proving recovery failures block live order enablement and emit actionable metrics/logs instead of disappearing. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## DS-02 - Feature store JSON serialization

Review feature snapshot serialization in `engine/strategy/feature_store.py`. Remove avoidable `sort_keys=True` on large feature dicts unless deterministic ordering is required by a tested contract, and evaluate switching hot serialization to `orjson` or the repository codec layer. Preserve JSONB compatibility and deterministic test fixtures where needed. Add benchmarks or targeted tests proving serialization output remains valid and CPU time is reduced. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## DS-04 - Remove redundant row dict copies

Review hot ingestion data flow around `async_writer.py`, price router, and storage normalization. Identify redundant `dict()` copies across layers, establish one normalization/copy boundary, and pass immutable or trusted row mappings internally where safe. Preserve mutation safety at public boundaries and tests that prove callers cannot observe unintended mutation. Add microbenchmarks or instrumentation for copy reduction on large batches. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

Status: Implemented for the price-ingestion hot path. `async_writer.py` now shards trusted row mappings without a defensive per-row `dict()` clone and relies on `async_writer_spool.py` as the single public mutation-safety/copy boundary before durable JSON serialization. Spool replay trusts JSON-decoded row dicts instead of cloning them again. `storage_pg_prices.write_batch()` performs one normalization pass directly over trusted mappings, materializing only SQL/COPY tuples and counting fallback copies for non-mapping inputs. Production snapshots/metrics expose `row_copy_avoided_rows`, `row_copy_fallback_rows`, and `normalization_input_rows`; `tools/ingestion_spine_benchmark.py --skip-postgres` includes a local `price_row_copy_boundary` benchmark for large synthetic batches.

## DS-06 - Use orjson on hot decode/encode paths

Review WebSocket and polling hot paths that decode or encode large JSON payloads, especially Polygon WS. Replace stdlib `json` with `orjson` or the repository codec layer where behavior is compatible, keeping fallbacks and type normalization explicit. Preserve error handling and logging. Add tests for bytes/string inputs, invalid JSON, numeric handling, and performance-sensitive call sites using the faster codec. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

Implementation note: `engine.runtime.json_codec` is now the repository JSON
codec for ingestion hot paths and prefers the pinned runtime `orjson`
dependency, with explicit stdlib fallback. The active Polygon WebSocket session
uses it for message decode and auth/subscribe/unsubscribe frame encode; the
legacy stream callback uses it for compatible receive/control-frame/replay-state
paths; Polygon REST snapshot/replay polling decodes raw response bytes through
it instead of `Response.json()` when content is available; and `poll_prices`
uses it for per-cycle symbol metadata and provider-health JSON. Regression
coverage lives in `tests/test_runtime_json_codec.py`,
`tests/test_polygon_ws_backpressure.py`, and `tests/test_polygon_live.py`.

## DS-08 - Optimize write_batch row normalization

Review per-row work in `storage_pg_prices.write_batch`, including `_safe_float`, `_safe_int`, datetime conversion, symbol parsing, and event-key string construction. Refactor into one explicit normalization pass per row that computes shared values once and feeds all target-table row builders. Preserve validation behavior and idempotency. Add tests for edge-case input rows and a targeted benchmark or instrumentation proving fewer repeated conversions per row. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## PGW-5 - Remove redundant search_path checkout work

Review connection checkout/setup paths for redundant `SET search_path` calls or dead setup code. Remove repeated no-op work where session state is already guaranteed, or make it conditional on a tracked connection state marker. Preserve schema safety for new connections and reset connections. Add tests proving fresh connections set the path once and repeated checkouts do not issue redundant SQL. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

2026-06-22 implemented: `engine/runtime/storage_pool.py` now tracks the configured
`search_path` schema on each psycopg connection and skips redundant checkout SQL
when the marker matches the current schema. `engine/runtime/storage_pg.py`
invalidates the marker for wrapper-executed `SET search_path`, `RESET`, and
`DISCARD` statements so the next checkout restores the runtime schema. Regression
coverage lives in `tests/test_storage_pg_runtime_regressions.py`, and runtime
documentation is in `engine/runtime/README.md`.

## PG-06 - Reconcile divergent Timescale price schemas

Review the two Timescale price schema definitions and all code paths that create, migrate, or query them. Consolidate the source of truth, add compatibility migrations if required, and remove divergent duplicate definitions. Preserve deployed data compatibility and document canonical table/column ownership. Add tests proving all schema creation paths emit the same canonical definition. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## PG-07 - Env-gated relaxed durability tier

Review relaxed durability configuration and production env allowlists. Implement a clear env-gated tier for refetchable ingestion writes, with defaults, docs, preflight visibility, and hard guardrails preventing accidental use on order/ledger/risk tables. Add tests for env parsing, default behavior, allowed scopes, rejected scopes, and documentation/allowlist consistency. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## PHL-07 - Avoid duplicate symbol-set loading

Review `poll_prices` startup and per-cycle symbol discovery. Remove duplicate symbol-set loads in a single cycle by computing the active universe once, caching it for the cycle, and passing it to downstream provider/history/cache logic. Preserve dynamic symbol changes between cycles. Add tests proving one cycle performs one symbol load and the next cycle observes changes. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## CACHE-6 - Remove extra Redis GETs per write

Review cache write paths for confirmatory or redundant Redis GETs. Remove GETs that do not change correctness, replace freshness checks with Lua or pipeline-based atomic operations where needed, and expose metrics for write path mode. Add tests proving writes preserve stale-update protection without extra round trips. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

2026-06-22 implementation note: `engine/cache/store.py` removes the generic write-through before/after Redis payload-version `GET`s and emits `cache_write_through_path_total` mode counters after Postgres-first cache updates. `write_through_many()` batches Redis sets through a pipeline when supported while preserving exact write TTLs, and falls back to sequential sets only for clients without pipeline support. `engine/runtime/live_cache.py` keeps stale-update protection inside the existing Lua `EVALSHA` script and emits `live_cache_redis_write_path_total` for accepted, stale-rejected, and failed Lua writes. `tests/test_cache_write_through.py` and `tests/test_live_cache.py` assert no client-side Redis `GET` occurs on these write paths, that stale live snapshots are rejected, and that write-path mode metrics are emitted. `engine/cache/README.md` and `docs/OBSERVABILITY.md` document the no-readback and Lua write metrics boundary.

## CACHE-7 - Add negative caching to safe L1 paths

Review L1 cache wrappers for repeated misses that hammer Redis or Postgres. Add short negative caching only where absence is safe to cache, with explicit invalidation on writes and no negative cache for safety-critical live trading gates. Add tests for miss caching, invalidation, TTL expiry, and excluded unsafe keys. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

2026-06-22 implementation note: safe L1 negative caching is limited to
`engine/cache/wrappers/feature_snapshots.py`, where a confirmed empty
`model_feature_snapshots` latest-row query stores the shared
`L1_MISSING` sentinel for `L1_NEGATIVE_CACHE_TTL_S = 1.0` and skips the
next Redis/Postgres miss. Batch `latest_many()` applies the same sentinel
only for symbols absent after a successful batch query. `store_latest()` and
`prime_feature_snapshot()` invalidate the affected L1 key before replacing
the sentinel with the committed payload. Safety-critical live gates are
excluded: kill-switch reads do not negative-cache missing snapshots and
execution-mode L1 does not store `live` + `armed=1`; those policies are
documented in `engine/cache/README.md` and covered by
`tests/test_cache_wrappers_integration.py`.

## CACHE-8 - Require msgpack for production cache codec

Review cache codec initialization and external service readiness. Prevent silent downgrade to JSON in production when msgpack is required for hot paths, while preserving an explicit development fallback if desired. Add preflight checks, startup health output, and tests proving production readiness fails when msgpack is unavailable. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## CACHE-9 - Optimize Redis SCAN/delete and pool ping behavior

Review Redis cache maintenance and pool access. Replace SCAN plus per-key DELETE with batched UNLINK/DEL pipelines, avoid pinging Redis on every pool call, and add health checks at bounded intervals instead. Preserve correctness for namespace clears and stale connection recovery. Add tests proving large clears are pipelined and repeated pool access does not ping every time. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.

## DS-07 - Narrow global WebSocket parse lock

Review WebSocket receive loops for global locks around parse or per-message CPU work. Move parsing and validation outside global locks, use per-symbol or minimal critical sections for shared state mutation, and verify flush coordination remains correct. Add tests or instrumentation proving unrelated symbols can be processed concurrently and lock hold time excludes parse work. After implementation, audit your own work. Show exact files changed, why each change is required, and how the fix is enforced in production code rather than only tests/docs. Update documentation to reflect changes made. Run targeted tests for the changed behavior, then run git status --short --untracked-files=all, python tools/git_worktree_triage.py, and the relevant validators. Capture exact exit codes and key output lines. If any requirement is not fully implemented, say NO-GO and explain the remaining work.
