Integration ID: INT-01
Scope: S01-S03
Goal: Verify that the first three migration slices work together without cross-slice regressions before opening `S04`.

Slices covered:
- `S01` hot-path raw/telemetry deferral fix
- `S02` shared ingest write policy
- `S03` live-ingestion schema extraction

Files reviewed:
- `engine/runtime/price_router.py`
- `engine/runtime/timeseries_write_policy.py`
- `engine/runtime/ingestion_status.py`
- `engine/runtime/storage.py`
- `engine/runtime/storage_live_ingestion_schema.py`
- `engine/data/poll_prices.py`
- `tests/test_storage_contracts.py`

Checks performed:
- Confirmed the shared ingest write-policy surface still feeds the extracted storage-owned live-ingestion schema path through `storage.py` compatibility wrappers.
- Confirmed `price_router.py` still imports storage-owned schema helpers from `storage.py`, not directly from the extracted module, so the call boundary stayed stable across `S02` and `S03`.
- Confirmed `tests/test_storage_contracts.py` now treats `engine/runtime/storage_live_ingestion_schema.py` as the canonical DDL owner for the extracted live-ingestion family.
- Confirmed no schema-version increment or `repair_schema.py` dependency was introduced across `S01-S03`.

Verification:
- `python -m pytest tests/test_storage_contracts.py -q`
- `python -m pytest tests/test_ingestion_runtime_reliability.py -q`
- `python -m pytest tests/test_trade_lifecycle_regressions.py -q`
- `python -m pytest tests/test_sqlite_contention_relief.py -q -k "not writer"`
- `python -m pytest tests/test_sqlite_contention_relief.py -q -k "writer"`

Result:
- Findings: none.
- Integration gate status: passed.

Notes:
- The command runner remains intermittently flaky on full straight replays of `tests/test_sqlite_contention_relief.py -q`. The same suite passed intact during slice verification, and both audit partitions passed during this integration audit.
- `engine/runtime/storage.py` and `engine/data/poll_prices.py` are noisy files in a dirty worktree. The integration audit stayed scoped to the `S01-S03` boundary rather than treating the entire file-level git diff as slice-owned.
